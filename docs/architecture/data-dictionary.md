# MateryalPH Data Dictionary

> Generated by `php artisan materyalph:schema-document` from the fully migrated PostgreSQL `public` schema. Do not edit generated table definitions by hand.

All `*_centavos` columns store integer Philippine centavos. All `timestamp with time zone` values are persisted in UTC and business deadlines are rendered in Asia/Manila.

## `account_email_changes`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `auth_session_id` | `uuid` | No | — | Auth session id. |
| `normalized_email` | `character varying` | No | — | Normalized email. |
| `email_otp_id` | `uuid` | No | — | Email otp id. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `completed_at` | `timestamp with time zone` | Yes | — | Completed at. |
| `invalidated_at` | `timestamp with time zone` | Yes | — | Invalidated at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `account_email_changes_auth_session_id_foreign` — FOREIGN KEY: `FOREIGN KEY (auth_session_id) REFERENCES auth_sessions(id) ON DELETE RESTRICT`
- `account_email_changes_email_otp_id_foreign` — FOREIGN KEY: `FOREIGN KEY (email_otp_id) REFERENCES email_otps(id) ON DELETE RESTRICT`
- `account_email_changes_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `account_email_changes_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `account_email_changes_pkey` — `CREATE UNIQUE INDEX account_email_changes_pkey ON public.account_email_changes USING btree (id)`
- `account_email_changes_user_id_completed_at_index` — `CREATE INDEX account_email_changes_user_id_completed_at_index ON public.account_email_changes USING btree (user_id, completed_at)`

## `account_factor_replacements`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `auth_session_id` | `uuid` | No | — | Auth session id. |
| `encrypted_secret` | `text` | No | — | Encrypted secret. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `completed_at` | `timestamp with time zone` | Yes | — | Completed at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `account_factor_replacements_auth_session_id_foreign` — FOREIGN KEY: `FOREIGN KEY (auth_session_id) REFERENCES auth_sessions(id) ON DELETE RESTRICT`
- `account_factor_replacements_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `account_factor_replacements_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `account_factor_replacements_pkey` — `CREATE UNIQUE INDEX account_factor_replacements_pkey ON public.account_factor_replacements USING btree (id)`

## `addresses`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `owner_type` | `character varying` | No | — | Owner type. |
| `owner_id` | `uuid` | No | — | Owner id. |
| `label` | `character varying` | Yes | — | Label. |
| `formatted_address` | `text` | No | — | Formatted address. |
| `psgc_code` | `character varying` | Yes | — | Psgc code. |
| `latitude` | `numeric` | Yes | — | Latitude. |
| `longitude` | `numeric` | Yes | — | Longitude. |
| `geography_wkt` | `text` | Yes | — | Geography wkt. |
| `verified` | `boolean` | No | `false` | Verified. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `location` | `geography` | Yes | — | Location. |

**Constraints**

- `addresses_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `addresses_location_gist` — `CREATE INDEX addresses_location_gist ON public.addresses USING gist (location)`
- `addresses_owner_type_owner_id_index` — `CREATE INDEX addresses_owner_type_owner_id_index ON public.addresses USING btree (owner_type, owner_id)`
- `addresses_pkey` — `CREATE UNIQUE INDEX addresses_pkey ON public.addresses USING btree (id)`
- `addresses_psgc_code_index` — `CREATE INDEX addresses_psgc_code_index ON public.addresses USING btree (psgc_code)`

## `admin_invitations`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `normalized_email` | `character varying` | No | — | Normalized email. |
| `platform_role_id` | `uuid` | No | — | Platform role id. |
| `invited_by_user_id` | `bigint` | Yes | — | Invited by user id. |
| `token_hash` | `character varying` | No | — | Token hash. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `accepted_at` | `timestamp with time zone` | Yes | — | Accepted at. |
| `bootstrap` | `boolean` | No | `false` | Bootstrap. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `revoked_at` | `timestamp with time zone` | Yes | — | Revoked at. |

**Constraints**

- `admin_invitations_invited_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (invited_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `admin_invitations_platform_role_id_foreign` — FOREIGN KEY: `FOREIGN KEY (platform_role_id) REFERENCES platform_roles(id) ON DELETE RESTRICT`
- `admin_invitations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `admin_invitations_token_hash_unique` — UNIQUE: `UNIQUE (token_hash)`

**Indexes**

- `admin_invitations_normalized_email_index` — `CREATE INDEX admin_invitations_normalized_email_index ON public.admin_invitations USING btree (normalized_email)`
- `admin_invitations_pkey` — `CREATE UNIQUE INDEX admin_invitations_pkey ON public.admin_invitations USING btree (id)`
- `admin_invitations_token_hash_unique` — `CREATE UNIQUE INDEX admin_invitations_token_hash_unique ON public.admin_invitations USING btree (token_hash)`

## `admin_memberships`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `platform_role_id` | `uuid` | No | — | Platform role id. |
| `status` | `character varying` | No | `'PENDING'::character varying` | Status. |
| `invited_by_user_id` | `bigint` | Yes | — | Invited by user id. |
| `activated_at` | `timestamp with time zone` | Yes | — | Activated at. |
| `deactivated_at` | `timestamp with time zone` | Yes | — | Deactivated at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `admin_memberships_invited_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (invited_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `admin_memberships_platform_role_id_foreign` — FOREIGN KEY: `FOREIGN KEY (platform_role_id) REFERENCES platform_roles(id) ON DELETE RESTRICT`
- `admin_memberships_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `admin_memberships_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `admin_memberships_user_id_unique` — UNIQUE: `UNIQUE (user_id)`

**Indexes**

- `admin_memberships_pkey` — `CREATE UNIQUE INDEX admin_memberships_pkey ON public.admin_memberships USING btree (id)`
- `admin_memberships_user_id_unique` — `CREATE UNIQUE INDEX admin_memberships_user_id_unique ON public.admin_memberships USING btree (user_id)`

## `agreement_acceptances`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `agreement_version_id` | `uuid` | No | — | Agreement version id. |
| `vendor_organization_id` | `uuid` | Yes | — | Vendor organization id. |
| `source` | `character varying` | No | — | Source. |
| `accepted_at` | `timestamp with time zone` | No | — | Accepted at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `agreement_acceptances_agreement_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (agreement_version_id) REFERENCES agreement_versions(id) ON DELETE RESTRICT`
- `agreement_acceptances_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `agreement_acceptances_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `agreement_acceptances_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `agreement_acceptances_user_id_agreement_version_id_vendor_organ` — UNIQUE: `UNIQUE (user_id, agreement_version_id, vendor_organization_id)`

**Indexes**

- `agreement_acceptances_pkey` — `CREATE UNIQUE INDEX agreement_acceptances_pkey ON public.agreement_acceptances USING btree (id)`
- `agreement_acceptances_user_id_agreement_version_id_vendor_organ` — `CREATE UNIQUE INDEX agreement_acceptances_user_id_agreement_version_id_vendor_organ ON public.agreement_acceptances USING btree (user_id, agreement_version_id, vendor_organization_id)`

## `agreement_documents`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `code` | `character varying` | No | — | Code. |
| `audience` | `character varying` | No | — | Audience. |
| `title` | `character varying` | No | — | Title. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `agreement_documents_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `agreement_documents_code_unique` — UNIQUE: `UNIQUE (code)`

**Indexes**

- `agreement_documents_code_unique` — `CREATE UNIQUE INDEX agreement_documents_code_unique ON public.agreement_documents USING btree (code)`
- `agreement_documents_pkey` — `CREATE UNIQUE INDEX agreement_documents_pkey ON public.agreement_documents USING btree (id)`

## `agreement_versions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `agreement_document_id` | `uuid` | No | — | Agreement document id. |
| `version` | `integer` | No | — | Version. |
| `content_hash` | `character varying` | No | — | Content hash. |
| `content_uri` | `character varying` | No | — | Content uri. |
| `effective_at` | `timestamp with time zone` | No | — | Effective at. |
| `retired_at` | `timestamp with time zone` | Yes | — | Retired at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `requires_reacceptance` | `boolean` | No | `false` | Requires reacceptance. |

**Constraints**

- `agreement_versions_agreement_document_id_foreign` — FOREIGN KEY: `FOREIGN KEY (agreement_document_id) REFERENCES agreement_documents(id) ON DELETE RESTRICT`
- `agreement_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `agreement_versions_agreement_document_id_version_unique` — UNIQUE: `UNIQUE (agreement_document_id, version)`

**Indexes**

- `agreement_versions_agreement_document_id_version_unique` — `CREATE UNIQUE INDEX agreement_versions_agreement_document_id_version_unique ON public.agreement_versions USING btree (agreement_document_id, version)`
- `agreement_versions_pkey` — `CREATE UNIQUE INDEX agreement_versions_pkey ON public.agreement_versions USING btree (id)`

## `analytics_active_user_facts`

MAT-01–MAT-07 comparability, observation, publication, or audience-safe aggregate record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `fact_date` | `date` | No | — | Fact date. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `dataset_id` | `character varying` | No | — | Dataset id. |
| `dimension_key` | `character varying` | No | — | Dimension key. |
| `metrics` | `jsonb` | No | — | Metrics. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `analytics_active_user_facts_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `analytics_active_user_facts_environment_dataset_id_fact_date_di` — UNIQUE: `UNIQUE (environment, dataset_id, fact_date, dimension_key)`

**Indexes**

- `analytics_active_user_facts_environment_dataset_id_fact_date_di` — `CREATE UNIQUE INDEX analytics_active_user_facts_environment_dataset_id_fact_date_di ON public.analytics_active_user_facts USING btree (environment, dataset_id, fact_date, dimension_key)`
- `analytics_active_user_facts_pkey` — `CREATE UNIQUE INDEX analytics_active_user_facts_pkey ON public.analytics_active_user_facts USING btree (id)`

## `analytics_category_facts`

MAT-01–MAT-07 comparability, observation, publication, or audience-safe aggregate record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `fact_date` | `date` | No | — | Fact date. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `dataset_id` | `character varying` | No | — | Dataset id. |
| `dimension_key` | `character varying` | No | — | Dimension key. |
| `metrics` | `jsonb` | No | — | Metrics. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `analytics_category_facts_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `analytics_category_facts_environment_dataset_id_fact_date_dimen` — UNIQUE: `UNIQUE (environment, dataset_id, fact_date, dimension_key)`

**Indexes**

- `analytics_category_facts_environment_dataset_id_fact_date_dimen` — `CREATE UNIQUE INDEX analytics_category_facts_environment_dataset_id_fact_date_dimen ON public.analytics_category_facts USING btree (environment, dataset_id, fact_date, dimension_key)`
- `analytics_category_facts_pkey` — `CREATE UNIQUE INDEX analytics_category_facts_pkey ON public.analytics_category_facts USING btree (id)`

## `analytics_daily_facts`

MAT-01–MAT-07 comparability, observation, publication, or audience-safe aggregate record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `fact_date` | `date` | No | — | Fact date. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `dataset_id` | `character varying` | No | — | Dataset id. |
| `dimension_key` | `character varying` | No | — | Dimension key. |
| `metrics` | `jsonb` | No | — | Metrics. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `analytics_daily_facts_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `analytics_daily_facts_environment_dataset_id_fact_date_dimensio` — UNIQUE: `UNIQUE (environment, dataset_id, fact_date, dimension_key)`

**Indexes**

- `analytics_daily_facts_environment_dataset_id_fact_date_dimensio` — `CREATE UNIQUE INDEX analytics_daily_facts_environment_dataset_id_fact_date_dimensio ON public.analytics_daily_facts USING btree (environment, dataset_id, fact_date, dimension_key)`
- `analytics_daily_facts_pkey` — `CREATE UNIQUE INDEX analytics_daily_facts_pkey ON public.analytics_daily_facts USING btree (id)`

## `analytics_geography_facts`

MAT-01–MAT-07 comparability, observation, publication, or audience-safe aggregate record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `fact_date` | `date` | No | — | Fact date. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `dataset_id` | `character varying` | No | — | Dataset id. |
| `dimension_key` | `character varying` | No | — | Dimension key. |
| `metrics` | `jsonb` | No | — | Metrics. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `analytics_geography_facts_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `analytics_geography_facts_environment_dataset_id_fact_date_dime` — UNIQUE: `UNIQUE (environment, dataset_id, fact_date, dimension_key)`

**Indexes**

- `analytics_geography_facts_environment_dataset_id_fact_date_dime` — `CREATE UNIQUE INDEX analytics_geography_facts_environment_dataset_id_fact_date_dime ON public.analytics_geography_facts USING btree (environment, dataset_id, fact_date, dimension_key)`
- `analytics_geography_facts_pkey` — `CREATE UNIQUE INDEX analytics_geography_facts_pkey ON public.analytics_geography_facts USING btree (id)`

## `analytics_published_runs`

MAT-01–MAT-07 comparability, observation, publication, or audience-safe aggregate record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `kind` | `character varying` | No | — | Kind. |
| `environment` | `character varying` | No | — | Environment. |
| `dataset_id` | `character varying` | No | — | Dataset id. |
| `snapshot_date` | `date` | No | — | Snapshot date. |
| `analytics_refresh_run_id` | `uuid` | No | — | Analytics refresh run id. |
| `published_at` | `timestamp with time zone` | No | — | Published at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `analytics_published_run_scope_check` — CHECK: `CHECK (kind::text = 'MATERIAL_PRICE_DAILY'::text AND (environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying]::text[])))`
- `analytics_published_runs_analytics_refresh_run_id_foreign` — FOREIGN KEY: `FOREIGN KEY (analytics_refresh_run_id) REFERENCES analytics_refresh_runs(id) ON DELETE RESTRICT`
- `analytics_published_runs_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `analytics_published_run_scope_unique` — UNIQUE: `UNIQUE (kind, environment, dataset_id, snapshot_date)`
- `analytics_published_runs_analytics_refresh_run_id_unique` — UNIQUE: `UNIQUE (analytics_refresh_run_id)`

**Indexes**

- `analytics_published_run_scope_unique` — `CREATE UNIQUE INDEX analytics_published_run_scope_unique ON public.analytics_published_runs USING btree (kind, environment, dataset_id, snapshot_date)`
- `analytics_published_runs_analytics_refresh_run_id_unique` — `CREATE UNIQUE INDEX analytics_published_runs_analytics_refresh_run_id_unique ON public.analytics_published_runs USING btree (analytics_refresh_run_id)`
- `analytics_published_runs_pkey` — `CREATE UNIQUE INDEX analytics_published_runs_pkey ON public.analytics_published_runs USING btree (id)`

## `analytics_refresh_runs`

MAT-01–MAT-07 comparability, observation, publication, or audience-safe aggregate record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `kind` | `character varying` | No | — | Kind. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `dataset_id` | `character varying` | No | — | Dataset id. |
| `snapshot_date` | `date` | No | — | Snapshot date. |
| `revision` | `integer` | No | `1` | Revision. |
| `state` | `character varying` | No | `'RUNNING'::character varying` | State. |
| `observed_at` | `timestamp with time zone` | Yes | — | Observed at. |
| `source_count` | `integer` | No | `0` | Source count. |
| `selected_count` | `integer` | No | `0` | Selected count. |
| `content_hash` | `character varying` | Yes | — | Content hash. |
| `supersedes_run_id` | `uuid` | Yes | — | Supersedes run id. |
| `reviewer_reason` | `text` | Yes | — | Reviewer reason. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `error_code` | `character varying` | Yes | — | Error code. |
| `validated_at` | `timestamp with time zone` | Yes | — | Validated at. |
| `published_at` | `timestamp with time zone` | Yes | — | Published at. |

**Constraints**

- `analytics_refresh_counts_check` — CHECK: `CHECK (selected_count <= source_count AND (state::text <> 'FAILED'::text OR error_code IS NOT NULL) AND (state::text <> 'VALIDATED'::text OR validated_at IS NOT NULL) AND (state::text <> 'PUBLISHED'::text OR published_at IS NOT NULL))`
- `analytics_refresh_environment_check` — CHECK: `CHECK (environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying]::text[]))`
- `analytics_refresh_kind_check` — CHECK: `CHECK (kind::text = 'MATERIAL_PRICE_DAILY'::text)`
- `analytics_refresh_state_check` — CHECK: `CHECK (state::text = ANY (ARRAY['RUNNING'::character varying, 'VALIDATED'::character varying, 'PUBLISHED'::character varying, 'FAILED'::character varying, 'SUPERSEDED'::character varying]::text[]))`
- `analytics_refresh_runs_supersedes_run_id_foreign` — FOREIGN KEY: `FOREIGN KEY (supersedes_run_id) REFERENCES analytics_refresh_runs(id) ON DELETE RESTRICT`
- `analytics_refresh_runs_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `analytics_refresh_runs_kind_environment_dataset_id_snapshot_dat` — UNIQUE: `UNIQUE (kind, environment, dataset_id, snapshot_date, revision)`

**Indexes**

- `analytics_refresh_runs_kind_environment_dataset_id_snapshot_dat` — `CREATE UNIQUE INDEX analytics_refresh_runs_kind_environment_dataset_id_snapshot_dat ON public.analytics_refresh_runs USING btree (kind, environment, dataset_id, snapshot_date, revision)`
- `analytics_refresh_runs_pkey` — `CREATE UNIQUE INDEX analytics_refresh_runs_pkey ON public.analytics_refresh_runs USING btree (id)`

## `audit_logs`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `actor_role` | `character varying` | Yes | — | Actor role. |
| `action` | `character varying` | No | — | Action. |
| `resource_type` | `character varying` | No | — | Resource type. |
| `resource_id` | `uuid` | Yes | — | Resource id. |
| `vendor_organization_id` | `uuid` | Yes | — | Vendor organization id. |
| `before` | `jsonb` | Yes | — | Before. |
| `after` | `jsonb` | Yes | — | After. |
| `reason` | `text` | Yes | — | Reason. |
| `correlation_id` | `character varying` | No | — | Correlation id. |
| `succeeded` | `boolean` | No | `true` | Succeeded. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `audit_logs_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `audit_logs_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `audit_logs_correlation_id_index` — `CREATE INDEX audit_logs_correlation_id_index ON public.audit_logs USING btree (correlation_id)`
- `audit_logs_pkey` — `CREATE UNIQUE INDEX audit_logs_pkey ON public.audit_logs USING btree (id)`
- `audit_logs_resource_type_resource_id_index` — `CREATE INDEX audit_logs_resource_type_resource_id_index ON public.audit_logs USING btree (resource_type, resource_id)`

## `auth_refresh_token_history`

Identity and secure-session record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `auth_session_id` | `uuid` | No | — | Auth session id. |
| `family_id` | `uuid` | No | — | Family id. |
| `token_hash` | `character varying` | No | — | Token hash. |
| `rotated_at` | `timestamp with time zone` | No | — | Rotated at. |
| `reuse_detected_at` | `timestamp with time zone` | Yes | — | Reuse detected at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `auth_refresh_token_history_auth_session_id_foreign` — FOREIGN KEY: `FOREIGN KEY (auth_session_id) REFERENCES auth_sessions(id) ON DELETE RESTRICT`
- `auth_refresh_token_history_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `auth_refresh_token_history_token_hash_unique` — UNIQUE: `UNIQUE (token_hash)`

**Indexes**

- `auth_refresh_token_history_family_id_index` — `CREATE INDEX auth_refresh_token_history_family_id_index ON public.auth_refresh_token_history USING btree (family_id)`
- `auth_refresh_token_history_pkey` — `CREATE UNIQUE INDEX auth_refresh_token_history_pkey ON public.auth_refresh_token_history USING btree (id)`
- `auth_refresh_token_history_token_hash_unique` — `CREATE UNIQUE INDEX auth_refresh_token_history_token_hash_unique ON public.auth_refresh_token_history USING btree (token_hash)`

## `auth_sessions`

Identity and secure-session record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `family_id` | `uuid` | No | — | Family id. |
| `device_id` | `uuid` | Yes | — | Device id. |
| `refresh_token_hash` | `character varying` | No | — | Refresh token hash. |
| `previous_refresh_token_hash` | `character varying` | Yes | — | Previous refresh token hash. |
| `oauth_access_token_id` | `character varying` | Yes | — | Oauth access token id. |
| `client_kind` | `character varying` | No | — | Client kind. |
| `ip_address` | `character varying` | Yes | — | Ip address. |
| `user_agent_hash` | `character varying` | Yes | — | User agent hash. |
| `last_used_at` | `timestamp with time zone` | No | — | Last used at. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `revoked_at` | `timestamp with time zone` | Yes | — | Revoked at. |
| `revoke_reason` | `character varying` | Yes | — | Revoke reason. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `reauthenticated_at` | `timestamp with time zone` | Yes | — | Reauthenticated at. |
| `reauthentication_method` | `character varying` | Yes | — | Reauthentication method. |
| `reauthentication_email_otp_id` | `uuid` | Yes | — | Reauthentication email otp id. |

**Constraints**

- `auth_sessions_reauthentication_email_otp_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reauthentication_email_otp_id) REFERENCES email_otps(id) ON DELETE RESTRICT`
- `auth_sessions_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `auth_sessions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `auth_sessions_refresh_token_hash_unique` — UNIQUE: `UNIQUE (refresh_token_hash)`

**Indexes**

- `auth_sessions_device_id_index` — `CREATE INDEX auth_sessions_device_id_index ON public.auth_sessions USING btree (device_id)`
- `auth_sessions_family_id_index` — `CREATE INDEX auth_sessions_family_id_index ON public.auth_sessions USING btree (family_id)`
- `auth_sessions_oauth_access_token_id_index` — `CREATE INDEX auth_sessions_oauth_access_token_id_index ON public.auth_sessions USING btree (oauth_access_token_id)`
- `auth_sessions_pkey` — `CREATE UNIQUE INDEX auth_sessions_pkey ON public.auth_sessions USING btree (id)`
- `auth_sessions_refresh_token_hash_unique` — `CREATE UNIQUE INDEX auth_sessions_refresh_token_hash_unique ON public.auth_sessions USING btree (refresh_token_hash)`
- `auth_sessions_user_id_revoked_at_expires_at_index` — `CREATE INDEX auth_sessions_user_id_revoked_at_expires_at_index ON public.auth_sessions USING btree (user_id, revoked_at, expires_at)`

## `auto_accept_policies`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `listing_variant_id` | `uuid` | No | — | Listing variant id. |
| `enabled` | `boolean` | No | `false` | Enabled. |
| `paused` | `boolean` | No | `true` | Paused. |
| `current_version` | `integer` | No | `1` | Current version. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `auto_accept_policies_listing_variant_id_foreign` — FOREIGN KEY: `FOREIGN KEY (listing_variant_id) REFERENCES listing_variants(id) ON DELETE RESTRICT`
- `auto_accept_policies_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `auto_accept_policies_listing_variant_id_unique` — UNIQUE: `UNIQUE (listing_variant_id)`

**Indexes**

- `auto_accept_policies_listing_variant_id_unique` — `CREATE UNIQUE INDEX auto_accept_policies_listing_variant_id_unique ON public.auto_accept_policies USING btree (listing_variant_id)`
- `auto_accept_policies_pkey` — `CREATE UNIQUE INDEX auto_accept_policies_pkey ON public.auto_accept_policies USING btree (id)`

## `auto_accept_policy_versions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `auto_accept_policy_id` | `uuid` | No | — | Auto accept policy id. |
| `version` | `integer` | No | — | Version. |
| `allotment_quantity` | `numeric` | No | — | Allotment quantity. |
| `remaining_allotment_quantity` | `numeric` | No | — | Remaining allotment quantity. |
| `max_unit_count` | `numeric` | Yes | — | Max unit count. |
| `max_order_amount_centavos` | `bigint` | Yes | — | Integer Philippine centavos. Max order amount centavos. |
| `created_by_user_id` | `bigint` | No | — | Created by user id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `auto_accept_allotment_check` — CHECK: `CHECK (allotment_quantity >= 0::numeric AND remaining_allotment_quantity >= 0::numeric AND remaining_allotment_quantity <= allotment_quantity)`
- `auto_accept_policy_versions_auto_accept_policy_id_foreign` — FOREIGN KEY: `FOREIGN KEY (auto_accept_policy_id) REFERENCES auto_accept_policies(id) ON DELETE RESTRICT`
- `auto_accept_policy_versions_created_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (created_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `auto_accept_policy_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `auto_accept_policy_versions_auto_accept_policy_id_version_uniqu` — UNIQUE: `UNIQUE (auto_accept_policy_id, version)`

**Indexes**

- `auto_accept_policy_versions_auto_accept_policy_id_version_uniqu` — `CREATE UNIQUE INDEX auto_accept_policy_versions_auto_accept_policy_id_version_uniqu ON public.auto_accept_policy_versions USING btree (auto_accept_policy_id, version)`
- `auto_accept_policy_versions_pkey` — `CREATE UNIQUE INDEX auto_accept_policy_versions_pkey ON public.auto_accept_policy_versions USING btree (id)`

## `badge_definitions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `code` | `character varying` | No | — | Code. |
| `name` | `character varying` | No | — | Name. |
| `rule` | `jsonb` | No | — | Rule. |
| `version` | `integer` | No | — | Version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `badge_definitions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `badge_definitions_code_unique` — UNIQUE: `UNIQUE (code)`

**Indexes**

- `badge_definitions_code_unique` — `CREATE UNIQUE INDEX badge_definitions_code_unique ON public.badge_definitions USING btree (code)`
- `badge_definitions_pkey` — `CREATE UNIQUE INDEX badge_definitions_pkey ON public.badge_definitions USING btree (id)`

## `bot_risk_challenges`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `normalized_email_hash` | `character varying` | No | — | Normalized email hash. |
| `portal` | `character varying` | No | — | Portal. |
| `action` | `character varying` | No | — | Action. |
| `platform` | `character varying` | No | — | Platform. |
| `outcome` | `character varying` | No | — | Outcome. |
| `delivery_allowed` | `boolean` | No | `false` | Delivery allowed. |
| `proof_hash` | `character varying` | Yes | — | Proof hash. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `verified_at` | `timestamp with time zone` | Yes | — | Verified at. |
| `proof_expires_at` | `timestamp with time zone` | Yes | — | Proof expires at. |
| `consumed_at` | `timestamp with time zone` | Yes | — | Consumed at. |
| `invalidated_at` | `timestamp with time zone` | Yes | — | Invalidated at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `bot_risk_platform_check` — CHECK: `CHECK (platform::text = ANY (ARRAY['WEB'::character varying, 'ANDROID'::character varying, 'IOS'::character varying]::text[]))`
- `bot_risk_portal_check` — CHECK: `CHECK (portal::text = ANY (ARRAY['BUYER'::character varying, 'VENDOR'::character varying]::text[]))`
- `bot_risk_challenges_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `bot_risk_challenges_proof_hash_unique` — UNIQUE: `UNIQUE (proof_hash)`

**Indexes**

- `bot_risk_challenges_pkey` — `CREATE UNIQUE INDEX bot_risk_challenges_pkey ON public.bot_risk_challenges USING btree (id)`
- `bot_risk_challenges_proof_hash_unique` — `CREATE UNIQUE INDEX bot_risk_challenges_proof_hash_unique ON public.bot_risk_challenges USING btree (proof_hash)`
- `bot_risk_lookup_idx` — `CREATE INDEX bot_risk_lookup_idx ON public.bot_risk_challenges USING btree (normalized_email_hash, action, expires_at)`
- `bot_risk_proof_idx` — `CREATE INDEX bot_risk_proof_idx ON public.bot_risk_challenges USING btree (proof_hash, proof_expires_at)`

## `budget_overrides`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `project_id` | `uuid` | No | — | Project id. |
| `work_package_id` | `uuid` | Yes | — | Work package id. |
| `buyer_user_id` | `bigint` | No | — | Buyer user id. |
| `overage_centavos` | `bigint` | No | — | Integer Philippine centavos. Overage centavos. |
| `reason` | `text` | No | — | Reason. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `budget_overrides_buyer_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (buyer_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `budget_overrides_project_id_foreign` — FOREIGN KEY: `FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE RESTRICT`
- `budget_overrides_work_package_id_foreign` — FOREIGN KEY: `FOREIGN KEY (work_package_id) REFERENCES work_packages(id) ON DELETE RESTRICT`
- `budget_overrides_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `budget_overrides_pkey` — `CREATE UNIQUE INDEX budget_overrides_pkey ON public.budget_overrides USING btree (id)`

## `business_document_reviews`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `business_document_version_id` | `uuid` | No | — | Business document version id. |
| `reviewer_user_id` | `bigint` | No | — | Reviewer user id. |
| `decision` | `character varying` | No | — | Decision. |
| `reason` | `text` | Yes | — | Reason. |
| `source_reference` | `character varying` | Yes | — | Source reference. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `verified_reference` | `character varying` | Yes | — | Verified reference. |
| `verified_issued_on` | `date` | Yes | — | Verified issued on. |
| `verified_expires_on` | `date` | Yes | — | Verified expires on. |
| `verified_expiry_not_applicable` | `boolean` | No | `false` | Verified expiry not applicable. |
| `remarks` | `text` | Yes | — | Remarks. |
| `immediate_restriction` | `boolean` | No | `false` | Immediate restriction. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |

**Constraints**

- `verified_expiry_check` — CHECK: `CHECK (NOT verified_expiry_not_applicable OR verified_expires_on IS NULL)`
- `business_document_reviews_business_document_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (business_document_version_id) REFERENCES business_document_versions(id) ON DELETE RESTRICT`
- `business_document_reviews_reviewer_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reviewer_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `business_document_reviews_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `business_document_reviews_pkey` — `CREATE UNIQUE INDEX business_document_reviews_pkey ON public.business_document_reviews USING btree (id)`

## `business_document_versions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `business_document_id` | `uuid` | No | — | Business document id. |
| `version` | `integer` | No | — | Version. |
| `file_id` | `uuid` | No | — | File id. |
| `document_number_masked` | `character varying` | Yes | — | Document number masked. |
| `issued_on` | `date` | Yes | — | Issued on. |
| `expires_on` | `date` | Yes | — | Expires on. |
| `expiry_not_applicable` | `boolean` | No | `false` | Expiry not applicable. |
| `content_hash` | `character varying` | No | — | Content hash. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `evidence_origin` | `character varying` | No | `'SAMPLE_DEMO'::character varying` | Evidence origin. |
| `uploaded_by_user_id` | `bigint` | Yes | — | Uploaded by user id. |

**Constraints**

- `business_document_versions_business_document_id_foreign` — FOREIGN KEY: `FOREIGN KEY (business_document_id) REFERENCES business_documents(id) ON DELETE RESTRICT`
- `business_document_versions_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `business_document_versions_uploaded_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (uploaded_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `business_document_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `business_document_versions_business_document_id_version_unique` — UNIQUE: `UNIQUE (business_document_id, version)`

**Indexes**

- `business_document_versions_business_document_id_version_unique` — `CREATE UNIQUE INDEX business_document_versions_business_document_id_version_unique ON public.business_document_versions USING btree (business_document_id, version)`
- `business_document_versions_pkey` — `CREATE UNIQUE INDEX business_document_versions_pkey ON public.business_document_versions USING btree (id)`

## `business_documents`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `document_type` | `character varying` | No | — | Document type. |
| `current_version_id` | `uuid` | Yes | — | Current version id. |
| `status` | `character varying` | No | `'PENDING_VERIFICATION'::character varying` | Status. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `expiry_warned_at` | `timestamp with time zone` | Yes | — | Expiry warned at. |

**Constraints**

- `business_documents_current_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (current_version_id) REFERENCES business_document_versions(id) ON DELETE RESTRICT`
- `business_documents_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `business_documents_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `business_documents_vendor_organization_id_document_type_unique` — UNIQUE: `UNIQUE (vendor_organization_id, document_type)`

**Indexes**

- `business_documents_pkey` — `CREATE UNIQUE INDEX business_documents_pkey ON public.business_documents USING btree (id)`
- `business_documents_status_index` — `CREATE INDEX business_documents_status_index ON public.business_documents USING btree (status)`
- `business_documents_vendor_organization_id_document_type_unique` — `CREATE UNIQUE INDEX business_documents_vendor_organization_id_document_type_unique ON public.business_documents USING btree (vendor_organization_id, document_type)`

## `buyer_locations`

Buyer identity, profile, or saved-context record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `buyer_profile_id` | `uuid` | No | — | Buyer profile id. |
| `address_id` | `uuid` | No | — | Address id. |
| `is_primary` | `boolean` | No | `false` | Is primary. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `buyer_locations_address_id_foreign` — FOREIGN KEY: `FOREIGN KEY (address_id) REFERENCES addresses(id) ON DELETE RESTRICT`
- `buyer_locations_buyer_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (buyer_profile_id) REFERENCES buyer_profiles(id) ON DELETE RESTRICT`
- `buyer_locations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `buyer_locations_buyer_profile_id_address_id_unique` — UNIQUE: `UNIQUE (buyer_profile_id, address_id)`

**Indexes**

- `buyer_locations_buyer_profile_id_address_id_unique` — `CREATE UNIQUE INDEX buyer_locations_buyer_profile_id_address_id_unique ON public.buyer_locations USING btree (buyer_profile_id, address_id)`
- `buyer_locations_pkey` — `CREATE UNIQUE INDEX buyer_locations_pkey ON public.buyer_locations USING btree (id)`
- `buyer_one_primary_location_unique` — `CREATE UNIQUE INDEX buyer_one_primary_location_unique ON public.buyer_locations USING btree (buyer_profile_id) WHERE (is_primary = true)`

## `buyer_profiles`

Buyer identity, profile, or saved-context record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `buyer_type` | `character varying` | No | — | Buyer type. |
| `company_name` | `character varying` | Yes | — | Company name. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `buyer_profiles_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `buyer_profiles_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `buyer_profiles_user_id_unique` — UNIQUE: `UNIQUE (user_id)`

**Indexes**

- `buyer_profiles_pkey` — `CREATE UNIQUE INDEX buyer_profiles_pkey ON public.buyer_profiles USING btree (id)`
- `buyer_profiles_user_id_unique` — `CREATE UNIQUE INDEX buyer_profiles_user_id_unique ON public.buyer_profiles USING btree (user_id)`

## `buyer_ranking_preferences`

Buyer identity, profile, or saved-context record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `buyer_profile_id` | `uuid` | No | — | Buyer profile id. |
| `procurement_type` | `character varying` | No | — | Procurement type. |
| `weights` | `jsonb` | No | — | Weights. |
| `version` | `integer` | No | `1` | Version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `buyer_ranking_preferences_buyer_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (buyer_profile_id) REFERENCES buyer_profiles(id) ON DELETE RESTRICT`
- `buyer_ranking_preferences_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `buyer_ranking_preferences_buyer_profile_id_procurement_type_uni` — UNIQUE: `UNIQUE (buyer_profile_id, procurement_type)`

**Indexes**

- `buyer_ranking_preferences_buyer_profile_id_procurement_type_uni` — `CREATE UNIQUE INDEX buyer_ranking_preferences_buyer_profile_id_procurement_type_uni ON public.buyer_ranking_preferences USING btree (buyer_profile_id, procurement_type)`
- `buyer_ranking_preferences_pkey` — `CREATE UNIQUE INDEX buyer_ranking_preferences_pkey ON public.buyer_ranking_preferences USING btree (id)`

## `cache`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `key` | `character varying` | No | — | Primary key. Key. |
| `value` | `text` | No | — | Value. |
| `expiration` | `integer` | No | — | Expiration. |

**Constraints**

- `cache_pkey` — PRIMARY KEY: `PRIMARY KEY (key)`

**Indexes**

- `cache_pkey` — `CREATE UNIQUE INDEX cache_pkey ON public.cache USING btree (key)`

## `cache_locks`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `key` | `character varying` | No | — | Primary key. Key. |
| `owner` | `character varying` | No | — | Owner. |
| `expiration` | `integer` | No | — | Expiration. |

**Constraints**

- `cache_locks_pkey` — PRIMARY KEY: `PRIMARY KEY (key)`

**Indexes**

- `cache_locks_pkey` — `CREATE UNIQUE INDEX cache_locks_pkey ON public.cache_locks USING btree (key)`

## `cancellation_decisions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `reason` | `text` | Yes | — | Reason. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `cancellation_decisions_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `cancellation_decisions_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `cancellation_decisions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `cancellation_decisions_pkey` — `CREATE UNIQUE INDEX cancellation_decisions_pkey ON public.cancellation_decisions USING btree (id)`

## `cancellation_requests`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `reason` | `text` | Yes | — | Reason. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `cancellation_requests_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `cancellation_requests_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `cancellation_requests_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `cancellation_requests_pkey` — `CREATE UNIQUE INDEX cancellation_requests_pkey ON public.cancellation_requests USING btree (id)`

## `cart_items`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `cart_id` | `uuid` | No | — | Cart id. |
| `listing_variant_id` | `uuid` | No | — | Listing variant id. |
| `quantity` | `numeric` | No | — | Quantity. |
| `listing_price_version_id` | `uuid` | Yes | — | Listing price version id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `cart_items_cart_id_foreign` — FOREIGN KEY: `FOREIGN KEY (cart_id) REFERENCES carts(id) ON DELETE RESTRICT`
- `cart_items_listing_variant_id_foreign` — FOREIGN KEY: `FOREIGN KEY (listing_variant_id) REFERENCES listing_variants(id) ON DELETE RESTRICT`
- `cart_items_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `cart_items_cart_id_listing_variant_id_unique` — UNIQUE: `UNIQUE (cart_id, listing_variant_id)`

**Indexes**

- `cart_items_cart_id_listing_variant_id_unique` — `CREATE UNIQUE INDEX cart_items_cart_id_listing_variant_id_unique ON public.cart_items USING btree (cart_id, listing_variant_id)`
- `cart_items_pkey` — `CREATE UNIQUE INDEX cart_items_pkey ON public.cart_items USING btree (id)`

## `carts`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `buyer_profile_id` | `uuid` | No | — | Buyer profile id. |
| `state` | `character varying` | No | `'ACTIVE'::character varying` | State. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `carts_buyer_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (buyer_profile_id) REFERENCES buyer_profiles(id) ON DELETE RESTRICT`
- `carts_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `carts_pkey` — `CREATE UNIQUE INDEX carts_pkey ON public.carts USING btree (id)`

## `checkout_groups`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `buyer_profile_id` | `uuid` | No | — | Buyer profile id. |
| `reference` | `character varying` | No | — | Reference. |
| `state` | `character varying` | No | — | State. |
| `idempotency_key` | `character varying` | No | — | Idempotency key. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `checkout_groups_buyer_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (buyer_profile_id) REFERENCES buyer_profiles(id) ON DELETE RESTRICT`
- `checkout_groups_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `checkout_groups_idempotency_key_unique` — UNIQUE: `UNIQUE (idempotency_key)`
- `checkout_groups_reference_unique` — UNIQUE: `UNIQUE (reference)`

**Indexes**

- `checkout_groups_idempotency_key_unique` — `CREATE UNIQUE INDEX checkout_groups_idempotency_key_unique ON public.checkout_groups USING btree (idempotency_key)`
- `checkout_groups_pkey` — `CREATE UNIQUE INDEX checkout_groups_pkey ON public.checkout_groups USING btree (id)`
- `checkout_groups_reference_unique` — `CREATE UNIQUE INDEX checkout_groups_reference_unique ON public.checkout_groups USING btree (reference)`

## `checkout_vendor_groups`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `checkout_group_id` | `uuid` | No | — | Checkout group id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `order_id` | `uuid` | Yes | — | Order id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `checkout_vendor_groups_checkout_group_id_foreign` — FOREIGN KEY: `FOREIGN KEY (checkout_group_id) REFERENCES checkout_groups(id) ON DELETE RESTRICT`
- `checkout_vendor_groups_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `checkout_vendor_groups_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `checkout_vendor_groups_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `checkout_vendor_groups_checkout_group_id_vendor_organization_id` — UNIQUE: `UNIQUE (checkout_group_id, vendor_organization_id)`

**Indexes**

- `checkout_vendor_groups_checkout_group_id_vendor_organization_id` — `CREATE UNIQUE INDEX checkout_vendor_groups_checkout_group_id_vendor_organization_id ON public.checkout_vendor_groups USING btree (checkout_group_id, vendor_organization_id)`
- `checkout_vendor_groups_pkey` — `CREATE UNIQUE INDEX checkout_vendor_groups_pkey ON public.checkout_vendor_groups USING btree (id)`

## `compiled_estimate_lines`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `compiled_estimate_vendor_id` | `uuid` | No | — | Compiled estimate vendor id. |
| `work_package_line_id` | `uuid` | No | — | Work package line id. |
| `listing_variant_id` | `uuid` | Yes | — | Listing variant id. |
| `matched_quantity` | `numeric` | No | `'0'::numeric` | Matched quantity. |
| `amount_centavos` | `bigint` | Yes | — | Integer Philippine centavos. Amount centavos. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `compiled_estimate_lines_compiled_estimate_vendor_id_foreign` — FOREIGN KEY: `FOREIGN KEY (compiled_estimate_vendor_id) REFERENCES compiled_estimate_vendors(id) ON DELETE RESTRICT`
- `compiled_estimate_lines_listing_variant_id_foreign` — FOREIGN KEY: `FOREIGN KEY (listing_variant_id) REFERENCES listing_variants(id) ON DELETE RESTRICT`
- `compiled_estimate_lines_work_package_line_id_foreign` — FOREIGN KEY: `FOREIGN KEY (work_package_line_id) REFERENCES work_package_lines(id) ON DELETE RESTRICT`
- `compiled_estimate_lines_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `compiled_estimate_lines_pkey` — `CREATE UNIQUE INDEX compiled_estimate_lines_pkey ON public.compiled_estimate_lines USING btree (id)`

## `compiled_estimate_vendors`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `compiled_estimate_id` | `uuid` | No | — | Compiled estimate id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `fms` | `numeric` | No | — | Fms. |
| `projected_total_centavos` | `bigint` | No | — | Integer Philippine centavos. Projected total centavos. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `compiled_estimate_vendors_compiled_estimate_id_foreign` — FOREIGN KEY: `FOREIGN KEY (compiled_estimate_id) REFERENCES compiled_estimates(id) ON DELETE RESTRICT`
- `compiled_estimate_vendors_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `compiled_estimate_vendors_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `compiled_estimate_vendors_compiled_estimate_id_vendor_organizat` — UNIQUE: `UNIQUE (compiled_estimate_id, vendor_organization_id)`

**Indexes**

- `compiled_estimate_vendors_compiled_estimate_id_vendor_organizat` — `CREATE UNIQUE INDEX compiled_estimate_vendors_compiled_estimate_id_vendor_organizat ON public.compiled_estimate_vendors USING btree (compiled_estimate_id, vendor_organization_id)`
- `compiled_estimate_vendors_pkey` — `CREATE UNIQUE INDEX compiled_estimate_vendors_pkey ON public.compiled_estimate_vendors USING btree (id)`

## `compiled_estimates`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `work_package_version_id` | `uuid` | No | — | Work package version id. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `state` | `character varying` | No | `'ACTIVE'::character varying` | State. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `compiled_estimates_work_package_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (work_package_version_id) REFERENCES work_package_versions(id) ON DELETE RESTRICT`
- `compiled_estimates_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `compiled_estimates_pkey` — `CREATE UNIQUE INDEX compiled_estimates_pkey ON public.compiled_estimates USING btree (id)`

## `compliance_evidence`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `compliance_submission_id` | `uuid` | No | — | Compliance submission id. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `compliance_evidence_compliance_submission_id_foreign` — FOREIGN KEY: `FOREIGN KEY (compliance_submission_id) REFERENCES compliance_submissions(id) ON DELETE RESTRICT`
- `compliance_evidence_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `compliance_evidence_pkey` — `CREATE UNIQUE INDEX compliance_evidence_pkey ON public.compliance_evidence USING btree (id)`

## `compliance_extractions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `compliance_submission_id` | `uuid` | No | — | Compliance submission id. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `compliance_extractions_compliance_submission_id_foreign` — FOREIGN KEY: `FOREIGN KEY (compliance_submission_id) REFERENCES compliance_submissions(id) ON DELETE RESTRICT`
- `compliance_extractions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `compliance_extractions_pkey` — `CREATE UNIQUE INDEX compliance_extractions_pkey ON public.compliance_extractions USING btree (id)`

## `compliance_reference_matches`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `compliance_submission_id` | `uuid` | No | — | Compliance submission id. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `compliance_reference_matches_compliance_submission_id_foreign` — FOREIGN KEY: `FOREIGN KEY (compliance_submission_id) REFERENCES compliance_submissions(id) ON DELETE RESTRICT`
- `compliance_reference_matches_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `compliance_reference_matches_pkey` — `CREATE UNIQUE INDEX compliance_reference_matches_pkey ON public.compliance_reference_matches USING btree (id)`

## `compliance_reviews`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `compliance_submission_id` | `uuid` | No | — | Compliance submission id. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `reviewer_user_id` | `bigint` | Yes | — | Reviewer user id. |
| `decision` | `character varying` | Yes | — | Decision. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `compliance_reviews_compliance_submission_id_foreign` — FOREIGN KEY: `FOREIGN KEY (compliance_submission_id) REFERENCES compliance_submissions(id) ON DELETE RESTRICT`
- `compliance_reviews_reviewer_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reviewer_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `compliance_reviews_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `compliance_reviews_pkey` — `CREATE UNIQUE INDEX compliance_reviews_pkey ON public.compliance_reviews USING btree (id)`

## `compliance_submissions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_listing_id` | `uuid` | No | — | Vendor listing id. |
| `path` | `character varying` | No | — | Path. |
| `status` | `character varying` | No | `'PENDING_ADMIN_REVIEW'::character varying` | Status. |
| `version` | `integer` | No | `1` | Version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `compliance_submissions_vendor_listing_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_listing_id) REFERENCES vendor_listings(id) ON DELETE RESTRICT`
- `compliance_submissions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `compliance_submissions_pkey` — `CREATE UNIQUE INDEX compliance_submissions_pkey ON public.compliance_submissions USING btree (id)`
- `compliance_submissions_status_index` — `CREATE INDEX compliance_submissions_status_index ON public.compliance_submissions USING btree (status)`

## `conversation_assignments`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `conversation_id` | `uuid` | No | — | Conversation id. |
| `assigned_user_id` | `bigint` | No | — | Assigned user id. |
| `assigned_by_user_id` | `bigint` | Yes | — | Assigned by user id. |
| `ended_at` | `timestamp with time zone` | Yes | — | Ended at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `conversation_assignments_assigned_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (assigned_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `conversation_assignments_assigned_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (assigned_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `conversation_assignments_conversation_id_foreign` — FOREIGN KEY: `FOREIGN KEY (conversation_id) REFERENCES conversations(id) ON DELETE RESTRICT`
- `conversation_assignments_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `conversation_assignments_pkey` — `CREATE UNIQUE INDEX conversation_assignments_pkey ON public.conversation_assignments USING btree (id)`

## `conversation_participants`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `conversation_id` | `uuid` | No | — | Conversation id. |
| `user_id` | `bigint` | No | — | User id. |
| `participant_role` | `character varying` | No | — | Participant role. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `conversation_participants_conversation_id_foreign` — FOREIGN KEY: `FOREIGN KEY (conversation_id) REFERENCES conversations(id) ON DELETE RESTRICT`
- `conversation_participants_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `conversation_participants_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `conversation_participants_conversation_id_user_id_unique` — UNIQUE: `UNIQUE (conversation_id, user_id)`

**Indexes**

- `conversation_participants_conversation_id_user_id_unique` — `CREATE UNIQUE INDEX conversation_participants_conversation_id_user_id_unique ON public.conversation_participants USING btree (conversation_id, user_id)`
- `conversation_participants_pkey` — `CREATE UNIQUE INDEX conversation_participants_pkey ON public.conversation_participants USING btree (id)`

## `conversations`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `buyer_profile_id` | `uuid` | No | — | Buyer profile id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `context_type` | `character varying` | No | — | Context type. |
| `context_id` | `uuid` | Yes | — | Context id. |
| `state` | `character varying` | No | `'ACTIVE'::character varying` | State. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `conversations_buyer_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (buyer_profile_id) REFERENCES buyer_profiles(id) ON DELETE RESTRICT`
- `conversations_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `conversations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `conversations_pkey` — `CREATE UNIQUE INDEX conversations_pkey ON public.conversations USING btree (id)`

## `data_exports`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `requested_by_user_id` | `bigint` | No | — | Requested by user id. |
| `export_type` | `character varying` | No | — | Export type. |
| `filters` | `jsonb` | No | — | Filters. |
| `reason` | `text` | No | — | Reason. |
| `file_id` | `uuid` | Yes | — | File id. |
| `state` | `character varying` | No | — | State. |
| `expires_at` | `timestamp with time zone` | Yes | — | Expires at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `data_exports_requested_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (requested_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `data_exports_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `data_exports_pkey` — `CREATE UNIQUE INDEX data_exports_pkey ON public.data_exports USING btree (id)`

## `delivery_assignments`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `delivery_quote_id` | `uuid` | No | — | Delivery quote id. |
| `vendor_vehicle_id` | `uuid` | No | — | Vendor vehicle id. |
| `assigned_by_user_id` | `bigint` | No | — | Assigned by user id. |
| `trip_number` | `smallint` | No | — | Trip number. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `delivery_assignments_assigned_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (assigned_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `delivery_assignments_delivery_quote_id_foreign` — FOREIGN KEY: `FOREIGN KEY (delivery_quote_id) REFERENCES delivery_quotes(id) ON DELETE RESTRICT`
- `delivery_assignments_vendor_vehicle_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_vehicle_id) REFERENCES vendor_vehicles(id) ON DELETE RESTRICT`
- `delivery_assignments_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `delivery_assignments_delivery_quote_id_trip_number_unique` — UNIQUE: `UNIQUE (delivery_quote_id, trip_number)`

**Indexes**

- `delivery_assignments_delivery_quote_id_trip_number_unique` — `CREATE UNIQUE INDEX delivery_assignments_delivery_quote_id_trip_number_unique ON public.delivery_assignments USING btree (delivery_quote_id, trip_number)`
- `delivery_assignments_pkey` — `CREATE UNIQUE INDEX delivery_assignments_pkey ON public.delivery_assignments USING btree (id)`

## `delivery_quotes`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `vehicle_rate_version_id` | `uuid` | No | — | Vehicle rate version id. |
| `distance_meters` | `integer` | No | — | Distance meters. |
| `trip_count` | `smallint` | No | — | Trip count. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `delivery_quotes_vehicle_rate_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vehicle_rate_version_id) REFERENCES vehicle_rate_versions(id) ON DELETE RESTRICT`
- `delivery_quotes_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `delivery_quotes_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `delivery_quotes_pkey` — `CREATE UNIQUE INDEX delivery_quotes_pkey ON public.delivery_quotes USING btree (id)`

## `delivery_service_areas`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `origin_address_id` | `uuid` | No | — | Origin address id. |
| `radius_km` | `smallint` | No | — | Radius km. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `delivery_service_radius_check` — CHECK: `CHECK (radius_km = ANY (ARRAY[5, 10, 20, 30, 40, 50]))`
- `delivery_service_areas_origin_address_id_foreign` — FOREIGN KEY: `FOREIGN KEY (origin_address_id) REFERENCES addresses(id) ON DELETE RESTRICT`
- `delivery_service_areas_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `delivery_service_areas_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `delivery_service_areas_vendor_organization_id_origin_address_id` — UNIQUE: `UNIQUE (vendor_organization_id, origin_address_id, radius_km)`

**Indexes**

- `delivery_service_areas_pkey` — `CREATE UNIQUE INDEX delivery_service_areas_pkey ON public.delivery_service_areas USING btree (id)`
- `delivery_service_areas_vendor_organization_id_origin_address_id` — `CREATE UNIQUE INDEX delivery_service_areas_vendor_organization_id_origin_address_id ON public.delivery_service_areas USING btree (vendor_organization_id, origin_address_id, radius_km)`

## `directory_suppliers`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `google_place_id` | `character varying` | No | — | Google place id. |
| `name` | `character varying` | No | — | Name. |
| `formatted_address` | `text` | Yes | — | Formatted address. |
| `latitude` | `numeric` | No | — | Latitude. |
| `longitude` | `numeric` | No | — | Longitude. |
| `claimed_vendor_organization_id` | `uuid` | Yes | — | Claimed vendor organization id. |
| `refreshed_at` | `timestamp with time zone` | No | — | Refreshed at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `location` | `geography` | Yes | — | Location. |

**Constraints**

- `directory_suppliers_claimed_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (claimed_vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `directory_suppliers_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `directory_suppliers_google_place_id_unique` — UNIQUE: `UNIQUE (google_place_id)`

**Indexes**

- `directory_suppliers_google_place_id_unique` — `CREATE UNIQUE INDEX directory_suppliers_google_place_id_unique ON public.directory_suppliers USING btree (google_place_id)`
- `directory_suppliers_location_gist` — `CREATE INDEX directory_suppliers_location_gist ON public.directory_suppliers USING gist (location)`
- `directory_suppliers_pkey` — `CREATE UNIQUE INDEX directory_suppliers_pkey ON public.directory_suppliers USING btree (id)`

## `dispute_appeals`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `dispute_case_id` | `uuid` | No | — | Dispute case id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `dispute_appeals_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `dispute_appeals_dispute_case_id_foreign` — FOREIGN KEY: `FOREIGN KEY (dispute_case_id) REFERENCES dispute_cases(id) ON DELETE RESTRICT`
- `dispute_appeals_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `dispute_appeals_pkey` — `CREATE UNIQUE INDEX dispute_appeals_pkey ON public.dispute_appeals USING btree (id)`

## `dispute_cases`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `reference` | `character varying` | No | — | Reference. |
| `order_id` | `uuid` | No | — | Order id. |
| `status` | `character varying` | No | — | Status. |
| `issue_type` | `character varying` | No | — | Issue type. |
| `requested_remedy` | `character varying` | No | — | Requested remedy. |
| `response_due_at` | `timestamp with time zone` | No | — | Response due at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `dispute_cases_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `dispute_cases_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `dispute_cases_reference_unique` — UNIQUE: `UNIQUE (reference)`

**Indexes**

- `dispute_cases_pkey` — `CREATE UNIQUE INDEX dispute_cases_pkey ON public.dispute_cases USING btree (id)`
- `dispute_cases_reference_unique` — `CREATE UNIQUE INDEX dispute_cases_reference_unique ON public.dispute_cases USING btree (reference)`
- `dispute_cases_status_index` — `CREATE INDEX dispute_cases_status_index ON public.dispute_cases USING btree (status)`

## `dispute_clarifications`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `dispute_case_id` | `uuid` | No | — | Dispute case id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `dispute_clarifications_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `dispute_clarifications_dispute_case_id_foreign` — FOREIGN KEY: `FOREIGN KEY (dispute_case_id) REFERENCES dispute_cases(id) ON DELETE RESTRICT`
- `dispute_clarifications_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `dispute_clarifications_pkey` — `CREATE UNIQUE INDEX dispute_clarifications_pkey ON public.dispute_clarifications USING btree (id)`

## `dispute_decisions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `dispute_case_id` | `uuid` | No | — | Dispute case id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `awarded_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Awarded centavos. |
| `enforceable` | `boolean` | No | `false` | Enforceable. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `dispute_decisions_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `dispute_decisions_dispute_case_id_foreign` — FOREIGN KEY: `FOREIGN KEY (dispute_case_id) REFERENCES dispute_cases(id) ON DELETE RESTRICT`
- `dispute_decisions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `dispute_decisions_pkey` — `CREATE UNIQUE INDEX dispute_decisions_pkey ON public.dispute_decisions USING btree (id)`

## `dispute_evidence`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `dispute_case_id` | `uuid` | No | — | Dispute case id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `dispute_evidence_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `dispute_evidence_dispute_case_id_foreign` — FOREIGN KEY: `FOREIGN KEY (dispute_case_id) REFERENCES dispute_cases(id) ON DELETE RESTRICT`
- `dispute_evidence_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `dispute_evidence_pkey` — `CREATE UNIQUE INDEX dispute_evidence_pkey ON public.dispute_evidence USING btree (id)`

## `dispute_parties`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `dispute_case_id` | `uuid` | No | — | Dispute case id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `dispute_parties_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `dispute_parties_dispute_case_id_foreign` — FOREIGN KEY: `FOREIGN KEY (dispute_case_id) REFERENCES dispute_cases(id) ON DELETE RESTRICT`
- `dispute_parties_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `dispute_parties_pkey` — `CREATE UNIQUE INDEX dispute_parties_pkey ON public.dispute_parties USING btree (id)`

## `dispute_remedies`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `dispute_case_id` | `uuid` | No | — | Dispute case id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `dispute_remedies_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `dispute_remedies_dispute_case_id_foreign` — FOREIGN KEY: `FOREIGN KEY (dispute_case_id) REFERENCES dispute_cases(id) ON DELETE RESTRICT`
- `dispute_remedies_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `dispute_remedies_pkey` — `CREATE UNIQUE INDEX dispute_remedies_pkey ON public.dispute_remedies USING btree (id)`

## `dispute_responses`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `dispute_case_id` | `uuid` | No | — | Dispute case id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `dispute_responses_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `dispute_responses_dispute_case_id_foreign` — FOREIGN KEY: `FOREIGN KEY (dispute_case_id) REFERENCES dispute_cases(id) ON DELETE RESTRICT`
- `dispute_responses_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `dispute_responses_pkey` — `CREATE UNIQUE INDEX dispute_responses_pkey ON public.dispute_responses USING btree (id)`

## `email_otps`

Identity and secure-session record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | Yes | — | User id. |
| `normalized_email` | `character varying` | No | — | Normalized email. |
| `purpose` | `character varying` | No | — | Purpose. |
| `code_hash` | `character varying` | No | — | Code hash. |
| `attempts` | `smallint` | No | `'0'::smallint` | Attempts. |
| `max_attempts` | `smallint` | No | `'5'::smallint` | Max attempts. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `consumed_at` | `timestamp with time zone` | Yes | — | Consumed at. |
| `invalidated_at` | `timestamp with time zone` | Yes | — | Invalidated at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `bot_risk_challenge_id` | `uuid` | Yes | — | Bot risk challenge id. |

**Constraints**

- `email_otps_bot_risk_challenge_id_foreign` — FOREIGN KEY: `FOREIGN KEY (bot_risk_challenge_id) REFERENCES bot_risk_challenges(id) ON DELETE RESTRICT`
- `email_otps_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `email_otps_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `email_otps_bot_risk_challenge_id_index` — `CREATE INDEX email_otps_bot_risk_challenge_id_index ON public.email_otps USING btree (bot_risk_challenge_id)`
- `email_otps_normalized_email_purpose_expires_at_index` — `CREATE INDEX email_otps_normalized_email_purpose_expires_at_index ON public.email_otps USING btree (normalized_email, purpose, expires_at)`
- `email_otps_pkey` — `CREATE UNIQUE INDEX email_otps_pkey ON public.email_otps USING btree (id)`

## `external_filing_evidence`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `filing_deadline_id` | `uuid` | No | — | Filing deadline id. |
| `file_id` | `uuid` | No | — | File id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `evidence_type` | `character varying` | No | — | Evidence type. |
| `content_hash` | `character varying` | No | — | Content hash. |
| `filed_on` | `date` | No | — | Filed on. |
| `authentic_evidence` | `boolean` | No | `false` | Authentic evidence. |
| `verification_state` | `character varying` | No | `'UNVERIFIED'::character varying` | Verification state. |
| `verified_by_user_id` | `bigint` | Yes | — | Verified by user id. |
| `verified_at` | `timestamp with time zone` | Yes | — | Verified at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `filing_evidence_check` — CHECK: `CHECK (environment::text = 'LIVE'::text OR authentic_evidence = false)`
- `external_filing_evidence_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `external_filing_evidence_filing_deadline_id_foreign` — FOREIGN KEY: `FOREIGN KEY (filing_deadline_id) REFERENCES filing_deadlines(id) ON DELETE RESTRICT`
- `external_filing_evidence_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `external_filing_evidence_verified_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (verified_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `external_filing_evidence_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `external_filing_evidence_pkey` — `CREATE UNIQUE INDEX external_filing_evidence_pkey ON public.external_filing_evidence USING btree (id)`

## `external_identities`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `provider` | `character varying` | No | — | Provider. |
| `provider_subject` | `character varying` | No | — | Provider subject. |
| `email_at_link` | `character varying` | Yes | — | Email at link. |
| `linked_at` | `timestamp with time zone` | No | — | Linked at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `external_identities_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `external_identities_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `external_identities_provider_provider_subject_unique` — UNIQUE: `UNIQUE (provider, provider_subject)`

**Indexes**

- `external_identities_pkey` — `CREATE UNIQUE INDEX external_identities_pkey ON public.external_identities USING btree (id)`
- `external_identities_provider_provider_subject_unique` — `CREATE UNIQUE INDEX external_identities_provider_provider_subject_unique ON public.external_identities USING btree (provider, provider_subject)`

## `failed_jobs`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `bigint` | No | `nextval('failed_jobs_id_seq'::regclass)` | Primary key. Id. |
| `uuid` | `character varying` | No | — | Uuid. |
| `connection` | `text` | No | — | Connection. |
| `queue` | `text` | No | — | Queue. |
| `payload` | `text` | No | — | Payload. |
| `exception` | `text` | No | — | Exception. |
| `failed_at` | `timestamp with time zone` | No | `CURRENT_TIMESTAMP` | Failed at. |

**Constraints**

- `failed_jobs_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `failed_jobs_uuid_unique` — UNIQUE: `UNIQUE (uuid)`

**Indexes**

- `failed_jobs_pkey` — `CREATE UNIQUE INDEX failed_jobs_pkey ON public.failed_jobs USING btree (id)`
- `failed_jobs_uuid_unique` — `CREATE UNIQUE INDEX failed_jobs_uuid_unique ON public.failed_jobs USING btree (uuid)`

## `favorite_vendors`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `buyer_profile_id` | `uuid` | No | — | Buyer profile id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `favorite_vendors_buyer_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (buyer_profile_id) REFERENCES buyer_profiles(id) ON DELETE RESTRICT`
- `favorite_vendors_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `favorite_vendors_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `favorite_vendors_buyer_profile_id_vendor_organization_id_unique` — UNIQUE: `UNIQUE (buyer_profile_id, vendor_organization_id)`

**Indexes**

- `favorite_vendors_buyer_profile_id_vendor_organization_id_unique` — `CREATE UNIQUE INDEX favorite_vendors_buyer_profile_id_vendor_organization_id_unique ON public.favorite_vendors USING btree (buyer_profile_id, vendor_organization_id)`
- `favorite_vendors_pkey` — `CREATE UNIQUE INDEX favorite_vendors_pkey ON public.favorite_vendors USING btree (id)`

## `fee_adjustments`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `fee_assessment_id` | `uuid` | No | — | Fee assessment id. |
| `kind` | `character varying` | No | — | Kind. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `target_fee_before_centavos` | `bigint` | No | — | Integer Philippine centavos. Target fee before centavos. |
| `target_fee_after_centavos` | `bigint` | No | — | Integer Philippine centavos. Target fee after centavos. |
| `reason` | `text` | No | — | Reason. |
| `prepared_by_user_id` | `bigint` | No | — | Prepared by user id. |
| `approved_by_user_id` | `bigint` | No | — | Approved by user id. |
| `approved_at` | `timestamp with time zone` | No | — | Approved at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `fee_adjustment_actor_check` — CHECK: `CHECK (prepared_by_user_id <> approved_by_user_id)`
- `fee_adjustment_amount_check` — CHECK: `CHECK (amount_centavos > 0 AND target_fee_before_centavos >= target_fee_after_centavos AND target_fee_after_centavos >= 0)`
- `fee_adjustments_approved_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (approved_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `fee_adjustments_fee_assessment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fee_assessment_id) REFERENCES fee_assessments(id) ON DELETE RESTRICT`
- `fee_adjustments_prepared_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (prepared_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `fee_adjustments_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `fee_adjustments_pkey` — `CREATE UNIQUE INDEX fee_adjustments_pkey ON public.fee_adjustments USING btree (id)`

## `fee_assessments`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `financial_snapshot_id` | `uuid` | No | — | Financial snapshot id. |
| `fee_policy_version_id` | `uuid` | No | — | Fee policy version id. |
| `commission_basis_centavos` | `bigint` | No | — | Integer Philippine centavos. Commission basis centavos. |
| `commission_basis_points` | `integer` | No | — | Commission basis points. |
| `earned_centavos` | `bigint` | No | — | Integer Philippine centavos. Earned centavos. |
| `earned_target_centavos` | `bigint` | No | — | Integer Philippine centavos. Earned target centavos. |
| `credited_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Credited centavos. |
| `credited_state` | `character varying` | No | `'NOT_CREDITED'::character varying` | Credited state. |
| `state` | `character varying` | No | `'ESTIMATED'::character varying` | State. |
| `earned_at` | `timestamp with time zone` | Yes | — | Earned at. |
| `dispute_hold` | `boolean` | No | `false` | Dispute hold. |
| `dispute_hold_at` | `timestamp with time zone` | Yes | — | Dispute hold at. |
| `calculation_hash` | `character varying` | No | — | Calculation hash. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `fee_assessment_amounts_check` — CHECK: `CHECK (commission_basis_centavos >= 0 AND commission_basis_points = 200 AND earned_centavos >= 0 AND earned_target_centavos >= 0 AND credited_centavos >= 0 AND credited_centavos <= earned_centavos)`
- `fee_assessment_state_check` — CHECK: `CHECK ((state::text = ANY (ARRAY['ESTIMATED'::character varying, 'EARNED'::character varying, 'CANCELLED'::character varying]::text[])) AND (credited_state::text = ANY (ARRAY['NOT_CREDITED'::character varying, 'PARTIALLY_CREDITED'::character varying, 'CREDITED'::character varying]::text[])))`
- `fee_assessments_fee_policy_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fee_policy_version_id) REFERENCES fee_policy_versions(id) ON DELETE RESTRICT`
- `fee_assessments_financial_snapshot_id_foreign` — FOREIGN KEY: `FOREIGN KEY (financial_snapshot_id) REFERENCES financial_snapshots(id) ON DELETE RESTRICT`
- `fee_assessments_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `fee_assessments_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `fee_assessments_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `fee_assessments_order_id_fee_policy_version_id_unique` — UNIQUE: `UNIQUE (order_id, fee_policy_version_id)`

**Indexes**

- `fee_assessments_order_id_fee_policy_version_id_unique` — `CREATE UNIQUE INDEX fee_assessments_order_id_fee_policy_version_id_unique ON public.fee_assessments USING btree (order_id, fee_policy_version_id)`
- `fee_assessments_pkey` — `CREATE UNIQUE INDEX fee_assessments_pkey ON public.fee_assessments USING btree (id)`

## `fee_payment_allocations`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `fee_statement_id` | `uuid` | No | — | Fee statement id. |
| `payment_id` | `uuid` | No | — | Payment id. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `statement_outstanding_before_centavos` | `bigint` | No | — | Integer Philippine centavos. Statement outstanding before centavos. |
| `payment_unallocated_before_centavos` | `bigint` | No | — | Integer Philippine centavos. Payment unallocated before centavos. |
| `allocated_at` | `timestamp with time zone` | No | — | Allocated at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `fee_payment_allocation_check` — CHECK: `CHECK (amount_centavos > 0 AND statement_outstanding_before_centavos >= amount_centavos AND payment_unallocated_before_centavos >= amount_centavos)`
- `fee_payment_allocations_fee_statement_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fee_statement_id) REFERENCES fee_statements(id) ON DELETE RESTRICT`
- `fee_payment_allocations_payment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (payment_id) REFERENCES payments(id) ON DELETE RESTRICT`
- `fee_payment_allocations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `fee_payment_allocations_fee_statement_id_payment_id_unique` — UNIQUE: `UNIQUE (fee_statement_id, payment_id)`

**Indexes**

- `fee_payment_allocations_fee_statement_id_payment_id_unique` — `CREATE UNIQUE INDEX fee_payment_allocations_fee_statement_id_payment_id_unique ON public.fee_payment_allocations USING btree (fee_statement_id, payment_id)`
- `fee_payment_allocations_pkey` — `CREATE UNIQUE INDEX fee_payment_allocations_pkey ON public.fee_payment_allocations USING btree (id)`

## `fee_policy_versions`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `code` | `character varying` | No | — | Code. |
| `version` | `integer` | No | — | Version. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `commission_basis_points` | `integer` | No | `200` | Commission basis points. |
| `basis` | `character varying` | No | `'MATERIALS_AFTER_DISCOUNT_EXCLUDING_VAT'::character varying` | Basis. |
| `cadence` | `character varying` | No | `'MONTHLY'::character varying` | Cadence. |
| `effective_from` | `timestamp with time zone` | No | — | Effective from. |
| `effective_until` | `timestamp with time zone` | Yes | — | Effective until. |
| `approved_by_user_id` | `bigint` | Yes | — | Approved by user id. |
| `approved_at` | `timestamp with time zone` | Yes | — | Approved at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `fee_policy_commission_check` — CHECK: `CHECK (commission_basis_points = 200 AND basis::text = 'MATERIALS_AFTER_DISCOUNT_EXCLUDING_VAT'::text AND cadence::text = 'MONTHLY'::text)`
- `fee_policy_environment_check` — CHECK: `CHECK (environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying, 'LIVE'::character varying]::text[]))`
- `fee_policy_versions_approved_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (approved_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `fee_policy_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `fee_policy_versions_environment_code_version_unique` — UNIQUE: `UNIQUE (environment, code, version)`

**Indexes**

- `fee_policy_versions_environment_code_version_unique` — `CREATE UNIQUE INDEX fee_policy_versions_environment_code_version_unique ON public.fee_policy_versions USING btree (environment, code, version)`
- `fee_policy_versions_pkey` — `CREATE UNIQUE INDEX fee_policy_versions_pkey ON public.fee_policy_versions USING btree (id)`

## `fee_statement_lines`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `fee_statement_id` | `uuid` | No | — | Fee statement id. |
| `fee_assessment_id` | `uuid` | Yes | — | Fee assessment id. |
| `fee_adjustment_id` | `uuid` | Yes | — | Fee adjustment id. |
| `line_type` | `character varying` | No | — | Line type. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `description` | `character varying` | No | — | Description. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `fee_statement_line_source_check` — CHECK: `CHECK (((fee_assessment_id IS NOT NULL)::integer + (fee_adjustment_id IS NOT NULL)::integer) = 1)`
- `fee_statement_lines_fee_adjustment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fee_adjustment_id) REFERENCES fee_adjustments(id) ON DELETE RESTRICT`
- `fee_statement_lines_fee_assessment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fee_assessment_id) REFERENCES fee_assessments(id) ON DELETE RESTRICT`
- `fee_statement_lines_fee_statement_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fee_statement_id) REFERENCES fee_statements(id) ON DELETE RESTRICT`
- `fee_statement_lines_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `fee_statement_lines_fee_adjustment_id_unique` — UNIQUE: `UNIQUE (fee_adjustment_id)`
- `fee_statement_lines_fee_assessment_id_unique` — UNIQUE: `UNIQUE (fee_assessment_id)`

**Indexes**

- `fee_statement_lines_fee_adjustment_id_unique` — `CREATE UNIQUE INDEX fee_statement_lines_fee_adjustment_id_unique ON public.fee_statement_lines USING btree (fee_adjustment_id)`
- `fee_statement_lines_fee_assessment_id_unique` — `CREATE UNIQUE INDEX fee_statement_lines_fee_assessment_id_unique ON public.fee_statement_lines USING btree (fee_assessment_id)`
- `fee_statement_lines_pkey` — `CREATE UNIQUE INDEX fee_statement_lines_pkey ON public.fee_statement_lines USING btree (id)`

## `fee_statements`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `period_start` | `date` | No | — | Period start. |
| `period_end` | `date` | No | — | Period end. |
| `issued_on` | `date` | No | — | Issued on. |
| `due_on` | `date` | No | — | Due on. |
| `charges_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Charges centavos. |
| `credits_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Credits centavos. |
| `paid_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Paid centavos. |
| `balance_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Balance centavos. |
| `outstanding_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Outstanding centavos. |
| `state` | `character varying` | No | `'DRAFT'::character varying` | State. |
| `version` | `integer` | No | `1` | Version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `fee_statement_amounts_check` — CHECK: `CHECK (charges_centavos >= 0 AND credits_centavos >= 0 AND paid_centavos >= 0 AND balance_centavos >= 0 AND outstanding_centavos = balance_centavos AND balance_centavos = (charges_centavos - credits_centavos - paid_centavos))`
- `fee_statement_dates_check` — CHECK: `CHECK (period_start <= period_end AND issued_on <= due_on)`
- `fee_statement_state_check` — CHECK: `CHECK (state::text = ANY (ARRAY['DRAFT'::character varying, 'ISSUED'::character varying, 'PARTIALLY_PAID'::character varying, 'PAID'::character varying, 'VOIDED'::character varying]::text[]))`
- `fee_statements_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `fee_statements_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `fee_statements_vendor_organization_id_period_start_period_end_v` — UNIQUE: `UNIQUE (vendor_organization_id, period_start, period_end, version)`

**Indexes**

- `fee_statements_pkey` — `CREATE UNIQUE INDEX fee_statements_pkey ON public.fee_statements USING btree (id)`
- `fee_statements_vendor_organization_id_period_start_period_end_v` — `CREATE UNIQUE INDEX fee_statements_vendor_organization_id_period_start_period_end_v ON public.fee_statements USING btree (vendor_organization_id, period_start, period_end, version)`

## `files`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `owner_type` | `character varying` | No | — | Owner type. |
| `owner_id` | `uuid` | No | — | Owner id. |
| `purpose` | `character varying` | No | — | Purpose. |
| `visibility` | `character varying` | No | `'PRIVATE'::character varying` | Visibility. |
| `content_type` | `character varying` | No | — | Content type. |
| `byte_size` | `bigint` | No | — | Byte size. |
| `checksum_sha256` | `character varying` | No | — | Checksum sha256. |
| `scan_state` | `character varying` | No | `'PENDING'::character varying` | Scan state. |
| `object_key` | `character varying` | No | — | Object key. |
| `retention_class` | `character varying` | No | — | Retention class. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `vendor_organization_id` | `uuid` | Yes | — | Vendor organization id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `evidence_origin` | `character varying` | No | `'SAMPLE_DEMO'::character varying` | Evidence origin. |
| `storage_provider` | `character varying` | No | `'S3'::character varying` | Storage provider. |
| `provider_asset_id` | `character varying` | Yes | — | Provider asset id. |
| `provider_format` | `character varying` | Yes | — | Provider format. |

**Constraints**

- `files_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `files_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `files_object_key_unique` — UNIQUE: `UNIQUE (object_key)`

**Indexes**

- `files_object_key_unique` — `CREATE UNIQUE INDEX files_object_key_unique ON public.files USING btree (object_key)`
- `files_owner_type_owner_id_index` — `CREATE INDEX files_owner_type_owner_id_index ON public.files USING btree (owner_type, owner_id)`
- `files_pkey` — `CREATE UNIQUE INDEX files_pkey ON public.files USING btree (id)`

## `filing_calendar_versions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `version` | `integer` | No | — | Version. |
| `source_type` | `character varying` | No | — | Source type. |
| `source_reference` | `character varying` | Yes | — | Source reference. |
| `effective_from` | `timestamp with time zone` | No | — | Effective from. |
| `effective_until` | `timestamp with time zone` | Yes | — | Effective until. |
| `approved_by_user_id` | `bigint` | Yes | — | Approved by user id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `filing_calendar_versions_approved_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (approved_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `filing_calendar_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `filing_calendar_versions_environment_version_unique` — UNIQUE: `UNIQUE (environment, version)`

**Indexes**

- `filing_calendar_versions_environment_version_unique` — `CREATE UNIQUE INDEX filing_calendar_versions_environment_version_unique ON public.filing_calendar_versions USING btree (environment, version)`
- `filing_calendar_versions_pkey` — `CREATE UNIQUE INDEX filing_calendar_versions_pkey ON public.filing_calendar_versions USING btree (id)`

## `filing_deadlines`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `filing_calendar_version_id` | `uuid` | No | — | Filing calendar version id. |
| `obligation_code` | `character varying` | No | — | Obligation code. |
| `taxpayer_class` | `character varying` | No | — | Taxpayer class. |
| `periodicity` | `character varying` | No | — | Periodicity. |
| `due_rule` | `jsonb` | No | — | Due rule. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `filing_deadlines_filing_calendar_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (filing_calendar_version_id) REFERENCES filing_calendar_versions(id) ON DELETE RESTRICT`
- `filing_deadlines_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `filing_deadlines_filing_calendar_version_id_obligation_code_tax` — UNIQUE: `UNIQUE (filing_calendar_version_id, obligation_code, taxpayer_class)`

**Indexes**

- `filing_deadlines_filing_calendar_version_id_obligation_code_tax` — `CREATE UNIQUE INDEX filing_deadlines_filing_calendar_version_id_obligation_code_tax ON public.filing_deadlines USING btree (filing_calendar_version_id, obligation_code, taxpayer_class)`
- `filing_deadlines_pkey` — `CREATE UNIQUE INDEX filing_deadlines_pkey ON public.filing_deadlines USING btree (id)`

## `financial_allocations`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `financial_snapshot_id` | `uuid` | No | — | Financial snapshot id. |
| `allocation_type` | `character varying` | No | — | Allocation type. |
| `source_type` | `character varying` | No | — | Source type. |
| `source_id` | `uuid` | No | — | Source id. |
| `target_type` | `character varying` | No | — | Target type. |
| `target_id` | `uuid` | No | — | Target id. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `basis` | `jsonb` | No | — | Basis. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `financial_allocation_amount_check` — CHECK: `CHECK (amount_centavos >= 0)`
- `financial_allocations_financial_snapshot_id_foreign` — FOREIGN KEY: `FOREIGN KEY (financial_snapshot_id) REFERENCES financial_snapshots(id) ON DELETE RESTRICT`
- `financial_allocations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `financial_allocations_allocation_type_source_type_source_id_tar` — UNIQUE: `UNIQUE (allocation_type, source_type, source_id, target_type, target_id)`

**Indexes**

- `financial_allocations_allocation_type_source_type_source_id_tar` — `CREATE UNIQUE INDEX financial_allocations_allocation_type_source_type_source_id_tar ON public.financial_allocations USING btree (allocation_type, source_type, source_id, target_type, target_id)`
- `financial_allocations_pkey` — `CREATE UNIQUE INDEX financial_allocations_pkey ON public.financial_allocations USING btree (id)`

## `financial_ledger_entries`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `financial_posting_batch_id` | `uuid` | No | — | Financial posting batch id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `ledger` | `character varying` | No | — | Ledger. |
| `account_code` | `character varying` | No | — | Account code. |
| `source_type` | `character varying` | No | — | Source type. |
| `source_id` | `uuid` | No | — | Source id. |
| `direction` | `character varying` | No | — | Direction. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `currency` | `character varying` | No | `'PHP'::character varying` | Currency. |
| `idempotency_key` | `character varying` | No | — | Idempotency key. |
| `dimensions` | `jsonb` | Yes | — | Dimensions. |
| `reverses_ledger_entry_id` | `uuid` | Yes | — | Reverses ledger entry id. |
| `original_available_centavos` | `bigint` | Yes | — | Integer Philippine centavos. Original available centavos. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `actor_role` | `character varying` | Yes | — | Actor role. |
| `correlation_id` | `character varying` | No | — | Correlation id. |
| `occurred_at` | `timestamp with time zone` | No | — | Occurred at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `ledger_direction_check` — CHECK: `CHECK ((direction::text = ANY (ARRAY['DEBIT'::character varying, 'CREDIT'::character varying]::text[])) AND amount_centavos > 0 AND (environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying, 'LIVE'::character varying]::text[])) AND (ledger::text = ANY (ARRAY['DEMO_FLOW'::character varying, 'PLATFORM_FEE'::character varying]::text[])) AND currency::text = 'PHP'::text)`
- `ledger_reversal_amount_check` — CHECK: `CHECK (reverses_ledger_entry_id IS NULL OR original_available_centavos IS NOT NULL AND amount_centavos <= original_available_centavos)`
- `financial_ledger_entries_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `financial_ledger_entries_financial_posting_batch_id_foreign` — FOREIGN KEY: `FOREIGN KEY (financial_posting_batch_id) REFERENCES financial_posting_batches(id) ON DELETE RESTRICT`
- `financial_ledger_entries_reverses_ledger_entry_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reverses_ledger_entry_id) REFERENCES financial_ledger_entries(id) ON DELETE RESTRICT`
- `financial_ledger_entries_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `financial_ledger_entries_idempotency_key_unique` — UNIQUE: `UNIQUE (idempotency_key)`

**Indexes**

- `financial_ledger_entries_correlation_id_index` — `CREATE INDEX financial_ledger_entries_correlation_id_index ON public.financial_ledger_entries USING btree (correlation_id)`
- `financial_ledger_entries_idempotency_key_unique` — `CREATE UNIQUE INDEX financial_ledger_entries_idempotency_key_unique ON public.financial_ledger_entries USING btree (idempotency_key)`
- `financial_ledger_entries_pkey` — `CREATE UNIQUE INDEX financial_ledger_entries_pkey ON public.financial_ledger_entries USING btree (id)`

## `financial_posting_batches`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `ledger` | `character varying` | No | — | Ledger. |
| `currency` | `character` | No | `'PHP'::bpchar` | Currency. |
| `batch_type` | `character varying` | No | — | Batch type. |
| `source_event_key` | `character varying` | No | — | Source event key. |
| `posting_date` | `date` | No | — | Posting date. |
| `state` | `character varying` | No | `'PREPARED'::character varying` | State. |
| `debit_total_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Debit total centavos. |
| `credit_total_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Credit total centavos. |
| `prepared_by_user_id` | `bigint` | Yes | — | Prepared by user id. |
| `reviewed_by_user_id` | `bigint` | Yes | — | Reviewed by user id. |
| `content_hash` | `character varying` | Yes | — | Content hash. |
| `correlation_id` | `character varying` | No | — | Correlation id. |
| `posted_at` | `timestamp with time zone` | Yes | — | Posted at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `posting_batch_actor_check` — CHECK: `CHECK (reviewed_by_user_id IS NULL OR reviewed_by_user_id <> prepared_by_user_id)`
- `posting_batch_balance_check` — CHECK: `CHECK (debit_total_centavos >= 0 AND credit_total_centavos >= 0 AND (state::text <> 'POSTED'::text OR debit_total_centavos = credit_total_centavos AND posted_at IS NOT NULL))`
- `posting_batch_scope_check` — CHECK: `CHECK ((environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying, 'LIVE'::character varying]::text[])) AND currency = 'PHP'::bpchar AND (ledger::text = ANY (ARRAY['DEMO_FLOW'::character varying, 'PLATFORM_FEE'::character varying]::text[])))`
- `posting_batch_state_check` — CHECK: `CHECK (state::text = ANY (ARRAY['PREPARED'::character varying, 'REVIEWED'::character varying, 'POSTED'::character varying, 'REJECTED'::character varying, 'REVERSED'::character varying]::text[]))`
- `financial_posting_batches_prepared_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (prepared_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `financial_posting_batches_reviewed_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reviewed_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `financial_posting_batches_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `financial_posting_batches_content_hash_unique` — UNIQUE: `UNIQUE (content_hash)`
- `financial_posting_batches_source_event_key_unique` — UNIQUE: `UNIQUE (source_event_key)`

**Indexes**

- `financial_posting_batches_content_hash_unique` — `CREATE UNIQUE INDEX financial_posting_batches_content_hash_unique ON public.financial_posting_batches USING btree (content_hash)`
- `financial_posting_batches_correlation_id_index` — `CREATE INDEX financial_posting_batches_correlation_id_index ON public.financial_posting_batches USING btree (correlation_id)`
- `financial_posting_batches_pkey` — `CREATE UNIQUE INDEX financial_posting_batches_pkey ON public.financial_posting_batches USING btree (id)`
- `financial_posting_batches_source_event_key_unique` — `CREATE UNIQUE INDEX financial_posting_batches_source_event_key_unique ON public.financial_posting_batches USING btree (source_event_key)`

## `financial_snapshot_lines`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `financial_snapshot_id` | `uuid` | No | — | Financial snapshot id. |
| `order_line_id` | `uuid` | No | — | Order line id. |
| `source_quantity` | `numeric` | No | — | Source quantity. |
| `source_unit_price_centavos` | `bigint` | No | — | Integer Philippine centavos. Source unit price centavos. |
| `gross_centavos` | `bigint` | No | — | Integer Philippine centavos. Gross centavos. |
| `discount_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Discount centavos. |
| `vat_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Vat centavos. |
| `ordinary_payable_centavos` | `bigint` | No | — | Integer Philippine centavos. Ordinary payable centavos. |
| `principal_allocated_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Principal allocated centavos. |
| `refund_allocated_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Refund allocated centavos. |
| `tax_category` | `character varying` | No | — | Tax category. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `financial_snapshot_line_amounts_check` — CHECK: `CHECK (source_quantity > 0::numeric AND source_unit_price_centavos >= 0 AND gross_centavos >= 0 AND discount_centavos >= 0 AND discount_centavos <= gross_centavos AND vat_centavos >= 0 AND vat_centavos <= ordinary_payable_centavos AND ordinary_payable_centavos = (gross_centavos - discount_centavos) AND principal_allocated_centavos >= 0 AND refund_allocated_centavos >= 0 AND (principal_allocated_centavos + refund_allocated_centavos) <= ordinary_payable_centavos)`
- `financial_snapshot_lines_financial_snapshot_id_foreign` — FOREIGN KEY: `FOREIGN KEY (financial_snapshot_id) REFERENCES financial_snapshots(id) ON DELETE RESTRICT`
- `financial_snapshot_lines_order_line_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_line_id) REFERENCES order_lines(id) ON DELETE RESTRICT`
- `financial_snapshot_lines_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `financial_snapshot_lines_order_line_id_unique` — UNIQUE: `UNIQUE (order_line_id)`

**Indexes**

- `financial_snapshot_lines_order_line_id_unique` — `CREATE UNIQUE INDEX financial_snapshot_lines_order_line_id_unique ON public.financial_snapshot_lines USING btree (order_line_id)`
- `financial_snapshot_lines_pkey` — `CREATE UNIQUE INDEX financial_snapshot_lines_pkey ON public.financial_snapshot_lines USING btree (id)`

## `financial_snapshots`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `quotation_version_id` | `uuid` | Yes | — | Quotation version id. |
| `tax_rule_version_id` | `uuid` | Yes | — | Tax rule version id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `materials_gross_centavos` | `bigint` | No | — | Integer Philippine centavos. Materials gross centavos. |
| `vendor_discount_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Vendor discount centavos. |
| `materials_payable_centavos` | `bigint` | No | — | Integer Philippine centavos. Materials payable centavos. |
| `materials_vat_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Materials vat centavos. |
| `materials_exclusive_centavos` | `bigint` | No | — | Integer Philippine centavos. Materials exclusive centavos. |
| `delivery_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Delivery centavos. |
| `delivery_vat_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Delivery vat centavos. |
| `processing_fee_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Processing fee centavos. |
| `nrpc_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Nrpc centavos. |
| `buyer_total_centavos` | `bigint` | No | — | Integer Philippine centavos. Buyer total centavos. |
| `currency` | `character varying` | No | `'PHP'::character varying` | Currency. |
| `fee_policy_version_id` | `uuid` | Yes | — | Fee policy version id. |
| `calculation_version` | `character varying` | No | — | Calculation version. |
| `rounding_allocation` | `jsonb` | No | — | Rounding allocation. |
| `calculation_hash` | `character varying` | No | — | Calculation hash. |
| `published_at` | `timestamp with time zone` | No | — | Published at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `financial_snapshot_amounts_check` — CHECK: `CHECK (materials_gross_centavos >= 0 AND vendor_discount_centavos >= 0 AND materials_payable_centavos = (materials_gross_centavos - vendor_discount_centavos) AND materials_vat_centavos >= 0 AND materials_exclusive_centavos = (materials_payable_centavos - materials_vat_centavos) AND delivery_centavos >= 0 AND delivery_vat_centavos >= 0 AND delivery_vat_centavos <= delivery_centavos AND processing_fee_centavos >= 0 AND nrpc_centavos >= 0 AND nrpc_centavos <= materials_payable_centavos AND buyer_total_centavos = (materials_payable_centavos + delivery_centavos + processing_fee_centavos))`
- `financial_snapshot_environment_currency_check` — CHECK: `CHECK ((environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying, 'LIVE'::character varying]::text[])) AND currency::text = 'PHP'::text)`
- `financial_snapshots_fee_policy_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fee_policy_version_id) REFERENCES fee_policy_versions(id) ON DELETE RESTRICT`
- `financial_snapshots_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `financial_snapshots_quotation_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (quotation_version_id) REFERENCES quotation_versions(id) ON DELETE RESTRICT`
- `financial_snapshots_tax_rule_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (tax_rule_version_id) REFERENCES tax_rule_versions(id) ON DELETE RESTRICT`
- `financial_snapshots_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `financial_snapshots_calculation_hash_unique` — UNIQUE: `UNIQUE (calculation_hash)`
- `financial_snapshots_order_id_unique` — UNIQUE: `UNIQUE (order_id)`

**Indexes**

- `financial_snapshots_calculation_hash_unique` — `CREATE UNIQUE INDEX financial_snapshots_calculation_hash_unique ON public.financial_snapshots USING btree (calculation_hash)`
- `financial_snapshots_order_id_unique` — `CREATE UNIQUE INDEX financial_snapshots_order_id_unique ON public.financial_snapshots USING btree (order_id)`
- `financial_snapshots_pkey` — `CREATE UNIQUE INDEX financial_snapshots_pkey ON public.financial_snapshots USING btree (id)`

## `fulfillment_milestones`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `fulfillment_id` | `uuid` | No | — | Fulfillment id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `event_type` | `character varying` | Yes | — | Event type. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `occurred_at` | `timestamp with time zone` | Yes | — | Occurred at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `fulfillment_milestones_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `fulfillment_milestones_fulfillment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fulfillment_id) REFERENCES fulfillments(id) ON DELETE RESTRICT`
- `fulfillment_milestones_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `fulfillment_milestones_pkey` — `CREATE UNIQUE INDEX fulfillment_milestones_pkey ON public.fulfillment_milestones USING btree (id)`

## `fulfillment_proofs`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `fulfillment_id` | `uuid` | No | — | Fulfillment id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `event_type` | `character varying` | Yes | — | Event type. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `occurred_at` | `timestamp with time zone` | Yes | — | Occurred at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `fulfillment_proofs_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `fulfillment_proofs_fulfillment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fulfillment_id) REFERENCES fulfillments(id) ON DELETE RESTRICT`
- `fulfillment_proofs_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `fulfillment_proofs_pkey` — `CREATE UNIQUE INDEX fulfillment_proofs_pkey ON public.fulfillment_proofs USING btree (id)`

## `fulfillments`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `state` | `character varying` | No | — | State. |
| `expected_at` | `timestamp with time zone` | Yes | — | Expected at. |
| `completed_at` | `timestamp with time zone` | Yes | — | Completed at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `fulfillments_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `fulfillments_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `fulfillments_order_id_unique` — UNIQUE: `UNIQUE (order_id)`

**Indexes**

- `fulfillments_order_id_unique` — `CREATE UNIQUE INDEX fulfillments_order_id_unique ON public.fulfillments USING btree (order_id)`
- `fulfillments_pkey` — `CREATE UNIQUE INDEX fulfillments_pkey ON public.fulfillments USING btree (id)`

## `geography_columns`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `f_table_catalog` | `name` | Yes | — | F table catalog. |
| `f_table_schema` | `name` | Yes | — | F table schema. |
| `f_table_name` | `name` | Yes | — | F table name. |
| `f_geography_column` | `name` | Yes | — | F geography column. |
| `coord_dimension` | `integer` | Yes | — | Coord dimension. |
| `srid` | `integer` | Yes | — | Srid. |
| `type` | `text` | Yes | — | Type. |

**Constraints**


**Indexes**


## `geometry_columns`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `f_table_catalog` | `character varying` | Yes | — | F table catalog. |
| `f_table_schema` | `name` | Yes | — | F table schema. |
| `f_table_name` | `name` | Yes | — | F table name. |
| `f_geometry_column` | `name` | Yes | — | F geometry column. |
| `coord_dimension` | `integer` | Yes | — | Coord dimension. |
| `srid` | `integer` | Yes | — | Srid. |
| `type` | `character varying` | Yes | — | Type. |

**Constraints**


**Indexes**


## `idempotency_records`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `endpoint` | `character varying` | No | — | Endpoint. |
| `idempotency_key` | `character varying` | No | — | Idempotency key. |
| `request_hash` | `character varying` | No | — | Request hash. |
| `response_status` | `smallint` | Yes | — | Response status. |
| `response_body` | `jsonb` | Yes | — | Response body. |
| `expires_at` | `timestamp with time zone` | Yes | — | Expires at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `idempotency_records_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `idempotency_records_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `idempotency_records_actor_user_id_endpoint_idempotency_key_uniq` — UNIQUE: `UNIQUE (actor_user_id, endpoint, idempotency_key)`

**Indexes**

- `idempotency_records_actor_user_id_endpoint_idempotency_key_uniq` — `CREATE UNIQUE INDEX idempotency_records_actor_user_id_endpoint_idempotency_key_uniq ON public.idempotency_records USING btree (actor_user_id, endpoint, idempotency_key)`
- `idempotency_records_pkey` — `CREATE UNIQUE INDEX idempotency_records_pkey ON public.idempotency_records USING btree (id)`

## `inventory_holds`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `inventory_item_id` | `uuid` | No | — | Inventory item id. |
| `hold_type` | `character varying` | No | — | Hold type. |
| `quantity` | `numeric` | No | — | Quantity. |
| `state` | `character varying` | No | `'ACTIVE'::character varying` | State. |
| `source_type` | `character varying` | No | — | Source type. |
| `source_id` | `uuid` | No | — | Source id. |
| `expires_at` | `timestamp with time zone` | Yes | — | Expires at. |
| `released_at` | `timestamp with time zone` | Yes | — | Released at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `inventory_hold_quantity_check` — CHECK: `CHECK (quantity > 0::numeric)`
- `inventory_holds_inventory_item_id_foreign` — FOREIGN KEY: `FOREIGN KEY (inventory_item_id) REFERENCES inventory_items(id) ON DELETE RESTRICT`
- `inventory_holds_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `inventory_holds_source_type_source_id_inventory_item_id_hold_ty` — UNIQUE: `UNIQUE (source_type, source_id, inventory_item_id, hold_type)`

**Indexes**

- `inventory_holds_pkey` — `CREATE UNIQUE INDEX inventory_holds_pkey ON public.inventory_holds USING btree (id)`
- `inventory_holds_source_type_source_id_inventory_item_id_hold_ty` — `CREATE UNIQUE INDEX inventory_holds_source_type_source_id_inventory_item_id_hold_ty ON public.inventory_holds USING btree (source_type, source_id, inventory_item_id, hold_type)`

## `inventory_items`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `listing_variant_id` | `uuid` | No | — | Listing variant id. |
| `quantity_on_hand` | `numeric` | No | `'0'::numeric` | Quantity on hand. |
| `hard_reserved_quantity` | `numeric` | No | `'0'::numeric` | Hard reserved quantity. |
| `soft_held_quantity` | `numeric` | No | `'0'::numeric` | Soft held quantity. |
| `confirmed_at` | `timestamp with time zone` | Yes | — | Confirmed at. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `inventory_balance_check` — CHECK: `CHECK (quantity_on_hand >= 0::numeric AND hard_reserved_quantity >= 0::numeric AND soft_held_quantity >= 0::numeric AND hard_reserved_quantity <= quantity_on_hand)`
- `inventory_items_listing_variant_id_foreign` — FOREIGN KEY: `FOREIGN KEY (listing_variant_id) REFERENCES listing_variants(id) ON DELETE RESTRICT`
- `inventory_items_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `inventory_items_listing_variant_id_unique` — UNIQUE: `UNIQUE (listing_variant_id)`

**Indexes**

- `inventory_items_listing_variant_id_unique` — `CREATE UNIQUE INDEX inventory_items_listing_variant_id_unique ON public.inventory_items USING btree (listing_variant_id)`
- `inventory_items_pkey` — `CREATE UNIQUE INDEX inventory_items_pkey ON public.inventory_items USING btree (id)`

## `inventory_movements`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `inventory_item_id` | `uuid` | No | — | Inventory item id. |
| `movement_type` | `character varying` | No | — | Movement type. |
| `quantity` | `numeric` | No | — | Quantity. |
| `quantity_on_hand_after` | `numeric` | No | — | Quantity on hand after. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `source_type` | `character varying` | No | — | Source type. |
| `source_id` | `uuid` | Yes | — | Source id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `inventory_movements_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `inventory_movements_inventory_item_id_foreign` — FOREIGN KEY: `FOREIGN KEY (inventory_item_id) REFERENCES inventory_items(id) ON DELETE RESTRICT`
- `inventory_movements_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `inventory_movements_pkey` — `CREATE UNIQUE INDEX inventory_movements_pkey ON public.inventory_movements USING btree (id)`

## `invoice_records`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `order_id` | `uuid` | Yes | — | Order id. |
| `fee_statement_id` | `uuid` | Yes | — | Fee statement id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `invoice_type` | `character varying` | No | — | Invoice type. |
| `document_origin` | `character varying` | No | — | Document origin. |
| `issuer` | `character varying` | No | — | Issuer. |
| `issuer_taxpayer_key_hash` | `character varying` | Yes | — | Issuer taxpayer key hash. |
| `payee_name` | `character varying` | No | — | Payee name. |
| `payee_taxpayer_key_hash` | `character varying` | Yes | — | Payee taxpayer key hash. |
| `invoice_number_hash` | `character varying` | No | — | Invoice number hash. |
| `issued_on` | `date` | No | — | Issued on. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `file_id` | `uuid` | No | — | File id. |
| `content_hash` | `character varying` | No | — | Content hash. |
| `verification_state` | `character varying` | No | `'UNVERIFIED'::character varying` | Verification state. |
| `verified_by_user_id` | `bigint` | Yes | — | Verified by user id. |
| `verified_at` | `timestamp with time zone` | Yes | — | Verified at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `invoice_record_target_check` — CHECK: `CHECK (((order_id IS NOT NULL)::integer + (fee_statement_id IS NOT NULL)::integer) = 1 AND amount_centavos >= 0)`
- `invoice_records_fee_statement_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fee_statement_id) REFERENCES fee_statements(id) ON DELETE RESTRICT`
- `invoice_records_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `invoice_records_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `invoice_records_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `invoice_records_verified_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (verified_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `invoice_records_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `invoice_issuer_number_unique` — UNIQUE: `UNIQUE (environment, issuer_taxpayer_key_hash, invoice_number_hash)`

**Indexes**

- `invoice_issuer_number_unique` — `CREATE UNIQUE INDEX invoice_issuer_number_unique ON public.invoice_records USING btree (environment, issuer_taxpayer_key_hash, invoice_number_hash)`
- `invoice_records_pkey` — `CREATE UNIQUE INDEX invoice_records_pkey ON public.invoice_records USING btree (id)`

## `job_batches`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `character varying` | No | — | Primary key. Id. |
| `name` | `character varying` | No | — | Name. |
| `total_jobs` | `integer` | No | — | Total jobs. |
| `pending_jobs` | `integer` | No | — | Pending jobs. |
| `failed_jobs` | `integer` | No | — | Failed jobs. |
| `failed_job_ids` | `text` | No | — | Failed job ids. |
| `options` | `text` | Yes | — | Options. |
| `cancelled_at` | `integer` | Yes | — | Cancelled at. |
| `created_at` | `integer` | No | — | Created at. |
| `finished_at` | `integer` | Yes | — | Finished at. |

**Constraints**

- `job_batches_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `job_batches_pkey` — `CREATE UNIQUE INDEX job_batches_pkey ON public.job_batches USING btree (id)`

## `jobs`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `bigint` | No | `nextval('jobs_id_seq'::regclass)` | Primary key. Id. |
| `queue` | `character varying` | No | — | Queue. |
| `payload` | `text` | No | — | Payload. |
| `attempts` | `smallint` | No | — | Attempts. |
| `reserved_at` | `integer` | Yes | — | Reserved at. |
| `available_at` | `integer` | No | — | Available at. |
| `created_at` | `integer` | No | — | Created at. |

**Constraints**

- `jobs_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `jobs_pkey` — `CREATE UNIQUE INDEX jobs_pkey ON public.jobs USING btree (id)`
- `jobs_queue_index` — `CREATE INDEX jobs_queue_index ON public.jobs USING btree (queue)`

## `listing_comparable_assignments`

MAT-01–MAT-07 comparability, observation, publication, or audience-safe aggregate record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `listing_variant_id` | `uuid` | No | — | Listing variant id. |
| `material_comparable_group_version_id` | `uuid` | No | — | Material comparable group version id. |
| `mapping_state` | `character varying` | No | `'PENDING_REVIEW'::character varying` | Mapping state. |
| `reviewed_by_user_id` | `bigint` | Yes | — | Reviewed by user id. |
| `review_reason` | `text` | Yes | — | Review reason. |
| `effective_from` | `timestamp with time zone` | No | — | Effective from. |
| `effective_until` | `timestamp with time zone` | Yes | — | Effective until. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `comparable_assignment_dates_check` — CHECK: `CHECK (effective_until IS NULL OR effective_until > effective_from)`
- `comparable_assignment_state_check` — CHECK: `CHECK (mapping_state::text = ANY (ARRAY['PENDING_REVIEW'::character varying, 'APPROVED'::character varying, 'REJECTED'::character varying, 'RETIRED'::character varying]::text[]))`
- `listing_comparable_assignments_listing_variant_id_foreign` — FOREIGN KEY: `FOREIGN KEY (listing_variant_id) REFERENCES listing_variants(id) ON DELETE RESTRICT`
- `listing_comparable_assignments_material_comparable_group_versio` — FOREIGN KEY: `FOREIGN KEY (material_comparable_group_version_id) REFERENCES material_comparable_group_versions(id) ON DELETE RESTRICT`
- `listing_comparable_assignments_reviewed_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reviewed_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `listing_comparable_assignments_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `listing_comparable_assignments_listing_variant_id_material_comp` — UNIQUE: `UNIQUE (listing_variant_id, material_comparable_group_version_id)`

**Indexes**

- `listing_comparable_assignments_listing_variant_id_material_comp` — `CREATE UNIQUE INDEX listing_comparable_assignments_listing_variant_id_material_comp ON public.listing_comparable_assignments USING btree (listing_variant_id, material_comparable_group_version_id)`
- `listing_comparable_assignments_pkey` — `CREATE UNIQUE INDEX listing_comparable_assignments_pkey ON public.listing_comparable_assignments USING btree (id)`
- `listing_one_effective_comparable_assignment` — `CREATE UNIQUE INDEX listing_one_effective_comparable_assignment ON public.listing_comparable_assignments USING btree (listing_variant_id) WHERE ((effective_until IS NULL) AND ((mapping_state)::text = 'APPROVED'::text))`

## `listing_media`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_listing_id` | `uuid` | No | — | Vendor listing id. |
| `file_id` | `uuid` | No | — | File id. |
| `alt_text` | `character varying` | Yes | — | Alt text. |
| `sort_order` | `smallint` | No | `'0'::smallint` | Sort order. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `listing_media_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `listing_media_vendor_listing_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_listing_id) REFERENCES vendor_listings(id) ON DELETE RESTRICT`
- `listing_media_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `listing_media_pkey` — `CREATE UNIQUE INDEX listing_media_pkey ON public.listing_media USING btree (id)`

## `listing_price_versions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `listing_variant_id` | `uuid` | No | — | Listing variant id. |
| `version` | `integer` | No | — | Version. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `currency` | `character` | No | `'PHP'::bpchar` | Currency. |
| `tax_category` | `character varying` | No | — | Tax category. |
| `effective_at` | `timestamp with time zone` | No | — | Effective at. |
| `retired_at` | `timestamp with time zone` | Yes | — | Retired at. |
| `created_by_user_id` | `bigint` | No | — | Created by user id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `listing_price_positive_check` — CHECK: `CHECK (amount_centavos > 0)`
- `listing_tax_category_check` — CHECK: `CHECK (tax_category::text = ANY (ARRAY['VAT_12'::character varying, 'VAT_ZERO'::character varying, 'VAT_EXEMPT'::character varying, 'NON_VAT'::character varying]::text[]))`
- `listing_price_versions_created_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (created_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `listing_price_versions_listing_variant_id_foreign` — FOREIGN KEY: `FOREIGN KEY (listing_variant_id) REFERENCES listing_variants(id) ON DELETE RESTRICT`
- `listing_price_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `listing_price_versions_listing_variant_id_version_unique` — UNIQUE: `UNIQUE (listing_variant_id, version)`

**Indexes**

- `listing_price_versions_listing_variant_id_effective_at_index` — `CREATE INDEX listing_price_versions_listing_variant_id_effective_at_index ON public.listing_price_versions USING btree (listing_variant_id, effective_at)`
- `listing_price_versions_listing_variant_id_version_unique` — `CREATE UNIQUE INDEX listing_price_versions_listing_variant_id_version_unique ON public.listing_price_versions USING btree (listing_variant_id, version)`
- `listing_price_versions_pkey` — `CREATE UNIQUE INDEX listing_price_versions_pkey ON public.listing_price_versions USING btree (id)`

## `listing_status_history`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_listing_id` | `uuid` | No | — | Vendor listing id. |
| `from_status` | `character varying` | Yes | — | From status. |
| `to_status` | `character varying` | No | — | To status. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `reason` | `text` | Yes | — | Reason. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `listing_status_history_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `listing_status_history_vendor_listing_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_listing_id) REFERENCES vendor_listings(id) ON DELETE RESTRICT`
- `listing_status_history_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `listing_status_history_pkey` — `CREATE UNIQUE INDEX listing_status_history_pkey ON public.listing_status_history USING btree (id)`

## `listing_variants`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_listing_id` | `uuid` | No | — | Vendor listing id. |
| `sku` | `character varying` | No | — | Sku. |
| `unit_id` | `uuid` | No | — | Unit id. |
| `attributes` | `jsonb` | Yes | — | Attributes. |
| `pack_quantity` | `numeric` | No | `'1'::numeric` | Pack quantity. |
| `active` | `boolean` | No | `true` | Active. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `listing_variants_pack_positive_check` — CHECK: `CHECK (pack_quantity > 0::numeric)`
- `listing_variants_unit_id_foreign` — FOREIGN KEY: `FOREIGN KEY (unit_id) REFERENCES units(id) ON DELETE RESTRICT`
- `listing_variants_vendor_listing_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_listing_id) REFERENCES vendor_listings(id) ON DELETE RESTRICT`
- `listing_variants_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `listing_variants_vendor_listing_id_sku_unique` — UNIQUE: `UNIQUE (vendor_listing_id, sku)`

**Indexes**

- `listing_variants_pkey` — `CREATE UNIQUE INDEX listing_variants_pkey ON public.listing_variants USING btree (id)`
- `listing_variants_vendor_listing_id_sku_unique` — `CREATE UNIQUE INDEX listing_variants_vendor_listing_id_sku_unique ON public.listing_variants USING btree (vendor_listing_id, sku)`

## `login_events`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | Yes | — | User id. |
| `normalized_email_hash` | `character varying` | Yes | — | Normalized email hash. |
| `event_type` | `character varying` | No | — | Event type. |
| `succeeded` | `boolean` | No | — | Succeeded. |
| `ip_address` | `character varying` | Yes | — | Ip address. |
| `correlation_id` | `character varying` | No | — | Correlation id. |
| `safe_context` | `jsonb` | Yes | — | Safe context. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `login_events_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL`
- `login_events_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `login_events_correlation_id_index` — `CREATE INDEX login_events_correlation_id_index ON public.login_events USING btree (correlation_id)`
- `login_events_event_type_created_at_index` — `CREATE INDEX login_events_event_type_created_at_index ON public.login_events USING btree (event_type, created_at)`
- `login_events_pkey` — `CREATE UNIQUE INDEX login_events_pkey ON public.login_events USING btree (id)`

## `material_aliases`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `material_id` | `uuid` | No | — | Material id. |
| `normalized_alias` | `character varying` | No | — | Normalized alias. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `material_aliases_material_id_foreign` — FOREIGN KEY: `FOREIGN KEY (material_id) REFERENCES materials(id) ON DELETE RESTRICT`
- `material_aliases_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `material_aliases_material_id_normalized_alias_unique` — UNIQUE: `UNIQUE (material_id, normalized_alias)`

**Indexes**

- `material_aliases_material_id_normalized_alias_unique` — `CREATE UNIQUE INDEX material_aliases_material_id_normalized_alias_unique ON public.material_aliases USING btree (material_id, normalized_alias)`
- `material_aliases_pkey` — `CREATE UNIQUE INDEX material_aliases_pkey ON public.material_aliases USING btree (id)`

## `material_categories`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `code` | `character varying` | No | — | Code. |
| `name` | `character varying` | No | — | Name. |
| `sort_order` | `smallint` | No | `'0'::smallint` | Sort order. |
| `active` | `boolean` | No | `true` | Active. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `material_categories_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `material_categories_code_unique` — UNIQUE: `UNIQUE (code)`

**Indexes**

- `material_categories_code_unique` — `CREATE UNIQUE INDEX material_categories_code_unique ON public.material_categories USING btree (code)`
- `material_categories_pkey` — `CREATE UNIQUE INDEX material_categories_pkey ON public.material_categories USING btree (id)`

## `material_comparable_group_versions`

MAT-01–MAT-07 comparability, observation, publication, or audience-safe aggregate record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `material_comparable_group_id` | `uuid` | No | — | Material comparable group id. |
| `version` | `integer` | No | — | Version. |
| `brand` | `character varying` | Yes | — | Brand. |
| `model` | `character varying` | Yes | — | Model. |
| `controlled_specification` | `jsonb` | No | — | Controlled specification. |
| `variant_key` | `character varying` | No | — | Variant key. |
| `canonical_unit_id` | `uuid` | No | — | Canonical unit id. |
| `conversion_version` | `character varying` | No | — | Conversion version. |
| `review_state` | `character varying` | No | `'DRAFT'::character varying` | Review state. |
| `reviewed_by_user_id` | `bigint` | Yes | — | Reviewed by user id. |
| `reviewed_at` | `timestamp with time zone` | Yes | — | Reviewed at. |
| `effective_from` | `timestamp with time zone` | No | — | Effective from. |
| `effective_until` | `timestamp with time zone` | Yes | — | Effective until. |
| `content_hash` | `character varying` | No | — | Content hash. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `comparable_group_review_check` — CHECK: `CHECK (review_state::text = ANY (ARRAY['DRAFT'::character varying, 'APPROVED'::character varying, 'REJECTED'::character varying, 'SUPERSEDED'::character varying]::text[]))`
- `comparable_group_version_dates_check` — CHECK: `CHECK (effective_until IS NULL OR effective_until > effective_from)`
- `material_comparable_group_versions_canonical_unit_id_foreign` — FOREIGN KEY: `FOREIGN KEY (canonical_unit_id) REFERENCES units(id) ON DELETE RESTRICT`
- `material_comparable_group_versions_material_comparable_group_id` — FOREIGN KEY: `FOREIGN KEY (material_comparable_group_id) REFERENCES material_comparable_groups(id) ON DELETE RESTRICT`
- `material_comparable_group_versions_reviewed_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reviewed_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `material_comparable_group_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `material_comparable_group_versions_unique_variant_version` — UNIQUE: `UNIQUE (material_comparable_group_id, variant_key, version)`

**Indexes**

- `material_comparable_group_versions_pkey` — `CREATE UNIQUE INDEX material_comparable_group_versions_pkey ON public.material_comparable_group_versions USING btree (id)`
- `material_comparable_group_versions_unique_variant_version` — `CREATE UNIQUE INDEX material_comparable_group_versions_unique_variant_version ON public.material_comparable_group_versions USING btree (material_comparable_group_id, variant_key, version)`

## `material_comparable_groups`

MAT-01–MAT-07 comparability, observation, publication, or audience-safe aggregate record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `material_id` | `uuid` | No | — | Material id. |
| `code` | `character varying` | No | — | Code. |
| `display_name` | `character varying` | No | — | Display name. |
| `status` | `character varying` | No | `'DRAFT'::character varying` | Status. |
| `current_version_id` | `uuid` | Yes | — | Current version id. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `material_comparable_groups_current_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (current_version_id) REFERENCES material_comparable_group_versions(id) ON DELETE RESTRICT`
- `material_comparable_groups_material_id_foreign` — FOREIGN KEY: `FOREIGN KEY (material_id) REFERENCES materials(id) ON DELETE RESTRICT`
- `material_comparable_groups_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `material_comparable_groups_code_unique` — UNIQUE: `UNIQUE (code)`

**Indexes**

- `material_comparable_groups_code_unique` — `CREATE UNIQUE INDEX material_comparable_groups_code_unique ON public.material_comparable_groups USING btree (code)`
- `material_comparable_groups_pkey` — `CREATE UNIQUE INDEX material_comparable_groups_pkey ON public.material_comparable_groups USING btree (id)`

## `material_price_daily_aggregates`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `analytics_refresh_run_id` | `uuid` | No | — | Analytics refresh run id. |
| `material_comparable_group_version_id` | `uuid` | No | — | Material comparable group version id. |
| `environment` | `character varying` | No | — | Environment. |
| `dataset_id` | `character varying` | No | — | Dataset id. |
| `local_snapshot_date` | `date` | No | — | Local snapshot date. |
| `audience` | `character varying` | No | — | Audience. |
| `geography_scope_type` | `character varying` | No | — | Geography scope type. |
| `geography_scope_hash` | `character varying` | No | — | Geography scope hash. |
| `radius_km` | `smallint` | Yes | — | Radius km. |
| `distinct_vendor_count` | `integer` | No | — | Distinct vendor count. |
| `distinct_listing_count` | `integer` | No | — | Distinct listing count. |
| `average_php_price` | `numeric` | No | — | Average php price. |
| `minimum_centavos` | `bigint` | No | — | Integer Philippine centavos. Minimum centavos. |
| `maximum_centavos` | `bigint` | No | — | Integer Philippine centavos. Maximum centavos. |
| `composition_hash` | `character varying` | No | — | Composition hash. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `material_daily_aggregate_radius_check` — CHECK: `CHECK (geography_scope_type::text = 'RADIUS'::text AND (radius_km = ANY (ARRAY[5, 10, 20, 30, 40, 50])) OR geography_scope_type::text = 'PSGC'::text AND radius_km IS NULL)`
- `material_daily_aggregate_scope_check` — CHECK: `CHECK ((environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying]::text[])) AND (audience::text = ANY (ARRAY['BUYER'::character varying, 'VENDOR'::character varying, 'ADMIN'::character varying]::text[])) AND (geography_scope_type::text = ANY (ARRAY['RADIUS'::character varying, 'PSGC'::character varying]::text[])))`
- `material_daily_aggregate_values_check` — CHECK: `CHECK (distinct_vendor_count >= 1 AND distinct_listing_count >= 1 AND average_php_price > 0::numeric AND minimum_centavos > 0 AND maximum_centavos >= minimum_centavos AND (audience::text <> 'VENDOR'::text OR distinct_vendor_count >= 3))`
- `material_price_daily_aggregates_analytics_refresh_run_id_foreig` — FOREIGN KEY: `FOREIGN KEY (analytics_refresh_run_id) REFERENCES analytics_refresh_runs(id) ON DELETE RESTRICT`
- `material_price_daily_aggregates_material_comparable_group_versi` — FOREIGN KEY: `FOREIGN KEY (material_comparable_group_version_id) REFERENCES material_comparable_group_versions(id) ON DELETE RESTRICT`
- `material_price_daily_aggregates_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `material_daily_aggregate_scope_unique` — UNIQUE: `UNIQUE (analytics_refresh_run_id, material_comparable_group_version_id, audience, geography_scope_hash, radius_km)`

**Indexes**

- `material_daily_aggregate_lookup_index` — `CREATE INDEX material_daily_aggregate_lookup_index ON public.material_price_daily_aggregates USING btree (environment, dataset_id, material_comparable_group_version_id, local_snapshot_date)`
- `material_daily_aggregate_null_safe_unique` — `CREATE UNIQUE INDEX material_daily_aggregate_null_safe_unique ON public.material_price_daily_aggregates USING btree (analytics_refresh_run_id, material_comparable_group_version_id, audience, geography_scope_hash, COALESCE((radius_km)::integer, 0))`
- `material_daily_aggregate_scope_unique` — `CREATE UNIQUE INDEX material_daily_aggregate_scope_unique ON public.material_price_daily_aggregates USING btree (analytics_refresh_run_id, material_comparable_group_version_id, audience, geography_scope_hash, radius_km)`
- `material_price_daily_aggregates_pkey` — `CREATE UNIQUE INDEX material_price_daily_aggregates_pkey ON public.material_price_daily_aggregates USING btree (id)`

## `material_tag_links`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `material_id` | `uuid` | No | — | Material id. |
| `material_tag_id` | `uuid` | No | — | Material tag id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `material_tag_links_material_id_foreign` — FOREIGN KEY: `FOREIGN KEY (material_id) REFERENCES materials(id) ON DELETE RESTRICT`
- `material_tag_links_material_tag_id_foreign` — FOREIGN KEY: `FOREIGN KEY (material_tag_id) REFERENCES material_tags(id) ON DELETE RESTRICT`
- `material_tag_links_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `material_tag_links_material_id_material_tag_id_unique` — UNIQUE: `UNIQUE (material_id, material_tag_id)`

**Indexes**

- `material_tag_links_material_id_material_tag_id_unique` — `CREATE UNIQUE INDEX material_tag_links_material_id_material_tag_id_unique ON public.material_tag_links USING btree (material_id, material_tag_id)`
- `material_tag_links_pkey` — `CREATE UNIQUE INDEX material_tag_links_pkey ON public.material_tag_links USING btree (id)`

## `material_tags`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `code` | `character varying` | No | — | Code. |
| `name` | `character varying` | No | — | Name. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `material_tags_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `material_tags_code_unique` — UNIQUE: `UNIQUE (code)`

**Indexes**

- `material_tags_code_unique` — `CREATE UNIQUE INDEX material_tags_code_unique ON public.material_tags USING btree (code)`
- `material_tags_pkey` — `CREATE UNIQUE INDEX material_tags_pkey ON public.material_tags USING btree (id)`

## `materials`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `material_category_id` | `uuid` | No | — | Material category id. |
| `code` | `character varying` | No | — | Code. |
| `name` | `character varying` | No | — | Name. |
| `description` | `text` | Yes | — | Description. |
| `canonical_unit_id` | `uuid` | No | — | Canonical unit id. |
| `regulated` | `boolean` | No | `false` | Regulated. |
| `active` | `boolean` | No | `true` | Active. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `materials_canonical_unit_id_foreign` — FOREIGN KEY: `FOREIGN KEY (canonical_unit_id) REFERENCES units(id) ON DELETE RESTRICT`
- `materials_material_category_id_foreign` — FOREIGN KEY: `FOREIGN KEY (material_category_id) REFERENCES material_categories(id) ON DELETE RESTRICT`
- `materials_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `materials_code_unique` — UNIQUE: `UNIQUE (code)`

**Indexes**

- `materials_code_unique` — `CREATE UNIQUE INDEX materials_code_unique ON public.materials USING btree (code)`
- `materials_pkey` — `CREATE UNIQUE INDEX materials_pkey ON public.materials USING btree (id)`

## `materials_analytics_result_cache`

MAT-01–MAT-07 comparability, observation, publication, or audience-safe aggregate record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `analytics_refresh_run_id` | `uuid` | No | — | Analytics refresh run id. |
| `environment` | `character varying` | No | — | Environment. |
| `dataset_id` | `character varying` | No | — | Dataset id. |
| `audience` | `character varying` | No | — | Audience. |
| `geography_scope_type` | `character varying` | No | — | Geography scope type. |
| `geography_scope_hash` | `character varying` | No | — | Geography scope hash. |
| `radius_km` | `smallint` | Yes | — | Radius km. |
| `window_start` | `date` | No | — | Window start. |
| `window_end` | `date` | No | — | Window end. |
| `minimum_vendor_count` | `smallint` | No | — | Minimum vendor count. |
| `filters_hash` | `character varying` | No | — | Filters hash. |
| `safe_response` | `jsonb` | No | — | Safe response. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `materials_analytics_cache_radius_check` — CHECK: `CHECK (geography_scope_type::text = 'RADIUS'::text AND (radius_km = ANY (ARRAY[5, 10, 20, 30, 40, 50])) OR geography_scope_type::text = 'PSGC'::text AND radius_km IS NULL)`
- `materials_analytics_cache_scope_check` — CHECK: `CHECK ((environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying]::text[])) AND (audience::text = ANY (ARRAY['BUYER'::character varying, 'VENDOR'::character varying, 'ADMIN'::character varying]::text[])) AND (geography_scope_type::text = ANY (ARRAY['RADIUS'::character varying, 'PSGC'::character varying]::text[])))`
- `materials_analytics_cache_window_check` — CHECK: `CHECK (window_start <= window_end AND (window_end - window_start) <= 365 AND minimum_vendor_count >= 1 AND (audience::text <> 'VENDOR'::text OR minimum_vendor_count >= 3))`
- `materials_analytics_result_cache_analytics_refresh_run_id_forei` — FOREIGN KEY: `FOREIGN KEY (analytics_refresh_run_id) REFERENCES analytics_refresh_runs(id) ON DELETE RESTRICT`
- `materials_analytics_result_cache_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `materials_analytics_cache_scope_unique` — UNIQUE: `UNIQUE (analytics_refresh_run_id, audience, geography_scope_hash, radius_km, window_start, window_end, filters_hash)`

**Indexes**

- `materials_analytics_cache_expiry_index` — `CREATE INDEX materials_analytics_cache_expiry_index ON public.materials_analytics_result_cache USING btree (environment, dataset_id, audience, expires_at)`
- `materials_analytics_cache_null_safe_unique` — `CREATE UNIQUE INDEX materials_analytics_cache_null_safe_unique ON public.materials_analytics_result_cache USING btree (analytics_refresh_run_id, audience, geography_scope_hash, COALESCE((radius_km)::integer, 0), window_start, window_end, filters_hash)`
- `materials_analytics_cache_scope_unique` — `CREATE UNIQUE INDEX materials_analytics_cache_scope_unique ON public.materials_analytics_result_cache USING btree (analytics_refresh_run_id, audience, geography_scope_hash, radius_km, window_start, window_end, filters_hash)`
- `materials_analytics_result_cache_pkey` — `CREATE UNIQUE INDEX materials_analytics_result_cache_pkey ON public.materials_analytics_result_cache USING btree (id)`

## `message_attachments`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `message_id` | `uuid` | No | — | Message id. |
| `file_id` | `uuid` | No | — | File id. |
| `scan_state` | `character varying` | No | — | Scan state. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `message_attachments_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `message_attachments_message_id_foreign` — FOREIGN KEY: `FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE RESTRICT`
- `message_attachments_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `message_attachments_pkey` — `CREATE UNIQUE INDEX message_attachments_pkey ON public.message_attachments USING btree (id)`

## `message_read_receipts`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `message_id` | `uuid` | No | — | Message id. |
| `user_id` | `bigint` | No | — | User id. |
| `read_at` | `timestamp with time zone` | No | — | Read at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `message_read_receipts_message_id_foreign` — FOREIGN KEY: `FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE RESTRICT`
- `message_read_receipts_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `message_read_receipts_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `message_read_receipts_message_id_user_id_unique` — UNIQUE: `UNIQUE (message_id, user_id)`

**Indexes**

- `message_read_receipts_message_id_user_id_unique` — `CREATE UNIQUE INDEX message_read_receipts_message_id_user_id_unique ON public.message_read_receipts USING btree (message_id, user_id)`
- `message_read_receipts_pkey` — `CREATE UNIQUE INDEX message_read_receipts_pkey ON public.message_read_receipts USING btree (id)`

## `messages`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `conversation_id` | `uuid` | No | — | Conversation id. |
| `sender_user_id` | `bigint` | No | — | Sender user id. |
| `client_message_id` | `uuid` | No | — | Client message id. |
| `body` | `text` | No | — | Body. |
| `sent_at` | `timestamp with time zone` | No | — | Sent at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `messages_conversation_id_foreign` — FOREIGN KEY: `FOREIGN KEY (conversation_id) REFERENCES conversations(id) ON DELETE RESTRICT`
- `messages_sender_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (sender_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `messages_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `messages_conversation_id_client_message_id_unique` — UNIQUE: `UNIQUE (conversation_id, client_message_id)`

**Indexes**

- `messages_conversation_id_client_message_id_unique` — `CREATE UNIQUE INDEX messages_conversation_id_client_message_id_unique ON public.messages USING btree (conversation_id, client_message_id)`
- `messages_pkey` — `CREATE UNIQUE INDEX messages_pkey ON public.messages USING btree (id)`

## `metric_events`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `metric` | `character varying` | No | — | Metric. |
| `source_id` | `uuid` | No | — | Source id. |
| `value` | `numeric` | No | — | Value. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `metric_events_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `metric_events_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `metric_events_metric_source_id_unique` — UNIQUE: `UNIQUE (metric, source_id)`

**Indexes**

- `metric_events_metric_source_id_unique` — `CREATE UNIQUE INDEX metric_events_metric_source_id_unique ON public.metric_events USING btree (metric, source_id)`
- `metric_events_pkey` — `CREATE UNIQUE INDEX metric_events_pkey ON public.metric_events USING btree (id)`

## `migrations`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `integer` | No | `nextval('migrations_id_seq'::regclass)` | Primary key. Id. |
| `migration` | `character varying` | No | — | Migration. |
| `batch` | `integer` | No | — | Batch. |

**Constraints**

- `migrations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `migrations_pkey` — `CREATE UNIQUE INDEX migrations_pkey ON public.migrations USING btree (id)`

## `no_show_events`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `fulfillment_id` | `uuid` | No | — | Fulfillment id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `event_type` | `character varying` | Yes | — | Event type. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `occurred_at` | `timestamp with time zone` | Yes | — | Occurred at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `no_show_events_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `no_show_events_fulfillment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fulfillment_id) REFERENCES fulfillments(id) ON DELETE RESTRICT`
- `no_show_events_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `no_show_events_pkey` — `CREATE UNIQUE INDEX no_show_events_pkey ON public.no_show_events USING btree (id)`

## `notification_deliveries`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `notification_id` | `uuid` | No | — | Notification id. |
| `channel` | `character varying` | No | — | Channel. |
| `state` | `character varying` | No | — | State. |
| `attempt_count` | `smallint` | No | `'0'::smallint` | Attempt count. |
| `delivered_at` | `timestamp with time zone` | Yes | — | Delivered at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `notification_deliveries_notification_id_foreign` — FOREIGN KEY: `FOREIGN KEY (notification_id) REFERENCES notifications(id) ON DELETE RESTRICT`
- `notification_deliveries_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `notification_deliveries_pkey` — `CREATE UNIQUE INDEX notification_deliveries_pkey ON public.notification_deliveries USING btree (id)`

## `notification_preferences`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `category` | `character varying` | No | — | Category. |
| `in_app` | `boolean` | No | `true` | In app. |
| `email` | `boolean` | No | `true` | Email. |
| `push` | `boolean` | No | `true` | Push. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `notification_preferences_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `notification_preferences_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `notification_preferences_user_id_category_unique` — UNIQUE: `UNIQUE (user_id, category)`

**Indexes**

- `notification_preferences_pkey` — `CREATE UNIQUE INDEX notification_preferences_pkey ON public.notification_preferences USING btree (id)`
- `notification_preferences_user_id_category_unique` — `CREATE UNIQUE INDEX notification_preferences_user_id_category_unique ON public.notification_preferences USING btree (user_id, category)`

## `notifications`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `category` | `character varying` | No | — | Category. |
| `title` | `character varying` | No | — | Title. |
| `body` | `text` | No | — | Body. |
| `resource_type` | `character varying` | Yes | — | Resource type. |
| `resource_id` | `uuid` | Yes | — | Resource id. |
| `read_at` | `timestamp with time zone` | Yes | — | Read at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `notifications_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `notifications_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `notifications_pkey` — `CREATE UNIQUE INDEX notifications_pkey ON public.notifications USING btree (id)`
- `notifications_user_id_read_at_created_at_index` — `CREATE INDEX notifications_user_id_read_at_created_at_index ON public.notifications USING btree (user_id, read_at, created_at)`

## `nrpc_acceptances`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `reason` | `text` | Yes | — | Reason. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `nrpc_acceptances_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `nrpc_acceptances_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `nrpc_acceptances_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `nrpc_acceptances_pkey` — `CREATE UNIQUE INDEX nrpc_acceptances_pkey ON public.nrpc_acceptances USING btree (id)`

## `nrpc_records`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `reason` | `text` | Yes | — | Reason. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `amount_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Amount centavos. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `nrpc_records_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `nrpc_records_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `nrpc_records_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `nrpc_records_pkey` — `CREATE UNIQUE INDEX nrpc_records_pkey ON public.nrpc_records USING btree (id)`

## `oauth_access_tokens`

Identity and secure-session record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `character` | No | — | Primary key. Id. |
| `user_id` | `bigint` | Yes | — | User id. |
| `client_id` | `uuid` | No | — | Client id. |
| `name` | `character varying` | Yes | — | Name. |
| `scopes` | `text` | Yes | — | Scopes. |
| `revoked` | `boolean` | No | — | Revoked. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `expires_at` | `timestamp with time zone` | Yes | — | Expires at. |

**Constraints**

- `oauth_access_tokens_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `oauth_access_tokens_pkey` — `CREATE UNIQUE INDEX oauth_access_tokens_pkey ON public.oauth_access_tokens USING btree (id)`
- `oauth_access_tokens_user_id_index` — `CREATE INDEX oauth_access_tokens_user_id_index ON public.oauth_access_tokens USING btree (user_id)`

## `oauth_auth_codes`

Identity and secure-session record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `character` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `client_id` | `uuid` | No | — | Client id. |
| `scopes` | `text` | Yes | — | Scopes. |
| `revoked` | `boolean` | No | — | Revoked. |
| `expires_at` | `timestamp with time zone` | Yes | — | Expires at. |

**Constraints**

- `oauth_auth_codes_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `oauth_auth_codes_pkey` — `CREATE UNIQUE INDEX oauth_auth_codes_pkey ON public.oauth_auth_codes USING btree (id)`
- `oauth_auth_codes_user_id_index` — `CREATE INDEX oauth_auth_codes_user_id_index ON public.oauth_auth_codes USING btree (user_id)`

## `oauth_clients`

Identity and secure-session record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `owner_type` | `character varying` | Yes | — | Owner type. |
| `owner_id` | `bigint` | Yes | — | Owner id. |
| `name` | `character varying` | No | — | Name. |
| `secret` | `character varying` | Yes | — | Secret. |
| `provider` | `character varying` | Yes | — | Provider. |
| `redirect_uris` | `text` | No | — | Redirect uris. |
| `grant_types` | `text` | No | — | Grant types. |
| `revoked` | `boolean` | No | — | Revoked. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `oauth_clients_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `oauth_clients_owner_type_owner_id_index` — `CREATE INDEX oauth_clients_owner_type_owner_id_index ON public.oauth_clients USING btree (owner_type, owner_id)`
- `oauth_clients_pkey` — `CREATE UNIQUE INDEX oauth_clients_pkey ON public.oauth_clients USING btree (id)`

## `oauth_device_codes`

Identity and secure-session record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `character` | No | — | Primary key. Id. |
| `user_id` | `bigint` | Yes | — | User id. |
| `client_id` | `uuid` | No | — | Client id. |
| `user_code` | `character` | No | — | User code. |
| `scopes` | `text` | No | — | Scopes. |
| `revoked` | `boolean` | No | — | Revoked. |
| `user_approved_at` | `timestamp with time zone` | Yes | — | User approved at. |
| `last_polled_at` | `timestamp with time zone` | Yes | — | Last polled at. |
| `expires_at` | `timestamp with time zone` | Yes | — | Expires at. |

**Constraints**

- `oauth_device_codes_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `oauth_device_codes_user_code_unique` — UNIQUE: `UNIQUE (user_code)`

**Indexes**

- `oauth_device_codes_client_id_index` — `CREATE INDEX oauth_device_codes_client_id_index ON public.oauth_device_codes USING btree (client_id)`
- `oauth_device_codes_pkey` — `CREATE UNIQUE INDEX oauth_device_codes_pkey ON public.oauth_device_codes USING btree (id)`
- `oauth_device_codes_user_code_unique` — `CREATE UNIQUE INDEX oauth_device_codes_user_code_unique ON public.oauth_device_codes USING btree (user_code)`
- `oauth_device_codes_user_id_index` — `CREATE INDEX oauth_device_codes_user_id_index ON public.oauth_device_codes USING btree (user_id)`

## `oauth_refresh_tokens`

Identity and secure-session record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `character` | No | — | Primary key. Id. |
| `access_token_id` | `character` | No | — | Access token id. |
| `revoked` | `boolean` | No | — | Revoked. |
| `expires_at` | `timestamp with time zone` | Yes | — | Expires at. |

**Constraints**

- `oauth_refresh_tokens_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `oauth_refresh_tokens_access_token_id_index` — `CREATE INDEX oauth_refresh_tokens_access_token_id_index ON public.oauth_refresh_tokens USING btree (access_token_id)`
- `oauth_refresh_tokens_pkey` — `CREATE UNIQUE INDEX oauth_refresh_tokens_pkey ON public.oauth_refresh_tokens USING btree (id)`

## `operating_hours`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `store_profile_id` | `uuid` | No | — | Store profile id. |
| `weekday` | `smallint` | No | — | Weekday. |
| `opens_at` | `time without time zone` | Yes | — | Opens at. |
| `closes_at` | `time without time zone` | Yes | — | Closes at. |
| `closed` | `boolean` | No | `false` | Closed. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `operating_hours_store_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (store_profile_id) REFERENCES store_profiles(id) ON DELETE RESTRICT`
- `operating_hours_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `operating_hours_store_profile_id_weekday_unique` — UNIQUE: `UNIQUE (store_profile_id, weekday)`

**Indexes**

- `operating_hours_pkey` — `CREATE UNIQUE INDEX operating_hours_pkey ON public.operating_hours USING btree (id)`
- `operating_hours_store_profile_id_weekday_unique` — `CREATE UNIQUE INDEX operating_hours_store_profile_id_weekday_unique ON public.operating_hours USING btree (store_profile_id, weekday)`

## `order_lines`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `listing_variant_id` | `uuid` | No | — | Listing variant id. |
| `unit_id` | `uuid` | No | — | Unit id. |
| `quantity` | `numeric` | No | — | Quantity. |
| `unit_price_centavos` | `bigint` | No | — | Integer Philippine centavos. Unit price centavos. |
| `discount_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Discount centavos. |
| `included_vat_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Included vat centavos. |
| `payable_centavos` | `bigint` | No | — | Integer Philippine centavos. Payable centavos. |
| `tax_category` | `character varying` | No | — | Tax category. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `order_line_amounts_check` — CHECK: `CHECK (quantity > 0::numeric AND unit_price_centavos >= 0 AND discount_centavos >= 0 AND included_vat_centavos >= 0 AND payable_centavos >= 0)`
- `order_lines_listing_variant_id_foreign` — FOREIGN KEY: `FOREIGN KEY (listing_variant_id) REFERENCES listing_variants(id) ON DELETE RESTRICT`
- `order_lines_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `order_lines_unit_id_foreign` — FOREIGN KEY: `FOREIGN KEY (unit_id) REFERENCES units(id) ON DELETE RESTRICT`
- `order_lines_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `order_lines_pkey` — `CREATE UNIQUE INDEX order_lines_pkey ON public.order_lines USING btree (id)`

## `order_snapshots`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `version` | `integer` | No | — | Version. |
| `snapshot` | `jsonb` | No | — | Snapshot. |
| `content_hash` | `character varying` | No | — | Content hash. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `order_snapshots_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `order_snapshots_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `order_snapshots_order_id_version_unique` — UNIQUE: `UNIQUE (order_id, version)`

**Indexes**

- `order_snapshots_order_id_version_unique` — `CREATE UNIQUE INDEX order_snapshots_order_id_version_unique ON public.order_snapshots USING btree (order_id, version)`
- `order_snapshots_pkey` — `CREATE UNIQUE INDEX order_snapshots_pkey ON public.order_snapshots USING btree (id)`

## `order_status_history`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `from_state` | `character varying` | Yes | — | From state. |
| `to_state` | `character varying` | No | — | To state. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `reason` | `text` | Yes | — | Reason. |
| `correlation_id` | `character varying` | No | — | Correlation id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `order_status_history_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `order_status_history_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `order_status_history_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `order_status_history_correlation_id_index` — `CREATE INDEX order_status_history_correlation_id_index ON public.order_status_history USING btree (correlation_id)`
- `order_status_history_pkey` — `CREATE UNIQUE INDEX order_status_history_pkey ON public.order_status_history USING btree (id)`

## `orders`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `reference` | `character varying` | No | — | Reference. |
| `buyer_profile_id` | `uuid` | No | — | Buyer profile id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `checkout_group_id` | `uuid` | Yes | — | Checkout group id. |
| `work_package_id` | `uuid` | Yes | — | Work package id. |
| `quotation_version_id` | `uuid` | Yes | — | Quotation version id. |
| `procurement_type` | `character varying` | No | — | Procurement type. |
| `order_state` | `character varying` | No | — | Order state. |
| `payment_state` | `character varying` | No | `'NOT_REQUIRED'::character varying` | Payment state. |
| `refund_state` | `character varying` | No | `'NOT_REQUESTED'::character varying` | Refund state. |
| `fulfillment_method` | `character varying` | No | — | Fulfillment method. |
| `payment_method` | `character varying` | No | — | Payment method. |
| `commercial_total_centavos` | `bigint` | No | — | Integer Philippine centavos. Commercial total centavos. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `orders_buyer_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (buyer_profile_id) REFERENCES buyer_profiles(id) ON DELETE RESTRICT`
- `orders_checkout_group_id_foreign` — FOREIGN KEY: `FOREIGN KEY (checkout_group_id) REFERENCES checkout_groups(id) ON DELETE RESTRICT`
- `orders_quotation_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (quotation_version_id) REFERENCES quotation_versions(id) ON DELETE RESTRICT`
- `orders_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `orders_work_package_id_foreign` — FOREIGN KEY: `FOREIGN KEY (work_package_id) REFERENCES work_packages(id) ON DELETE RESTRICT`
- `orders_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `orders_reference_unique` — UNIQUE: `UNIQUE (reference)`

**Indexes**

- `orders_order_state_index` — `CREATE INDEX orders_order_state_index ON public.orders USING btree (order_state)`
- `orders_pkey` — `CREATE UNIQUE INDEX orders_pkey ON public.orders USING btree (id)`
- `orders_reference_unique` — `CREATE UNIQUE INDEX orders_reference_unique ON public.orders USING btree (reference)`

## `outbox_events`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `event_type` | `character varying` | No | — | Event type. |
| `aggregate_type` | `character varying` | No | — | Aggregate type. |
| `aggregate_id` | `uuid` | No | — | Aggregate id. |
| `payload` | `jsonb` | No | — | Payload. |
| `available_at` | `timestamp with time zone` | No | — | Available at. |
| `processed_at` | `timestamp with time zone` | Yes | — | Processed at. |
| `attempts` | `smallint` | No | `'0'::smallint` | Attempts. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `outbox_events_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `outbox_events_pkey` — `CREATE UNIQUE INDEX outbox_events_pkey ON public.outbox_events USING btree (id)`
- `outbox_events_processed_at_available_at_index` — `CREATE INDEX outbox_events_processed_at_available_at_index ON public.outbox_events USING btree (processed_at, available_at)`

## `password_reset_tokens`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `email` | `character varying` | No | — | Primary key. Email. |
| `token` | `character varying` | No | — | Token. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |

**Constraints**

- `password_reset_tokens_pkey` — PRIMARY KEY: `PRIMARY KEY (email)`

**Indexes**

- `password_reset_tokens_pkey` — `CREATE UNIQUE INDEX password_reset_tokens_pkey ON public.password_reset_tokens USING btree (email)`

## `payment_attempts`

Payment, refund, or physical-money evidence record with an independent lifecycle.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `payment_id` | `uuid` | No | — | Payment id. |
| `provider_event_id` | `character varying` | Yes | — | Provider event id. |
| `state` | `character varying` | No | — | State. |
| `safe_payload` | `jsonb` | Yes | — | Safe payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `payment_attempts_payment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (payment_id) REFERENCES payments(id) ON DELETE RESTRICT`
- `payment_attempts_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `payment_attempts_provider_event_id_unique` — UNIQUE: `UNIQUE (provider_event_id)`

**Indexes**

- `payment_attempts_pkey` — `CREATE UNIQUE INDEX payment_attempts_pkey ON public.payment_attempts USING btree (id)`
- `payment_attempts_provider_event_id_unique` — `CREATE UNIQUE INDEX payment_attempts_provider_event_id_unique ON public.payment_attempts USING btree (provider_event_id)`

## `payment_events`

Payment, refund, or physical-money evidence record with an independent lifecycle.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `payment_id` | `uuid` | No | — | Payment id. |
| `provider_event_id` | `character varying` | Yes | — | Provider event id. |
| `state` | `character varying` | No | — | State. |
| `safe_payload` | `jsonb` | Yes | — | Safe payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `payment_events_payment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (payment_id) REFERENCES payments(id) ON DELETE RESTRICT`
- `payment_events_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `payment_events_provider_event_id_unique` — UNIQUE: `UNIQUE (provider_event_id)`

**Indexes**

- `payment_events_pkey` — `CREATE UNIQUE INDEX payment_events_pkey ON public.payment_events USING btree (id)`
- `payment_events_provider_event_id_unique` — `CREATE UNIQUE INDEX payment_events_provider_event_id_unique ON public.payment_events USING btree (provider_event_id)`

## `payments`

Payment, refund, or physical-money evidence record with an independent lifecycle.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | Yes | — | Order id. |
| `fee_statement_id` | `uuid` | Yes | — | Fee statement id. |
| `purpose` | `character varying` | No | — | Purpose. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `provider` | `character varying` | No | `'XENDIT'::character varying` | Provider. |
| `provider_reference` | `character varying` | Yes | — | Provider reference. |
| `principal_centavos` | `bigint` | No | — | Integer Philippine centavos. Principal centavos. |
| `processing_fee_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Processing fee centavos. |
| `total_centavos` | `bigint` | No | — | Integer Philippine centavos. Total centavos. |
| `currency` | `character` | No | `'PHP'::bpchar` | Currency. |
| `state` | `character varying` | No | `'PENDING'::character varying` | State. |
| `idempotency_key` | `character varying` | No | — | Idempotency key. |
| `expires_at` | `timestamp with time zone` | Yes | — | Expires at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `payment_amounts_check` — CHECK: `CHECK (principal_centavos >= 0 AND processing_fee_centavos >= 0 AND total_centavos = (principal_centavos + processing_fee_centavos))`
- `payments_purpose_check` — CHECK: `CHECK (purpose::text = ANY (ARRAY['FULL_ORDER_PAYMENT'::character varying, 'NRPC_ASSURANCE_PAYMENT'::character varying, 'ORDER_BALANCE_PAYMENT'::character varying, 'PLATFORM_FEE_PAYMENT'::character varying]::text[]))`
- `payments_fee_statement_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fee_statement_id) REFERENCES fee_statements(id) ON DELETE RESTRICT`
- `payments_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `payments_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `payments_idempotency_key_unique` — UNIQUE: `UNIQUE (idempotency_key)`
- `payments_provider_reference_unique` — UNIQUE: `UNIQUE (provider_reference)`

**Indexes**

- `payments_idempotency_key_unique` — `CREATE UNIQUE INDEX payments_idempotency_key_unique ON public.payments USING btree (idempotency_key)`
- `payments_pkey` — `CREATE UNIQUE INDEX payments_pkey ON public.payments USING btree (id)`
- `payments_provider_reference_unique` — `CREATE UNIQUE INDEX payments_provider_reference_unique ON public.payments USING btree (provider_reference)`

## `permissions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `code` | `character varying` | No | — | Code. |
| `description` | `character varying` | No | — | Description. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `permissions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `permissions_code_unique` — UNIQUE: `UNIQUE (code)`

**Indexes**

- `permissions_code_unique` — `CREATE UNIQUE INDEX permissions_code_unique ON public.permissions USING btree (code)`
- `permissions_pkey` — `CREATE UNIQUE INDEX permissions_pkey ON public.permissions USING btree (id)`

## `physical_payment_records`

Payment, refund, or physical-money evidence record with an independent lifecycle.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `recorded_by_user_id` | `bigint` | No | — | Recorded by user id. |
| `record_kind` | `character varying` | No | `'COLLECTION'::character varying` | Record kind. |
| `method` | `character varying` | No | — | Method. |
| `obligation_before_centavos` | `bigint` | No | — | Integer Philippine centavos. Obligation before centavos. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `remaining_obligation_centavos` | `bigint` | No | — | Integer Philippine centavos. Remaining obligation centavos. |
| `state` | `character varying` | No | — | State. |
| `evidence_file_id` | `uuid` | Yes | — | Evidence file id. |
| `original_record_id` | `uuid` | Yes | — | Original record id. |
| `correction_reason` | `text` | Yes | — | Correction reason. |
| `idempotency_key` | `character varying` | No | — | Idempotency key. |
| `recorded_at` | `timestamp with time zone` | No | — | Recorded at. |
| `buyer_acknowledged_at` | `timestamp with time zone` | Yes | — | Buyer acknowledged at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `physical_payment_amount_check` — CHECK: `CHECK (obligation_before_centavos >= 0 AND amount_centavos >= 0 AND remaining_obligation_centavos >= 0 AND amount_centavos <= obligation_before_centavos AND remaining_obligation_centavos = (obligation_before_centavos - amount_centavos))`
- `physical_payment_correction_link_check` — CHECK: `CHECK ((record_kind::text = 'CORRECTION'::text) = (original_record_id IS NOT NULL AND correction_reason IS NOT NULL))`
- `physical_payment_evidence_check` — CHECK: `CHECK (amount_centavos = 0 OR evidence_file_id IS NOT NULL)`
- `physical_payment_kind_check` — CHECK: `CHECK (record_kind::text = ANY (ARRAY['COLLECTION'::character varying, 'CORRECTION'::character varying, 'CANCELLATION_RELEASE'::character varying]::text[]))`
- `physical_payment_state_amount_check` — CHECK: `CHECK (state::text = 'UNPAID'::text AND amount_centavos = 0 AND remaining_obligation_centavos = obligation_before_centavos OR state::text = 'PARTIALLY_RECORDED'::text AND amount_centavos > 0 AND remaining_obligation_centavos > 0 OR state::text = 'PHYSICAL_PAYMENT_RECORDED'::text AND amount_centavos > 0 AND remaining_obligation_centavos = 0 OR state::text = 'CANCELLED_UNPAID'::text AND record_kind::text = 'CANCELLATION_RELEASE'::text AND amount_centavos = 0 AND remaining_obligation_centavos = obligation_before_centavos)`
- `physical_payment_state_check` — CHECK: `CHECK (state::text = ANY (ARRAY['UNPAID'::character varying, 'PARTIALLY_RECORDED'::character varying, 'PHYSICAL_PAYMENT_RECORDED'::character varying, 'CANCELLED_UNPAID'::character varying]::text[]))`
- `physical_payment_records_evidence_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (evidence_file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `physical_payment_records_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `physical_payment_records_original_record_id_foreign` — FOREIGN KEY: `FOREIGN KEY (original_record_id) REFERENCES physical_payment_records(id) ON DELETE RESTRICT`
- `physical_payment_records_recorded_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (recorded_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `physical_payment_records_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `physical_payment_records_idempotency_key_unique` — UNIQUE: `UNIQUE (idempotency_key)`

**Indexes**

- `physical_payment_records_idempotency_key_unique` — `CREATE UNIQUE INDEX physical_payment_records_idempotency_key_unique ON public.physical_payment_records USING btree (idempotency_key)`
- `physical_payment_records_pkey` — `CREATE UNIQUE INDEX physical_payment_records_pkey ON public.physical_payment_records USING btree (id)`

## `physical_reimbursements`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `refund_id` | `uuid` | No | — | Refund id. |
| `physical_payment_record_id` | `uuid` | No | — | Physical payment record id. |
| `recorded_by_user_id` | `bigint` | No | — | Recorded by user id. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `method` | `character varying` | No | — | Method. |
| `state` | `character varying` | No | `'VENDOR_REIMBURSEMENT_PENDING'::character varying` | State. |
| `evidence_file_id` | `uuid` | Yes | — | Evidence file id. |
| `reimbursed_at` | `timestamp with time zone` | Yes | — | Reimbursed at. |
| `buyer_acknowledged_at` | `timestamp with time zone` | Yes | — | Buyer acknowledged at. |
| `confirmed_by_user_id` | `bigint` | Yes | — | Confirmed by user id. |
| `confirmation_reason` | `text` | Yes | — | Confirmation reason. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `physical_reimbursement_amount_check` — CHECK: `CHECK (amount_centavos > 0)`
- `physical_reimbursement_state_check` — CHECK: `CHECK (state::text = ANY (ARRAY['VENDOR_REIMBURSEMENT_PENDING'::character varying, 'REIMBURSEMENT_CONFIRMED'::character varying]::text[]))`
- `physical_reimbursements_confirmed_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (confirmed_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `physical_reimbursements_evidence_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (evidence_file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `physical_reimbursements_physical_payment_record_id_foreign` — FOREIGN KEY: `FOREIGN KEY (physical_payment_record_id) REFERENCES physical_payment_records(id) ON DELETE RESTRICT`
- `physical_reimbursements_recorded_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (recorded_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `physical_reimbursements_refund_id_foreign` — FOREIGN KEY: `FOREIGN KEY (refund_id) REFERENCES refunds(id) ON DELETE RESTRICT`
- `physical_reimbursements_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `physical_reimbursements_refund_id_unique` — UNIQUE: `UNIQUE (refund_id)`

**Indexes**

- `physical_reimbursements_pkey` — `CREATE UNIQUE INDEX physical_reimbursements_pkey ON public.physical_reimbursements USING btree (id)`
- `physical_reimbursements_refund_id_unique` — `CREATE UNIQUE INDEX physical_reimbursements_refund_id_unique ON public.physical_reimbursements USING btree (refund_id)`

## `pickup_authorizations`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `fulfillment_id` | `uuid` | No | — | Fulfillment id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `event_type` | `character varying` | Yes | — | Event type. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `occurred_at` | `timestamp with time zone` | Yes | — | Occurred at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `pickup_authorizations_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `pickup_authorizations_fulfillment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fulfillment_id) REFERENCES fulfillments(id) ON DELETE RESTRICT`
- `pickup_authorizations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `pickup_authorizations_pkey` — `CREATE UNIQUE INDEX pickup_authorizations_pkey ON public.pickup_authorizations USING btree (id)`

## `place_cache_entries`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `cache_key_hash` | `character varying` | No | — | Cache key hash. |
| `payload` | `jsonb` | No | — | Payload. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `provider` | `character varying` | No | `'GOOGLE'::character varying` | Provider. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `place_cache_entries_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `place_cache_entries_cache_key_hash_unique` — UNIQUE: `UNIQUE (cache_key_hash)`

**Indexes**

- `place_cache_entries_cache_key_hash_unique` — `CREATE UNIQUE INDEX place_cache_entries_cache_key_hash_unique ON public.place_cache_entries USING btree (cache_key_hash)`
- `place_cache_entries_pkey` — `CREATE UNIQUE INDEX place_cache_entries_pkey ON public.place_cache_entries USING btree (id)`

## `platform_roles`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `code` | `character varying` | No | — | Code. |
| `platform` | `character varying` | No | — | Platform. |
| `name` | `character varying` | No | — | Name. |
| `system_role` | `boolean` | No | `true` | System role. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `platform_roles_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `platform_roles_code_unique` — UNIQUE: `UNIQUE (code)`

**Indexes**

- `platform_roles_code_unique` — `CREATE UNIQUE INDEX platform_roles_code_unique ON public.platform_roles USING btree (code)`
- `platform_roles_pkey` — `CREATE UNIQUE INDEX platform_roles_pkey ON public.platform_roles USING btree (id)`

## `platform_settings`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `key` | `character varying` | No | — | Key. |
| `value` | `jsonb` | No | — | Value. |
| `version` | `integer` | No | — | Version. |
| `updated_by_user_id` | `bigint` | Yes | — | Updated by user id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `platform_settings_updated_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (updated_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `platform_settings_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `platform_settings_key_unique` — UNIQUE: `UNIQUE (key)`

**Indexes**

- `platform_settings_key_unique` — `CREATE UNIQUE INDEX platform_settings_key_unique ON public.platform_settings USING btree (key)`
- `platform_settings_pkey` — `CREATE UNIQUE INDEX platform_settings_pkey ON public.platform_settings USING btree (id)`

## `price_observations`

MAT-01–MAT-07 comparability, observation, publication, or audience-safe aggregate record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `analytics_refresh_run_id` | `uuid` | No | — | Analytics refresh run id. |
| `source_kind` | `character varying` | No | `'PUBLIC_LISTING'::character varying` | Source kind. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `dataset_id` | `character varying` | No | — | Dataset id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `listing_variant_id` | `uuid` | No | — | Listing variant id. |
| `listing_price_version_id` | `uuid` | No | — | Listing price version id. |
| `comparable_group_version_id` | `uuid` | Yes | — | Comparable group version id. |
| `observed_at` | `timestamp with time zone` | No | — | Observed at. |
| `local_snapshot_date` | `date` | No | — | Local snapshot date. |
| `ordinary_payable_centavos` | `bigint` | No | — | Integer Philippine centavos. Ordinary payable centavos. |
| `normalized_php_price` | `numeric` | No | — | Normalized php price. |
| `tax_category` | `character varying` | No | — | Tax category. |
| `address_id` | `uuid` | No | — | Address id. |
| `address_version` | `integer` | No | — | Address version. |
| `eligibility_version` | `character varying` | No | — | Eligibility version. |
| `selected_for_average` | `boolean` | No | `false` | Selected for average. |
| `exclusion_reason` | `character varying` | Yes | — | Exclusion reason. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `recorded_store_geography` | `geography` | Yes | — | Recorded store geography. |

**Constraints**

- `price_observation_address_version_check` — CHECK: `CHECK (address_version >= 1)`
- `price_observation_amount_check` — CHECK: `CHECK (ordinary_payable_centavos > 0 AND normalized_php_price > 0::numeric)`
- `price_observation_date_check` — CHECK: `CHECK (local_snapshot_date = (observed_at AT TIME ZONE 'Asia/Manila'::text)::date)`
- `price_observation_environment_check` — CHECK: `CHECK (environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying]::text[]))`
- `price_observation_selection_check` — CHECK: `CHECK (selected_for_average = false OR comparable_group_version_id IS NOT NULL AND exclusion_reason IS NULL)`
- `price_observation_source_check` — CHECK: `CHECK (source_kind::text = 'PUBLIC_LISTING'::text)`
- `price_observations_address_id_foreign` — FOREIGN KEY: `FOREIGN KEY (address_id) REFERENCES addresses(id) ON DELETE RESTRICT`
- `price_observations_analytics_refresh_run_id_foreign` — FOREIGN KEY: `FOREIGN KEY (analytics_refresh_run_id) REFERENCES analytics_refresh_runs(id) ON DELETE RESTRICT`
- `price_observations_comparable_group_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (comparable_group_version_id) REFERENCES material_comparable_group_versions(id) ON DELETE RESTRICT`
- `price_observations_listing_price_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (listing_price_version_id) REFERENCES listing_price_versions(id) ON DELETE RESTRICT`
- `price_observations_listing_variant_id_foreign` — FOREIGN KEY: `FOREIGN KEY (listing_variant_id) REFERENCES listing_variants(id) ON DELETE RESTRICT`
- `price_observations_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `price_observations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `price_observations_analytics_refresh_run_id_listing_variant_id_` — UNIQUE: `UNIQUE (analytics_refresh_run_id, listing_variant_id)`

**Indexes**

- `price_observations_analytics_refresh_run_id_listing_variant_id_` — `CREATE UNIQUE INDEX price_observations_analytics_refresh_run_id_listing_variant_id_ ON public.price_observations USING btree (analytics_refresh_run_id, listing_variant_id)`
- `price_observations_environment_dataset_id_local_snapshot_date_i` — `CREATE INDEX price_observations_environment_dataset_id_local_snapshot_date_i ON public.price_observations USING btree (environment, dataset_id, local_snapshot_date)`
- `price_observations_geography_gist` — `CREATE INDEX price_observations_geography_gist ON public.price_observations USING gist (recorded_store_geography)`
- `price_observations_group_date_index` — `CREATE INDEX price_observations_group_date_index ON public.price_observations USING btree (environment, dataset_id, comparable_group_version_id, local_snapshot_date)`
- `price_observations_pkey` — `CREATE UNIQUE INDEX price_observations_pkey ON public.price_observations USING btree (id)`
- `price_observations_selected_vendor_group_unique` — `CREATE UNIQUE INDEX price_observations_selected_vendor_group_unique ON public.price_observations USING btree (analytics_refresh_run_id, vendor_organization_id, comparable_group_version_id) WHERE (selected_for_average = true)`

## `privacy_requests`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `request_type` | `character varying` | No | — | Request type. |
| `state` | `character varying` | No | — | State. |
| `retention_exception` | `text` | Yes | — | Retention exception. |
| `completed_at` | `timestamp with time zone` | Yes | — | Completed at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `privacy_requests_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `privacy_requests_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `privacy_requests_pkey` — `CREATE UNIQUE INDEX privacy_requests_pkey ON public.privacy_requests USING btree (id)`

## `processing_fee_snapshots`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `payment_id` | `uuid` | No | — | Payment id. |
| `channel` | `character varying` | No | — | Channel. |
| `quoted_centavos` | `bigint` | No | — | Integer Philippine centavos. Quoted centavos. |
| `calculation` | `jsonb` | No | — | Calculation. |
| `policy_version` | `character varying` | No | — | Policy version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `processing_fee_snapshots_payment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (payment_id) REFERENCES payments(id) ON DELETE RESTRICT`
- `processing_fee_snapshots_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `processing_fee_snapshots_payment_id_unique` — UNIQUE: `UNIQUE (payment_id)`

**Indexes**

- `processing_fee_snapshots_payment_id_unique` — `CREATE UNIQUE INDEX processing_fee_snapshots_payment_id_unique ON public.processing_fee_snapshots USING btree (payment_id)`
- `processing_fee_snapshots_pkey` — `CREATE UNIQUE INDEX processing_fee_snapshots_pkey ON public.processing_fee_snapshots USING btree (id)`

## `products`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `bigint` | No | `nextval('products_id_seq'::regclass)` | Primary key. Id. |
| `name` | `character varying` | No | — | Name. |
| `description` | `text` | Yes | — | Description. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `public_id` | `uuid` | No | — | Public id. |
| `material_id` | `uuid` | Yes | — | Material id. |
| `brand` | `character varying` | Yes | — | Brand. |
| `manufacturer` | `character varying` | Yes | — | Manufacturer. |
| `country_of_manufacture` | `character varying` | Yes | — | Country of manufacture. |
| `lock_version` | `integer` | No | `1` | Lock version. |

**Constraints**

- `products_material_id_foreign` — FOREIGN KEY: `FOREIGN KEY (material_id) REFERENCES materials(id) ON DELETE RESTRICT`
- `products_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `products_public_id_unique` — UNIQUE: `UNIQUE (public_id)`

**Indexes**

- `products_pkey` — `CREATE UNIQUE INDEX products_pkey ON public.products USING btree (id)`
- `products_public_id_unique` — `CREATE UNIQUE INDEX products_public_id_unique ON public.products USING btree (public_id)`

## `project_sites`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `project_id` | `uuid` | No | — | Project id. |
| `address_id` | `uuid` | No | — | Address id. |
| `active` | `boolean` | No | `true` | Active. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `project_sites_address_id_foreign` — FOREIGN KEY: `FOREIGN KEY (address_id) REFERENCES addresses(id) ON DELETE RESTRICT`
- `project_sites_project_id_foreign` — FOREIGN KEY: `FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE RESTRICT`
- `project_sites_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `project_sites_project_id_address_id_unique` — UNIQUE: `UNIQUE (project_id, address_id)`

**Indexes**

- `project_sites_pkey` — `CREATE UNIQUE INDEX project_sites_pkey ON public.project_sites USING btree (id)`
- `project_sites_project_id_address_id_unique` — `CREATE UNIQUE INDEX project_sites_project_id_address_id_unique ON public.project_sites USING btree (project_id, address_id)`

## `projects`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `buyer_profile_id` | `uuid` | No | — | Buyer profile id. |
| `name` | `character varying` | No | — | Name. |
| `status` | `character varying` | No | `'ACTIVE'::character varying` | Status. |
| `budget_centavos` | `bigint` | No | — | Integer Philippine centavos. Budget centavos. |
| `starts_on` | `date` | Yes | — | Starts on. |
| `ends_on` | `date` | Yes | — | Ends on. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `projects_budget_check` — CHECK: `CHECK (budget_centavos >= 0)`
- `projects_buyer_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (buyer_profile_id) REFERENCES buyer_profiles(id) ON DELETE RESTRICT`
- `projects_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `projects_pkey` — `CREATE UNIQUE INDEX projects_pkey ON public.projects USING btree (id)`

## `psgc_areas`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `psgc_version_id` | `uuid` | No | — | Psgc version id. |
| `code` | `character varying` | No | — | Code. |
| `name` | `character varying` | No | — | Name. |
| `level` | `character varying` | No | — | Level. |
| `parent_id` | `uuid` | Yes | — | Parent id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `psgc_areas_parent_id_foreign` — FOREIGN KEY: `FOREIGN KEY (parent_id) REFERENCES psgc_areas(id) ON DELETE RESTRICT`
- `psgc_areas_psgc_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (psgc_version_id) REFERENCES psgc_versions(id) ON DELETE RESTRICT`
- `psgc_areas_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `psgc_areas_psgc_version_id_code_unique` — UNIQUE: `UNIQUE (psgc_version_id, code)`

**Indexes**

- `psgc_areas_pkey` — `CREATE UNIQUE INDEX psgc_areas_pkey ON public.psgc_areas USING btree (id)`
- `psgc_areas_psgc_version_id_code_unique` — `CREATE UNIQUE INDEX psgc_areas_psgc_version_id_code_unique ON public.psgc_areas USING btree (psgc_version_id, code)`

## `psgc_boundaries`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `psgc_area_id` | `uuid` | No | — | Psgc area id. |
| `geometry_wkt` | `text` | No | — | Geometry wkt. |
| `content_hash` | `character varying` | No | — | Content hash. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `psgc_boundaries_psgc_area_id_foreign` — FOREIGN KEY: `FOREIGN KEY (psgc_area_id) REFERENCES psgc_areas(id) ON DELETE RESTRICT`
- `psgc_boundaries_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `psgc_boundaries_psgc_area_id_unique` — UNIQUE: `UNIQUE (psgc_area_id)`

**Indexes**

- `psgc_boundaries_pkey` — `CREATE UNIQUE INDEX psgc_boundaries_pkey ON public.psgc_boundaries USING btree (id)`
- `psgc_boundaries_psgc_area_id_unique` — `CREATE UNIQUE INDEX psgc_boundaries_psgc_area_id_unique ON public.psgc_boundaries USING btree (psgc_area_id)`

## `psgc_versions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `version` | `character varying` | No | — | Version. |
| `effective_on` | `date` | No | — | Effective on. |
| `source_reference` | `character varying` | No | — | Source reference. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `psgc_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `psgc_versions_version_unique` — UNIQUE: `UNIQUE (version)`

**Indexes**

- `psgc_versions_pkey` — `CREATE UNIQUE INDEX psgc_versions_pkey ON public.psgc_versions USING btree (id)`
- `psgc_versions_version_unique` — `CREATE UNIQUE INDEX psgc_versions_version_unique ON public.psgc_versions USING btree (version)`

## `quotation_changes`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `quotation_id` | `uuid` | No | — | Quotation id. |
| `quotation_version_id` | `uuid` | Yes | — | Quotation version id. |
| `actor_user_id` | `bigint` | No | — | Actor user id. |
| `event_type` | `character varying` | Yes | — | Event type. |
| `payload` | `jsonb` | No | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `quotation_changes_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `quotation_changes_quotation_id_foreign` — FOREIGN KEY: `FOREIGN KEY (quotation_id) REFERENCES quotations(id) ON DELETE RESTRICT`
- `quotation_changes_quotation_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (quotation_version_id) REFERENCES quotation_versions(id) ON DELETE RESTRICT`
- `quotation_changes_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `quotation_changes_pkey` — `CREATE UNIQUE INDEX quotation_changes_pkey ON public.quotation_changes USING btree (id)`

## `quotation_counter_offers`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `quotation_id` | `uuid` | No | — | Quotation id. |
| `quotation_version_id` | `uuid` | Yes | — | Quotation version id. |
| `actor_user_id` | `bigint` | No | — | Actor user id. |
| `event_type` | `character varying` | Yes | — | Event type. |
| `payload` | `jsonb` | No | — | Payload. |
| `state` | `character varying` | No | `'OPEN'::character varying` | State. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `quotation_counter_offers_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `quotation_counter_offers_quotation_id_foreign` — FOREIGN KEY: `FOREIGN KEY (quotation_id) REFERENCES quotations(id) ON DELETE RESTRICT`
- `quotation_counter_offers_quotation_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (quotation_version_id) REFERENCES quotation_versions(id) ON DELETE RESTRICT`
- `quotation_counter_offers_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `quotation_counter_offers_pkey` — `CREATE UNIQUE INDEX quotation_counter_offers_pkey ON public.quotation_counter_offers USING btree (id)`

## `quotation_events`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `quotation_id` | `uuid` | No | — | Quotation id. |
| `quotation_version_id` | `uuid` | Yes | — | Quotation version id. |
| `actor_user_id` | `bigint` | No | — | Actor user id. |
| `event_type` | `character varying` | Yes | — | Event type. |
| `payload` | `jsonb` | No | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `quotation_events_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `quotation_events_quotation_id_foreign` — FOREIGN KEY: `FOREIGN KEY (quotation_id) REFERENCES quotations(id) ON DELETE RESTRICT`
- `quotation_events_quotation_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (quotation_version_id) REFERENCES quotation_versions(id) ON DELETE RESTRICT`
- `quotation_events_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `quotation_events_pkey` — `CREATE UNIQUE INDEX quotation_events_pkey ON public.quotation_events USING btree (id)`

## `quotation_lines`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `quotation_version_id` | `uuid` | No | — | Quotation version id. |
| `listing_variant_id` | `uuid` | Yes | — | Listing variant id. |
| `description` | `character varying` | No | — | Description. |
| `unit_id` | `uuid` | No | — | Unit id. |
| `quantity` | `numeric` | No | — | Quantity. |
| `unit_price_centavos` | `bigint` | No | — | Integer Philippine centavos. Unit price centavos. |
| `subtotal_centavos` | `bigint` | No | — | Integer Philippine centavos. Subtotal centavos. |
| `substitution` | `boolean` | No | `false` | Substitution. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `quotation_line_amounts_check` — CHECK: `CHECK (quantity > 0::numeric AND unit_price_centavos >= 0 AND subtotal_centavos >= 0)`
- `quotation_lines_listing_variant_id_foreign` — FOREIGN KEY: `FOREIGN KEY (listing_variant_id) REFERENCES listing_variants(id) ON DELETE RESTRICT`
- `quotation_lines_quotation_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (quotation_version_id) REFERENCES quotation_versions(id) ON DELETE RESTRICT`
- `quotation_lines_unit_id_foreign` — FOREIGN KEY: `FOREIGN KEY (unit_id) REFERENCES units(id) ON DELETE RESTRICT`
- `quotation_lines_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `quotation_lines_pkey` — `CREATE UNIQUE INDEX quotation_lines_pkey ON public.quotation_lines USING btree (id)`

## `quotation_versions`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `quotation_id` | `uuid` | No | — | Quotation id. |
| `version` | `integer` | No | — | Version. |
| `created_by_user_id` | `bigint` | No | — | Created by user id. |
| `published_at` | `timestamp with time zone` | Yes | — | Published at. |
| `expires_at` | `timestamp with time zone` | Yes | — | Expires at. |
| `supersedes_version_id` | `uuid` | Yes | — | Supersedes version id. |
| `materials_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Materials centavos. |
| `delivery_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Delivery centavos. |
| `nrpc_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Nrpc centavos. |
| `total_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Total centavos. |
| `content_hash` | `character varying` | No | — | Content hash. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `quotation_totals_check` — CHECK: `CHECK (materials_centavos >= 0 AND delivery_centavos >= 0 AND nrpc_centavos >= 0 AND nrpc_centavos <= materials_centavos AND total_centavos >= (materials_centavos + delivery_centavos))`
- `quotation_versions_created_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (created_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `quotation_versions_quotation_id_foreign` — FOREIGN KEY: `FOREIGN KEY (quotation_id) REFERENCES quotations(id) ON DELETE RESTRICT`
- `quotation_versions_supersedes_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (supersedes_version_id) REFERENCES quotation_versions(id) ON DELETE RESTRICT`
- `quotation_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `quotation_versions_quotation_id_version_unique` — UNIQUE: `UNIQUE (quotation_id, version)`

**Indexes**

- `quotation_versions_pkey` — `CREATE UNIQUE INDEX quotation_versions_pkey ON public.quotation_versions USING btree (id)`
- `quotation_versions_quotation_id_version_unique` — `CREATE UNIQUE INDEX quotation_versions_quotation_id_version_unique ON public.quotation_versions USING btree (quotation_id, version)`

## `quotations`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `conversation_id` | `uuid` | No | — | Conversation id. |
| `buyer_profile_id` | `uuid` | No | — | Buyer profile id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `work_package_id` | `uuid` | Yes | — | Work package id. |
| `procurement_type` | `character varying` | No | — | Procurement type. |
| `state` | `character varying` | No | `'DRAFT'::character varying` | State. |
| `current_version_id` | `uuid` | Yes | — | Current version id. |
| `accepted_order_id` | `uuid` | Yes | — | Accepted order id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `quotations_accepted_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (accepted_order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `quotations_buyer_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (buyer_profile_id) REFERENCES buyer_profiles(id) ON DELETE RESTRICT`
- `quotations_conversation_id_foreign` — FOREIGN KEY: `FOREIGN KEY (conversation_id) REFERENCES conversations(id) ON DELETE RESTRICT`
- `quotations_current_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (current_version_id) REFERENCES quotation_versions(id) ON DELETE RESTRICT`
- `quotations_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `quotations_work_package_id_foreign` — FOREIGN KEY: `FOREIGN KEY (work_package_id) REFERENCES work_packages(id) ON DELETE RESTRICT`
- `quotations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `quotations_pkey` — `CREATE UNIQUE INDEX quotations_pkey ON public.quotations USING btree (id)`

## `recovery_codes`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `code_hash` | `character varying` | No | — | Code hash. |
| `used_at` | `timestamp with time zone` | Yes | — | Used at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `recovery_codes_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `recovery_codes_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `recovery_codes_user_id_code_hash_unique` — UNIQUE: `UNIQUE (user_id, code_hash)`

**Indexes**

- `recovery_codes_pkey` — `CREATE UNIQUE INDEX recovery_codes_pkey ON public.recovery_codes USING btree (id)`
- `recovery_codes_user_id_code_hash_unique` — `CREATE UNIQUE INDEX recovery_codes_user_id_code_hash_unique ON public.recovery_codes USING btree (user_id, code_hash)`

## `refund_attempts`

Payment, refund, or physical-money evidence record with an independent lifecycle.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `refund_id` | `uuid` | No | — | Refund id. |
| `provider_event_id` | `character varying` | Yes | — | Provider event id. |
| `state` | `character varying` | No | — | State. |
| `safe_payload` | `jsonb` | Yes | — | Safe payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `refund_attempts_refund_id_foreign` — FOREIGN KEY: `FOREIGN KEY (refund_id) REFERENCES refunds(id) ON DELETE RESTRICT`
- `refund_attempts_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `refund_attempts_provider_event_id_unique` — UNIQUE: `UNIQUE (provider_event_id)`

**Indexes**

- `refund_attempts_pkey` — `CREATE UNIQUE INDEX refund_attempts_pkey ON public.refund_attempts USING btree (id)`
- `refund_attempts_provider_event_id_unique` — `CREATE UNIQUE INDEX refund_attempts_provider_event_id_unique ON public.refund_attempts USING btree (provider_event_id)`

## `refund_events`

Payment, refund, or physical-money evidence record with an independent lifecycle.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `refund_id` | `uuid` | No | — | Refund id. |
| `provider_event_id` | `character varying` | Yes | — | Provider event id. |
| `state` | `character varying` | No | — | State. |
| `safe_payload` | `jsonb` | Yes | — | Safe payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `refund_events_refund_id_foreign` — FOREIGN KEY: `FOREIGN KEY (refund_id) REFERENCES refunds(id) ON DELETE RESTRICT`
- `refund_events_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `refund_events_provider_event_id_unique` — UNIQUE: `UNIQUE (provider_event_id)`

**Indexes**

- `refund_events_pkey` — `CREATE UNIQUE INDEX refund_events_pkey ON public.refund_events USING btree (id)`
- `refund_events_provider_event_id_unique` — `CREATE UNIQUE INDEX refund_events_provider_event_id_unique ON public.refund_events USING btree (provider_event_id)`

## `refunds`

Payment, refund, or physical-money evidence record with an independent lifecycle.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `target_type` | `character varying` | No | `'ORDER'::character varying` | Target type. |
| `order_id` | `uuid` | Yes | — | Order id. |
| `fee_statement_id` | `uuid` | Yes | — | Fee statement id. |
| `source_payment_id` | `uuid` | No | — | Source payment id. |
| `fee_adjustment_id` | `uuid` | Yes | — | Fee adjustment id. |
| `trigger` | `character varying` | No | — | Trigger. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `source_captured_centavos` | `bigint` | No | — | Integer Philippine centavos. Source captured centavos. |
| `prior_allocated_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Prior allocated centavos. |
| `state` | `character varying` | No | `'REFUND_PENDING'::character varying` | State. |
| `idempotency_key` | `character varying` | No | — | Idempotency key. |
| `provider_reference` | `character varying` | Yes | — | Provider reference. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `refund_amount_positive_check` — CHECK: `CHECK (amount_centavos > 0)`
- `refund_source_allocation_check` — CHECK: `CHECK (source_captured_centavos >= 0 AND prior_allocated_centavos >= 0 AND prior_allocated_centavos <= source_captured_centavos AND amount_centavos <= (source_captured_centavos - prior_allocated_centavos))`
- `refund_target_trigger_check` — CHECK: `CHECK (target_type::text = 'ORDER'::text AND (trigger::text = ANY (ARRAY['CANCELLATION'::character varying, 'DISPUTE_CONCLUSION'::character varying, 'TECHNICAL_COMPENSATION'::character varying]::text[])) AND order_id IS NOT NULL OR target_type::text = 'PLATFORM_FEE'::text AND trigger::text = 'FEE_CREDIT'::text AND fee_statement_id IS NOT NULL AND fee_adjustment_id IS NOT NULL)`
- `refunds_fee_adjustment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fee_adjustment_id) REFERENCES fee_adjustments(id) ON DELETE RESTRICT`
- `refunds_fee_statement_id_foreign` — FOREIGN KEY: `FOREIGN KEY (fee_statement_id) REFERENCES fee_statements(id) ON DELETE RESTRICT`
- `refunds_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `refunds_source_payment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (source_payment_id) REFERENCES payments(id) ON DELETE RESTRICT`
- `refunds_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `refunds_idempotency_key_unique` — UNIQUE: `UNIQUE (idempotency_key)`
- `refunds_provider_reference_unique` — UNIQUE: `UNIQUE (provider_reference)`

**Indexes**

- `refunds_idempotency_key_unique` — `CREATE UNIQUE INDEX refunds_idempotency_key_unique ON public.refunds USING btree (idempotency_key)`
- `refunds_pkey` — `CREATE UNIQUE INDEX refunds_pkey ON public.refunds USING btree (id)`
- `refunds_provider_reference_unique` — `CREATE UNIQUE INDEX refunds_provider_reference_unique ON public.refunds USING btree (provider_reference)`

## `regulated_material_rules`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `material_id` | `uuid` | No | — | Material id. |
| `required_marking` | `character varying` | No | — | Required marking. |
| `source_reference` | `character varying` | No | — | Source reference. |
| `effective_from` | `date` | No | — | Effective from. |
| `effective_to` | `date` | Yes | — | Effective to. |
| `version` | `integer` | No | — | Version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `regulated_material_rules_material_id_foreign` — FOREIGN KEY: `FOREIGN KEY (material_id) REFERENCES materials(id) ON DELETE RESTRICT`
- `regulated_material_rules_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `regulated_material_rules_material_id_version_unique` — UNIQUE: `UNIQUE (material_id, version)`

**Indexes**

- `regulated_material_rules_material_id_version_unique` — `CREATE UNIQUE INDEX regulated_material_rules_material_id_version_unique ON public.regulated_material_rules USING btree (material_id, version)`
- `regulated_material_rules_pkey` — `CREATE UNIQUE INDEX regulated_material_rules_pkey ON public.regulated_material_rules USING btree (id)`

## `remittance_assessments`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `remittance_group_id` | `uuid` | No | — | Remittance group id. |
| `financial_snapshot_id` | `uuid` | No | — | Financial snapshot id. |
| `order_id` | `uuid` | No | — | Order id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `taxpayer_key_hash` | `character varying` | No | — | Taxpayer key hash. |
| `obligation_type` | `character varying` | No | — | Obligation type. |
| `assigned_withholding_entity` | `character varying` | No | — | Assigned withholding entity. |
| `collections_centavos` | `bigint` | No | — | Integer Philippine centavos. Collections centavos. |
| `refunds_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Refunds centavos. |
| `discounts_refunded_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Discounts refunded centavos. |
| `vat_refunded_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Vat refunded centavos. |
| `delivery_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Delivery centavos. |
| `vat_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Vat centavos. |
| `provider_charge_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Provider charge centavos. |
| `principal_centavos` | `bigint` | No | — | Integer Philippine centavos. Principal centavos. |
| `gross_basis_centavos` | `bigint` | No | — | Integer Philippine centavos. Gross basis centavos. |
| `withheld_centavos` | `bigint` | No | — | Integer Philippine centavos. Withheld centavos. |
| `threshold_before_centavos` | `bigint` | No | — | Integer Philippine centavos. Threshold before centavos. |
| `threshold_after_centavos` | `bigint` | No | — | Integer Philippine centavos. Threshold after centavos. |
| `effective_rate_basis_points` | `integer` | No | — | Effective rate basis points. |
| `threshold_status` | `character varying` | No | — | Threshold status. |
| `calculation_state` | `character varying` | No | `'CALCULATED'::character varying` | Calculation state. |
| `deduction_evidence_state` | `character varying` | No | `'UNCONFIRMED'::character varying` | Deduction evidence state. |
| `reconciliation_state` | `character varying` | No | `'PENDING'::character varying` | Reconciliation state. |
| `evidence_file_id` | `uuid` | Yes | — | Evidence file id. |
| `assessment_reason` | `text` | No | — | Assessment reason. |
| `calculation` | `jsonb` | No | — | Calculation. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `remittance_amounts_check` — CHECK: `CHECK (collections_centavos >= 0 AND refunds_centavos >= 0 AND refunds_centavos <= collections_centavos AND discounts_refunded_centavos >= 0 AND vat_refunded_centavos >= 0 AND delivery_centavos >= 0 AND vat_centavos >= 0 AND provider_charge_centavos >= 0 AND principal_centavos >= 0 AND gross_basis_centavos >= 0 AND withheld_centavos >= 0 AND withheld_centavos <= gross_basis_centavos AND threshold_before_centavos >= 0 AND threshold_after_centavos >= threshold_before_centavos AND effective_rate_basis_points >= 0 AND effective_rate_basis_points <= 10000)`
- `remittance_calculation_state_check` — CHECK: `CHECK (calculation_state::text = ANY (ARRAY['CALCULATED'::character varying, 'POSTED'::character varying, 'BASE_REVIEW_REQUIRED'::character varying, 'VOIDED'::character varying]::text[]))`
- `remittance_deduction_state_check` — CHECK: `CHECK (deduction_evidence_state::text = ANY (ARRAY['UNCONFIRMED'::character varying, 'SIMULATED_WITHHELD'::character varying, 'PROVIDER_REPORTED'::character varying, 'PLATFORM_EVIDENCED'::character varying]::text[]))`
- `remittance_environment_check` — CHECK: `CHECK (environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying, 'LIVE'::character varying]::text[]))`
- `remittance_reconciliation_state_check` — CHECK: `CHECK (reconciliation_state::text = ANY (ARRAY['PENDING'::character varying, 'RECONCILED'::character varying, 'RECONCILIATION_EXCEPTION'::character varying]::text[]))`
- `remittance_assessments_evidence_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (evidence_file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `remittance_assessments_financial_snapshot_id_foreign` — FOREIGN KEY: `FOREIGN KEY (financial_snapshot_id) REFERENCES financial_snapshots(id) ON DELETE RESTRICT`
- `remittance_assessments_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `remittance_assessments_remittance_group_id_foreign` — FOREIGN KEY: `FOREIGN KEY (remittance_group_id) REFERENCES remittance_groups(id) ON DELETE RESTRICT`
- `remittance_assessments_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `remittance_assessment_scope_unique` — UNIQUE: `UNIQUE (environment, taxpayer_key_hash, remittance_group_id, obligation_type)`

**Indexes**

- `remittance_assessment_scope_unique` — `CREATE UNIQUE INDEX remittance_assessment_scope_unique ON public.remittance_assessments USING btree (environment, taxpayer_key_hash, remittance_group_id, obligation_type)`
- `remittance_assessments_pkey` — `CREATE UNIQUE INDEX remittance_assessments_pkey ON public.remittance_assessments USING btree (id)`

## `remittance_collections`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `remittance_group_id` | `uuid` | No | — | Remittance group id. |
| `payment_id` | `uuid` | No | — | Payment id. |
| `collected_centavos` | `bigint` | No | — | Integer Philippine centavos. Collected centavos. |
| `source_principal_centavos` | `bigint` | No | — | Integer Philippine centavos. Source principal centavos. |
| `prior_allocated_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Prior allocated centavos. |
| `collected_at` | `timestamp with time zone` | No | — | Collected at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `remittance_collection_allocation_check` — CHECK: `CHECK (source_principal_centavos >= 0 AND prior_allocated_centavos >= 0 AND collected_centavos > 0 AND (prior_allocated_centavos + collected_centavos) <= source_principal_centavos)`
- `remittance_collections_payment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (payment_id) REFERENCES payments(id) ON DELETE RESTRICT`
- `remittance_collections_remittance_group_id_foreign` — FOREIGN KEY: `FOREIGN KEY (remittance_group_id) REFERENCES remittance_groups(id) ON DELETE RESTRICT`
- `remittance_collections_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `remittance_collections_remittance_group_id_payment_id_unique` — UNIQUE: `UNIQUE (remittance_group_id, payment_id)`

**Indexes**

- `remittance_collections_pkey` — `CREATE UNIQUE INDEX remittance_collections_pkey ON public.remittance_collections USING btree (id)`
- `remittance_collections_remittance_group_id_payment_id_unique` — `CREATE UNIQUE INDEX remittance_collections_remittance_group_id_payment_id_unique ON public.remittance_collections USING btree (remittance_group_id, payment_id)`

## `remittance_groups`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `withholding_assignment_id` | `uuid` | No | — | Withholding assignment id. |
| `tax_year` | `smallint` | No | — | Tax year. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `taxpayer_key_hash` | `character varying` | No | — | Taxpayer key hash. |
| `obligation_type` | `character varying` | No | — | Obligation type. |
| `period_key` | `character varying` | No | — | Period key. |
| `state` | `character varying` | No | `'OPEN'::character varying` | State. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `remittance_group_environment_check` — CHECK: `CHECK (environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying, 'LIVE'::character varying]::text[]))`
- `remittance_groups_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `remittance_groups_withholding_assignment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (withholding_assignment_id) REFERENCES withholding_assignments(id) ON DELETE RESTRICT`
- `remittance_groups_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `remittance_groups_vendor_organization_id_withholding_assignment` — UNIQUE: `UNIQUE (vendor_organization_id, withholding_assignment_id, period_key)`

**Indexes**

- `remittance_groups_pkey` — `CREATE UNIQUE INDEX remittance_groups_pkey ON public.remittance_groups USING btree (id)`
- `remittance_groups_vendor_organization_id_withholding_assignment` — `CREATE UNIQUE INDEX remittance_groups_vendor_organization_id_withholding_assignment ON public.remittance_groups USING btree (vendor_organization_id, withholding_assignment_id, period_key)`

## `review_media`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `review_id` | `uuid` | No | — | Review id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `review_media_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `review_media_review_id_foreign` — FOREIGN KEY: `FOREIGN KEY (review_id) REFERENCES reviews(id) ON DELETE RESTRICT`
- `review_media_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `review_media_pkey` — `CREATE UNIQUE INDEX review_media_pkey ON public.review_media USING btree (id)`

## `review_moderation`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `review_id` | `uuid` | No | — | Review id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `review_moderation_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `review_moderation_review_id_foreign` — FOREIGN KEY: `FOREIGN KEY (review_id) REFERENCES reviews(id) ON DELETE RESTRICT`
- `review_moderation_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `review_moderation_pkey` — `CREATE UNIQUE INDEX review_moderation_pkey ON public.review_moderation USING btree (id)`

## `reviews`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `reviewer_user_id` | `bigint` | No | — | Reviewer user id. |
| `review_type` | `character varying` | No | — | Review type. |
| `order_line_id` | `uuid` | Yes | — | Order line id. |
| `rating` | `smallint` | No | — | Rating. |
| `comment` | `text` | Yes | — | Comment. |
| `revealed_at` | `timestamp with time zone` | Yes | — | Revealed at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `reviews_rating_check` — CHECK: `CHECK (rating >= 1 AND rating <= 5)`
- `reviews_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `reviews_reviewer_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reviewer_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `reviews_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `reviews_order_id_reviewer_user_id_review_type_order_line_id_uni` — UNIQUE: `UNIQUE (order_id, reviewer_user_id, review_type, order_line_id)`

**Indexes**

- `reviews_order_id_reviewer_user_id_review_type_order_line_id_uni` — `CREATE UNIQUE INDEX reviews_order_id_reviewer_user_id_review_type_order_line_id_uni ON public.reviews USING btree (order_id, reviewer_user_id, review_type, order_line_id)`
- `reviews_pkey` — `CREATE UNIQUE INDEX reviews_pkey ON public.reviews USING btree (id)`

## `role_permissions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `platform_role_id` | `uuid` | No | — | Platform role id. |
| `permission_id` | `uuid` | No | — | Permission id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `role_permissions_permission_id_foreign` — FOREIGN KEY: `FOREIGN KEY (permission_id) REFERENCES permissions(id) ON DELETE RESTRICT`
- `role_permissions_platform_role_id_foreign` — FOREIGN KEY: `FOREIGN KEY (platform_role_id) REFERENCES platform_roles(id) ON DELETE RESTRICT`
- `role_permissions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `role_permissions_platform_role_id_permission_id_unique` — UNIQUE: `UNIQUE (platform_role_id, permission_id)`

**Indexes**

- `role_permissions_pkey` — `CREATE UNIQUE INDEX role_permissions_pkey ON public.role_permissions USING btree (id)`
- `role_permissions_platform_role_id_permission_id_unique` — `CREATE UNIQUE INDEX role_permissions_platform_role_id_permission_id_unique ON public.role_permissions USING btree (platform_role_id, permission_id)`

## `route_cache_entries`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `cache_key_hash` | `character varying` | No | — | Cache key hash. |
| `payload` | `jsonb` | No | — | Payload. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `provider` | `character varying` | No | `'GOOGLE'::character varying` | Provider. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `route_cache_entries_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `route_cache_entries_cache_key_hash_unique` — UNIQUE: `UNIQUE (cache_key_hash)`

**Indexes**

- `route_cache_entries_cache_key_hash_unique` — `CREATE UNIQUE INDEX route_cache_entries_cache_key_hash_unique ON public.route_cache_entries USING btree (cache_key_hash)`
- `route_cache_entries_pkey` — `CREATE UNIQUE INDEX route_cache_entries_pkey ON public.route_cache_entries USING btree (id)`

## `score_snapshots`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `window_ends_on` | `date` | No | — | Window ends on. |
| `vcs` | `numeric` | Yes | — | Vcs. |
| `mqs` | `numeric` | Yes | — | Mqs. |
| `ohs` | `numeric` | Yes | — | Ohs. |
| `vps` | `numeric` | Yes | — | Vps. |
| `sample_counts` | `jsonb` | No | — | Sample counts. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `score_ranges_check` — CHECK: `CHECK ((vcs IS NULL OR vcs >= 1::numeric AND vcs <= 5::numeric) AND (mqs IS NULL OR mqs >= 1::numeric AND mqs <= 5::numeric) AND (ohs IS NULL OR ohs >= 1::numeric AND ohs <= 5::numeric) AND (vps IS NULL OR vps >= 1::numeric AND vps <= 5::numeric))`
- `score_snapshots_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `score_snapshots_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `score_snapshots_vendor_organization_id_window_ends_on_unique` — UNIQUE: `UNIQUE (vendor_organization_id, window_ends_on)`

**Indexes**

- `score_snapshots_pkey` — `CREATE UNIQUE INDEX score_snapshots_pkey ON public.score_snapshots USING btree (id)`
- `score_snapshots_vendor_organization_id_window_ends_on_unique` — `CREATE UNIQUE INDEX score_snapshots_vendor_organization_id_window_ends_on_unique ON public.score_snapshots USING btree (vendor_organization_id, window_ends_on)`

## `sessions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `character varying` | No | — | Primary key. Id. |
| `user_id` | `bigint` | Yes | — | User id. |
| `ip_address` | `character varying` | Yes | — | Ip address. |
| `user_agent` | `text` | Yes | — | User agent. |
| `payload` | `text` | No | — | Payload. |
| `last_activity` | `integer` | No | — | Last activity. |

**Constraints**

- `sessions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `sessions_last_activity_index` — `CREATE INDEX sessions_last_activity_index ON public.sessions USING btree (last_activity)`
- `sessions_pkey` — `CREATE UNIQUE INDEX sessions_pkey ON public.sessions USING btree (id)`
- `sessions_user_id_index` — `CREATE INDEX sessions_user_id_index ON public.sessions USING btree (user_id)`

## `spatial_ref_sys`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `srid` | `integer` | No | — | Primary key. Srid. |
| `auth_name` | `character varying` | Yes | — | Auth name. |
| `auth_srid` | `integer` | Yes | — | Auth srid. |
| `srtext` | `character varying` | Yes | — | Srtext. |
| `proj4text` | `character varying` | Yes | — | Proj4text. |

**Constraints**

- `spatial_ref_sys_srid_check` — CHECK: `CHECK (srid > 0 AND srid <= 998999)`
- `spatial_ref_sys_pkey` — PRIMARY KEY: `PRIMARY KEY (srid)`

**Indexes**

- `spatial_ref_sys_pkey` — `CREATE UNIQUE INDEX spatial_ref_sys_pkey ON public.spatial_ref_sys USING btree (srid)`

## `stock_confirmation_events`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `inventory_item_id` | `uuid` | No | — | Inventory item id. |
| `actor_user_id` | `bigint` | No | — | Actor user id. |
| `confirmed_quantity` | `numeric` | No | — | Confirmed quantity. |
| `confirmed_at` | `timestamp with time zone` | No | — | Confirmed at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `stock_confirmation_events_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `stock_confirmation_events_inventory_item_id_foreign` — FOREIGN KEY: `FOREIGN KEY (inventory_item_id) REFERENCES inventory_items(id) ON DELETE RESTRICT`
- `stock_confirmation_events_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `stock_confirmation_events_pkey` — `CREATE UNIQUE INDEX stock_confirmation_events_pkey ON public.stock_confirmation_events USING btree (id)`

## `store_media`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `store_profile_id` | `uuid` | No | — | Store profile id. |
| `file_id` | `uuid` | Yes | — | File id. |
| `kind` | `character varying` | No | — | Kind. |
| `alt_text` | `character varying` | Yes | — | Alt text. |
| `sort_order` | `smallint` | No | `'0'::smallint` | Sort order. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `review_status` | `character varying` | No | `'PENDING_VERIFICATION'::character varying` | Review status. |
| `replaced_at` | `timestamp with time zone` | Yes | — | Replaced at. |

**Constraints**

- `vendor_media_review_status` — CHECK: `CHECK (review_status::text = ANY (ARRAY['PENDING_VERIFICATION'::character varying, 'APPROVED'::character varying, 'RETURN_FOR_CORRECTION'::character varying, 'REJECTED'::character varying]::text[]))`
- `store_media_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `store_media_store_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (store_profile_id) REFERENCES store_profiles(id) ON DELETE RESTRICT`
- `store_media_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `store_media_current_approved_identity_unique` — `CREATE UNIQUE INDEX store_media_current_approved_identity_unique ON public.store_media USING btree (store_profile_id, kind) WHERE ((replaced_at IS NULL) AND ((review_status)::text = 'APPROVED'::text) AND ((kind)::text = ANY ((ARRAY['LOGO'::character varying, 'BANNER'::character varying])::text[])))`
- `store_media_current_pending_identity_unique` — `CREATE UNIQUE INDEX store_media_current_pending_identity_unique ON public.store_media USING btree (store_profile_id, kind) WHERE ((replaced_at IS NULL) AND ((review_status)::text = 'PENDING_VERIFICATION'::text) AND ((kind)::text = ANY ((ARRAY['LOGO'::character varying, 'BANNER'::character varying])::text[])))`
- `store_media_pkey` — `CREATE UNIQUE INDEX store_media_pkey ON public.store_media USING btree (id)`

## `store_profiles`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `address_id` | `uuid` | Yes | — | Address id. |
| `description` | `text` | Yes | — | Description. |
| `public_email` | `character varying` | Yes | — | Public email. |
| `public_phone` | `character varying` | Yes | — | Public phone. |
| `pickup_enabled` | `boolean` | No | `false` | Pickup enabled. |
| `delivery_enabled` | `boolean` | No | `false` | Delivery enabled. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `cod_enabled` | `boolean` | No | `false` | Cod enabled. |
| `in_store_payment_enabled` | `boolean` | No | `false` | In store payment enabled. |
| `lock_version` | `integer` | No | `1` | Lock version. |

**Constraints**

- `store_profiles_address_id_foreign` — FOREIGN KEY: `FOREIGN KEY (address_id) REFERENCES addresses(id) ON DELETE RESTRICT`
- `store_profiles_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `store_profiles_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `store_profiles_vendor_organization_id_unique` — UNIQUE: `UNIQUE (vendor_organization_id)`

**Indexes**

- `store_profiles_pkey` — `CREATE UNIQUE INDEX store_profiles_pkey ON public.store_profiles USING btree (id)`
- `store_profiles_vendor_organization_id_unique` — `CREATE UNIQUE INDEX store_profiles_vendor_organization_id_unique ON public.store_profiles USING btree (vendor_organization_id)`

## `tax_adjustments`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | Yes | — | Order id. |
| `refund_id` | `uuid` | Yes | — | Refund id. |
| `remittance_assessment_id` | `uuid` | Yes | — | Remittance assessment id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `original_record_type` | `character varying` | No | — | Original record type. |
| `original_record_id` | `uuid` | No | — | Original record id. |
| `period_start` | `date` | No | — | Period start. |
| `period_end` | `date` | No | — | Period end. |
| `kind` | `character varying` | No | — | Kind. |
| `amount_centavos` | `bigint` | No | — | Integer Philippine centavos. Amount centavos. |
| `before_values` | `jsonb` | No | — | Before values. |
| `after_values` | `jsonb` | No | — | After values. |
| `reason` | `text` | No | — | Reason. |
| `state` | `character varying` | No | `'ADJUSTMENT_REQUIRED'::character varying` | State. |
| `prepared_by_user_id` | `bigint` | No | — | Prepared by user id. |
| `reviewed_by_user_id` | `bigint` | Yes | — | Reviewed by user id. |
| `evidence_file_id` | `uuid` | Yes | — | Evidence file id. |
| `reviewed_at` | `timestamp with time zone` | Yes | — | Reviewed at. |
| `posted_at` | `timestamp with time zone` | Yes | — | Posted at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `tax_adjustment_actor_check` — CHECK: `CHECK (reviewed_by_user_id IS NULL OR reviewed_by_user_id <> prepared_by_user_id)`
- `tax_adjustment_dates_check` — CHECK: `CHECK (period_start <= period_end)`
- `tax_adjustment_state_check` — CHECK: `CHECK (state::text = ANY (ARRAY['ADJUSTMENT_REQUIRED'::character varying, 'UNDER_REVIEW'::character varying, 'APPROVED'::character varying, 'POSTED'::character varying, 'REJECTED'::character varying]::text[]))`
- `tax_adjustments_evidence_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (evidence_file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `tax_adjustments_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `tax_adjustments_prepared_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (prepared_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `tax_adjustments_refund_id_foreign` — FOREIGN KEY: `FOREIGN KEY (refund_id) REFERENCES refunds(id) ON DELETE RESTRICT`
- `tax_adjustments_remittance_assessment_id_foreign` — FOREIGN KEY: `FOREIGN KEY (remittance_assessment_id) REFERENCES remittance_assessments(id) ON DELETE RESTRICT`
- `tax_adjustments_reviewed_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reviewed_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `tax_adjustments_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `tax_adjustments_pkey` — `CREATE UNIQUE INDEX tax_adjustments_pkey ON public.tax_adjustments USING btree (id)`

## `tax_certificates`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `certificate_type` | `character varying` | No | — | Certificate type. |
| `document_origin` | `character varying` | No | — | Document origin. |
| `issuer` | `character varying` | No | — | Issuer. |
| `issuer_taxpayer_key_hash` | `character varying` | Yes | — | Issuer taxpayer key hash. |
| `payee_name` | `character varying` | No | — | Payee name. |
| `payee_taxpayer_key_hash` | `character varying` | No | — | Payee taxpayer key hash. |
| `certificate_number_hash` | `character varying` | Yes | — | Certificate number hash. |
| `atc_code` | `character varying` | Yes | — | Atc code. |
| `period_start` | `date` | No | — | Period start. |
| `period_end` | `date` | No | — | Period end. |
| `file_id` | `uuid` | No | — | File id. |
| `content_hash` | `character varying` | No | — | Content hash. |
| `basis_centavos` | `bigint` | No | — | Integer Philippine centavos. Basis centavos. |
| `tax_centavos` | `bigint` | No | — | Integer Philippine centavos. Tax centavos. |
| `verification_state` | `character varying` | No | `'UNVERIFIED'::character varying` | Verification state. |
| `verified_by_user_id` | `bigint` | Yes | — | Verified by user id. |
| `verified_at` | `timestamp with time zone` | Yes | — | Verified at. |
| `authentic_evidence` | `boolean` | No | `false` | Authentic evidence. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `tax_certificate_amounts_check` — CHECK: `CHECK (basis_centavos >= 0 AND tax_centavos >= 0 AND tax_centavos <= basis_centavos)`
- `tax_certificate_evidence_check` — CHECK: `CHECK (environment::text = 'LIVE'::text OR authentic_evidence = false)`
- `tax_certificates_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `tax_certificates_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `tax_certificates_verified_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (verified_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `tax_certificates_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `tax_certificates_pkey` — `CREATE UNIQUE INDEX tax_certificates_pkey ON public.tax_certificates USING btree (id)`

## `tax_evidence`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_tax_profile_version_id` | `uuid` | No | — | Vendor tax profile version id. |
| `file_id` | `uuid` | No | — | File id. |
| `evidence_type` | `character varying` | No | — | Evidence type. |
| `origin` | `character varying` | No | — | Origin. |
| `document_hash` | `character varying` | No | — | Document hash. |
| `valid_from` | `date` | Yes | — | Valid from. |
| `valid_until` | `date` | Yes | — | Valid until. |
| `review_state` | `character varying` | No | `'PENDING'::character varying` | Review state. |
| `reviewed_by_user_id` | `bigint` | Yes | — | Reviewed by user id. |
| `reviewed_at` | `timestamp with time zone` | Yes | — | Reviewed at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `tax_evidence_validity_check` — CHECK: `CHECK (valid_until IS NULL OR valid_from IS NULL OR valid_until >= valid_from)`
- `tax_evidence_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `tax_evidence_reviewed_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reviewed_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `tax_evidence_vendor_tax_profile_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_tax_profile_version_id) REFERENCES vendor_tax_profile_versions(id) ON DELETE RESTRICT`
- `tax_evidence_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `tax_evidence_pkey` — `CREATE UNIQUE INDEX tax_evidence_pkey ON public.tax_evidence USING btree (id)`

## `tax_report_package_lines`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `tax_report_package_id` | `uuid` | No | — | Tax report package id. |
| `source_type` | `character varying` | No | — | Source type. |
| `source_id` | `uuid` | No | — | Source id. |
| `atc_code` | `character varying` | Yes | — | Atc code. |
| `basis_centavos` | `bigint` | No | — | Integer Philippine centavos. Basis centavos. |
| `tax_centavos` | `bigint` | No | — | Integer Philippine centavos. Tax centavos. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `tax_report_package_lines_tax_report_package_id_foreign` — FOREIGN KEY: `FOREIGN KEY (tax_report_package_id) REFERENCES tax_report_packages(id) ON DELETE RESTRICT`
- `tax_report_package_lines_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `tax_report_package_lines_tax_report_package_id_source_type_sour` — UNIQUE: `UNIQUE (tax_report_package_id, source_type, source_id)`

**Indexes**

- `tax_report_package_lines_pkey` — `CREATE UNIQUE INDEX tax_report_package_lines_pkey ON public.tax_report_package_lines USING btree (id)`
- `tax_report_package_lines_tax_report_package_id_source_type_sour` — `CREATE UNIQUE INDEX tax_report_package_lines_tax_report_package_id_source_type_sour ON public.tax_report_package_lines USING btree (tax_report_package_id, source_type, source_id)`

## `tax_report_packages`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `package_type` | `character varying` | No | — | Package type. |
| `period_start` | `date` | No | — | Period start. |
| `period_end` | `date` | No | — | Period end. |
| `state` | `character varying` | No | `'DRAFT'::character varying` | State. |
| `filing_calendar_version_id` | `uuid` | Yes | — | Filing calendar version id. |
| `prepared_by_user_id` | `bigint` | No | — | Prepared by user id. |
| `reviewed_by_user_id` | `bigint` | Yes | — | Reviewed by user id. |
| `file_id` | `uuid` | Yes | — | File id. |
| `content_hash` | `character varying` | Yes | — | Content hash. |
| `export_hash` | `character varying` | Yes | — | Export hash. |
| `submission_state` | `character varying` | Yes | — | Submission state. |
| `review_reason` | `text` | Yes | — | Review reason. |
| `reviewed_at` | `timestamp with time zone` | Yes | — | Reviewed at. |
| `issued_at` | `timestamp with time zone` | Yes | — | Issued at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `tax_package_actor_check` — CHECK: `CHECK (reviewed_by_user_id IS NULL OR reviewed_by_user_id <> prepared_by_user_id)`
- `tax_package_state_check` — CHECK: `CHECK ((state::text = ANY (ARRAY['DRAFT'::character varying, 'REVIEWED'::character varying, 'EXPORTED'::character varying]::text[])) AND (submission_state IS NULL OR environment::text = 'TEST'::text AND submission_state::text = 'SIMULATED_SUBMISSION_RECORDED'::text))`
- `tax_report_packages_file_id_foreign` — FOREIGN KEY: `FOREIGN KEY (file_id) REFERENCES files(id) ON DELETE RESTRICT`
- `tax_report_packages_filing_calendar_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (filing_calendar_version_id) REFERENCES filing_calendar_versions(id) ON DELETE RESTRICT`
- `tax_report_packages_prepared_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (prepared_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `tax_report_packages_reviewed_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reviewed_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `tax_report_packages_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `tax_report_packages_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `tax_report_packages_export_hash_unique` — UNIQUE: `UNIQUE (export_hash)`

**Indexes**

- `tax_report_packages_export_hash_unique` — `CREATE UNIQUE INDEX tax_report_packages_export_hash_unique ON public.tax_report_packages USING btree (export_hash)`
- `tax_report_packages_pkey` — `CREATE UNIQUE INDEX tax_report_packages_pkey ON public.tax_report_packages USING btree (id)`

## `tax_rule_versions`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `code` | `character varying` | No | — | Code. |
| `version` | `integer` | No | — | Version. |
| `source_type` | `character varying` | No | — | Source type. |
| `source_reference` | `character varying` | Yes | — | Source reference. |
| `rules` | `jsonb` | No | — | Rules. |
| `effective_from` | `timestamp with time zone` | No | — | Effective from. |
| `effective_until` | `timestamp with time zone` | Yes | — | Effective until. |
| `approved_by_user_id` | `bigint` | Yes | — | Approved by user id. |
| `approved_at` | `timestamp with time zone` | Yes | — | Approved at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `tax_rule_environment_check` — CHECK: `CHECK (environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying, 'LIVE'::character varying]::text[]))`
- `tax_rule_versions_approved_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (approved_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `tax_rule_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `tax_rule_versions_environment_code_version_unique` — UNIQUE: `UNIQUE (environment, code, version)`

**Indexes**

- `tax_rule_versions_environment_code_version_unique` — `CREATE UNIQUE INDEX tax_rule_versions_environment_code_version_unique ON public.tax_rule_versions USING btree (environment, code, version)`
- `tax_rule_versions_pkey` — `CREATE UNIQUE INDEX tax_rule_versions_pkey ON public.tax_rule_versions USING btree (id)`

## `tax_year_accumulators`

FIN-01–FIN-12 financial control and evidence record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `taxpayer_key_hash` | `character varying` | No | — | Taxpayer key hash. |
| `tax_year` | `smallint` | No | — | Tax year. |
| `platform_gross_basis_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Platform gross basis centavos. |
| `platform_withheld_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Platform withheld centavos. |
| `outside_evidence_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. Outside evidence centavos. |
| `external_declaration_scope` | `character varying` | No | `'NONE'::character varying` | External declaration scope. |
| `external_declared_total_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. External declared total centavos. |
| `external_locally_represented_centavos` | `bigint` | No | `'0'::bigint` | Integer Philippine centavos. External locally represented centavos. |
| `overlap_state` | `character varying` | No | `'CLEAR'::character varying` | Overlap state. |
| `threshold_breached` | `boolean` | No | `false` | Threshold breached. |
| `threshold_breached_at` | `timestamp with time zone` | Yes | — | Threshold breached at. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `last_locked_event_id` | `uuid` | Yes | — | Last locked event id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `tax_accumulator_environment_check` — CHECK: `CHECK (environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying, 'LIVE'::character varying]::text[]))`
- `tax_year_amounts_check` — CHECK: `CHECK (platform_gross_basis_centavos >= 0 AND platform_withheld_centavos >= 0 AND outside_evidence_centavos >= 0 AND external_declared_total_centavos >= 0 AND external_locally_represented_centavos >= 0 AND external_locally_represented_centavos <= external_declared_total_centavos)`
- `tax_year_accumulators_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `tax_year_accumulators_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `tax_accumulator_taxpayer_year_unique` — UNIQUE: `UNIQUE (environment, taxpayer_key_hash, tax_year)`

**Indexes**

- `tax_accumulator_taxpayer_year_unique` — `CREATE UNIQUE INDEX tax_accumulator_taxpayer_year_unique ON public.tax_year_accumulators USING btree (environment, taxpayer_key_hash, tax_year)`
- `tax_year_accumulators_pkey` — `CREATE UNIQUE INDEX tax_year_accumulators_pkey ON public.tax_year_accumulators USING btree (id)`

## `technical_attribute_definitions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `material_category_id` | `uuid` | No | — | Material category id. |
| `code` | `character varying` | No | — | Code. |
| `label` | `character varying` | No | — | Label. |
| `value_type` | `character varying` | No | — | Value type. |
| `required` | `boolean` | No | `false` | Required. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `technical_attribute_definitions_material_category_id_foreign` — FOREIGN KEY: `FOREIGN KEY (material_category_id) REFERENCES material_categories(id) ON DELETE RESTRICT`
- `technical_attribute_definitions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `technical_attribute_definitions_material_category_id_code_uniqu` — UNIQUE: `UNIQUE (material_category_id, code)`

**Indexes**

- `technical_attribute_definitions_material_category_id_code_uniqu` — `CREATE UNIQUE INDEX technical_attribute_definitions_material_category_id_code_uniqu ON public.technical_attribute_definitions USING btree (material_category_id, code)`
- `technical_attribute_definitions_pkey` — `CREATE UNIQUE INDEX technical_attribute_definitions_pkey ON public.technical_attribute_definitions USING btree (id)`

## `totp_factors`

Identity and secure-session record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `encrypted_secret` | `text` | No | — | Encrypted secret. |
| `confirmed_at` | `timestamp with time zone` | Yes | — | Confirmed at. |
| `last_used_at` | `timestamp with time zone` | Yes | — | Last used at. |
| `last_used_counter` | `bigint` | Yes | — | Last used counter. |
| `revoked_at` | `timestamp with time zone` | Yes | — | Revoked at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `totp_factors_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `totp_factors_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `totp_factors_user_id_unique` — UNIQUE: `UNIQUE (user_id)`

**Indexes**

- `totp_factors_pkey` — `CREATE UNIQUE INDEX totp_factors_pkey ON public.totp_factors USING btree (id)`
- `totp_factors_user_id_unique` — `CREATE UNIQUE INDEX totp_factors_user_id_unique ON public.totp_factors USING btree (user_id)`

## `trusted_devices`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `device_id` | `uuid` | No | — | Device id. |
| `device_name` | `character varying` | Yes | — | Device name. |
| `fingerprint_hash` | `character varying` | No | — | Fingerprint hash. |
| `trusted_until` | `timestamp with time zone` | No | — | Trusted until. |
| `revoked_at` | `timestamp with time zone` | Yes | — | Revoked at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `trusted_devices_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `trusted_devices_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `trusted_devices_user_id_device_id_unique` — UNIQUE: `UNIQUE (user_id, device_id)`

**Indexes**

- `trusted_devices_pkey` — `CREATE UNIQUE INDEX trusted_devices_pkey ON public.trusted_devices USING btree (id)`
- `trusted_devices_user_id_device_id_unique` — `CREATE UNIQUE INDEX trusted_devices_user_id_device_id_unique ON public.trusted_devices USING btree (user_id, device_id)`

## `unit_conversions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `material_id` | `uuid` | Yes | — | Material id. |
| `from_unit_id` | `uuid` | No | — | From unit id. |
| `to_unit_id` | `uuid` | No | — | To unit id. |
| `multiplier` | `numeric` | No | — | Multiplier. |
| `version` | `integer` | No | `1` | Version. |
| `approved_by_user_id` | `bigint` | Yes | — | Approved by user id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `unit_conversion_positive_check` — CHECK: `CHECK (multiplier > 0::numeric)`
- `unit_conversions_approved_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (approved_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `unit_conversions_from_unit_id_foreign` — FOREIGN KEY: `FOREIGN KEY (from_unit_id) REFERENCES units(id) ON DELETE RESTRICT`
- `unit_conversions_material_id_foreign` — FOREIGN KEY: `FOREIGN KEY (material_id) REFERENCES materials(id) ON DELETE RESTRICT`
- `unit_conversions_to_unit_id_foreign` — FOREIGN KEY: `FOREIGN KEY (to_unit_id) REFERENCES units(id) ON DELETE RESTRICT`
- `unit_conversions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `unit_conversions_material_id_from_unit_id_to_unit_id_version_un` — UNIQUE: `UNIQUE (material_id, from_unit_id, to_unit_id, version)`

**Indexes**

- `unit_conversions_material_id_from_unit_id_to_unit_id_version_un` — `CREATE UNIQUE INDEX unit_conversions_material_id_from_unit_id_to_unit_id_version_un ON public.unit_conversions USING btree (material_id, from_unit_id, to_unit_id, version)`
- `unit_conversions_pkey` — `CREATE UNIQUE INDEX unit_conversions_pkey ON public.unit_conversions USING btree (id)`

## `units`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `code` | `character varying` | No | — | Code. |
| `name` | `character varying` | No | — | Name. |
| `dimension` | `character varying` | No | — | Dimension. |
| `precision` | `smallint` | No | `'4'::smallint` | Precision. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `units_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `units_code_unique` — UNIQUE: `UNIQUE (code)`

**Indexes**

- `units_code_unique` — `CREATE UNIQUE INDEX units_code_unique ON public.units USING btree (code)`
- `units_pkey` — `CREATE UNIQUE INDEX units_pkey ON public.units USING btree (id)`

## `user_profiles`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `user_id` | `bigint` | No | — | User id. |
| `full_name` | `character varying` | No | — | Full name. |
| `mobile_e164` | `character varying` | Yes | — | Mobile e164. |
| `locale` | `character varying` | No | `'en'::character varying` | Locale. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `user_profiles_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `user_profiles_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `user_profiles_user_id_unique` — UNIQUE: `UNIQUE (user_id)`

**Indexes**

- `user_profiles_pkey` — `CREATE UNIQUE INDEX user_profiles_pkey ON public.user_profiles USING btree (id)`
- `user_profiles_user_id_unique` — `CREATE UNIQUE INDEX user_profiles_user_id_unique ON public.user_profiles USING btree (user_id)`

## `users`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `bigint` | No | `nextval('users_id_seq'::regclass)` | Primary key. Id. |
| `name` | `character varying` | No | — | Name. |
| `email` | `character varying` | No | — | Email. |
| `email_verified_at` | `timestamp with time zone` | Yes | — | Email verified at. |
| `password` | `character varying` | No | — | Password. |
| `remember_token` | `character varying` | Yes | — | Remember token. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `public_id` | `uuid` | No | — | Public id. |
| `account_type` | `character varying` | No | `'BUYER'::character varying` | Account type. |
| `account_status` | `character varying` | No | `'PENDING_VERIFICATION'::character varying` | Account status. |
| `last_authenticated_at` | `timestamp with time zone` | Yes | — | Last authenticated at. |
| `lock_version` | `integer` | No | `1` | Lock version. |

**Constraints**

- `users_account_type_check` — CHECK: `CHECK (account_type::text = ANY (ARRAY['BUYER'::character varying, 'VENDOR'::character varying, 'ADMIN'::character varying]::text[]))`
- `users_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `users_email_unique` — UNIQUE: `UNIQUE (email)`
- `users_public_id_unique` — UNIQUE: `UNIQUE (public_id)`

**Indexes**

- `users_account_status_index` — `CREATE INDEX users_account_status_index ON public.users USING btree (account_status)`
- `users_account_type_index` — `CREATE INDEX users_account_type_index ON public.users USING btree (account_type)`
- `users_email_normalized_unique` — `CREATE UNIQUE INDEX users_email_normalized_unique ON public.users USING btree (lower((email)::text))`
- `users_email_unique` — `CREATE UNIQUE INDEX users_email_unique ON public.users USING btree (email)`
- `users_pkey` — `CREATE UNIQUE INDEX users_pkey ON public.users USING btree (id)`
- `users_public_id_unique` — `CREATE UNIQUE INDEX users_public_id_unique ON public.users USING btree (public_id)`

## `vehicle_rate_versions`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_vehicle_id` | `uuid` | No | — | Vendor vehicle id. |
| `version` | `integer` | No | — | Version. |
| `base_fee_centavos` | `bigint` | No | — | Integer Philippine centavos. Base fee centavos. |
| `per_km_centavos` | `bigint` | No | — | Integer Philippine centavos. Per km centavos. |
| `maximum_distance_km` | `smallint` | No | — | Maximum distance km. |
| `effective_at` | `timestamp with time zone` | No | — | Effective at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `vehicle_rate_versions_vendor_vehicle_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_vehicle_id) REFERENCES vendor_vehicles(id) ON DELETE RESTRICT`
- `vehicle_rate_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `vehicle_rate_versions_vendor_vehicle_id_version_unique` — UNIQUE: `UNIQUE (vendor_vehicle_id, version)`

**Indexes**

- `vehicle_rate_versions_pkey` — `CREATE UNIQUE INDEX vehicle_rate_versions_pkey ON public.vehicle_rate_versions USING btree (id)`
- `vehicle_rate_versions_vendor_vehicle_id_version_unique` — `CREATE UNIQUE INDEX vehicle_rate_versions_vendor_vehicle_id_version_unique ON public.vehicle_rate_versions USING btree (vendor_vehicle_id, version)`

## `vendor_activation_history`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `from_status` | `character varying` | Yes | — | From status. |
| `to_status` | `character varying` | No | — | To status. |
| `reason` | `text` | Yes | — | Reason. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `rule_version` | `character varying` | Yes | — | Rule version. |
| `result` | `jsonb` | Yes | — | Result. |
| `correlation_id` | `character varying` | Yes | — | Correlation id. |

**Constraints**

- `vendor_activation_history_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `vendor_activation_history_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_activation_history_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `vendor_activation_history_pkey` — `CREATE UNIQUE INDEX vendor_activation_history_pkey ON public.vendor_activation_history USING btree (id)`

## `vendor_badge_history`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `badge_definition_id` | `uuid` | No | — | Badge definition id. |
| `earned_at` | `timestamp with time zone` | No | — | Earned at. |
| `revoked_at` | `timestamp with time zone` | Yes | — | Revoked at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `vendor_badge_history_badge_definition_id_foreign` — FOREIGN KEY: `FOREIGN KEY (badge_definition_id) REFERENCES badge_definitions(id) ON DELETE RESTRICT`
- `vendor_badge_history_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_badge_history_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `vendor_badge_history_pkey` — `CREATE UNIQUE INDEX vendor_badge_history_pkey ON public.vendor_badge_history USING btree (id)`

## `vendor_business_submissions`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `submitted_by_user_id` | `bigint` | No | — | Submitted by user id. |
| `organization_version` | `integer` | No | — | Organization version. |
| `business_information` | `jsonb` | No | — | Business information. |
| `content_hash` | `character` | No | — | Content hash. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `vendor_business_submissions_submitted_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (submitted_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `vendor_business_submissions_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_business_submissions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `vendor_business_submissions_vendor_organization_id_organization` — UNIQUE: `UNIQUE (vendor_organization_id, organization_version)`

**Indexes**

- `vendor_business_submissions_pkey` — `CREATE UNIQUE INDEX vendor_business_submissions_pkey ON public.vendor_business_submissions USING btree (id)`
- `vendor_business_submissions_vendor_organization_id_organization` — `CREATE UNIQUE INDEX vendor_business_submissions_vendor_organization_id_organization ON public.vendor_business_submissions USING btree (vendor_organization_id, organization_version)`

## `vendor_classifications`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `classification` | `character varying` | No | — | Classification. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `vendor_classifications_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_classifications_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `vendor_classifications_vendor_organization_id_classification_un` — UNIQUE: `UNIQUE (vendor_organization_id, classification)`

**Indexes**

- `vendor_classifications_pkey` — `CREATE UNIQUE INDEX vendor_classifications_pkey ON public.vendor_classifications USING btree (id)`
- `vendor_classifications_vendor_organization_id_classification_un` — `CREATE UNIQUE INDEX vendor_classifications_vendor_organization_id_classification_un ON public.vendor_classifications USING btree (vendor_organization_id, classification)`

## `vendor_confirmations`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `order_id` | `uuid` | No | — | Order id. |
| `actor_user_id` | `bigint` | Yes | — | Actor user id. |
| `state` | `character varying` | Yes | — | State. |
| `reason` | `text` | Yes | — | Reason. |
| `payload` | `jsonb` | Yes | — | Payload. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `vendor_confirmations_actor_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `vendor_confirmations_order_id_foreign` — FOREIGN KEY: `FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE RESTRICT`
- `vendor_confirmations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `vendor_confirmations_pkey` — `CREATE UNIQUE INDEX vendor_confirmations_pkey ON public.vendor_confirmations USING btree (id)`

## `vendor_contacts`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `full_name` | `character varying` | No | — | Full name. |
| `position` | `character varying` | Yes | — | Position. |
| `email` | `character varying` | Yes | — | Email. |
| `telephone_e164` | `character varying` | Yes | — | Telephone e164. |
| `authorized_representative` | `boolean` | No | `false` | Authorized representative. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `is_primary` | `boolean` | No | `false` | Is primary. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `deactivated_at` | `timestamp with time zone` | Yes | — | Deactivated at. |

**Constraints**

- `vendor_contacts_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_contacts_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `vendor_contacts_pkey` — `CREATE UNIQUE INDEX vendor_contacts_pkey ON public.vendor_contacts USING btree (id)`
- `vendor_primary_contact_unique` — `CREATE UNIQUE INDEX vendor_primary_contact_unique ON public.vendor_contacts USING btree (vendor_organization_id) WHERE (is_primary AND (deactivated_at IS NULL))`

## `vendor_invitations`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `invited_by_user_id` | `bigint` | No | — | Invited by user id. |
| `normalized_email` | `character varying` | No | — | Normalized email. |
| `role` | `character varying` | No | — | Role. |
| `can_manage_staff` | `boolean` | No | `false` | Can manage staff. |
| `token_hash` | `character varying` | No | — | Token hash. |
| `expires_at` | `timestamp with time zone` | No | — | Expires at. |
| `accepted_at` | `timestamp with time zone` | Yes | — | Accepted at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `revoked_at` | `timestamp with time zone` | Yes | — | Revoked at. |
| `full_name` | `character varying` | Yes | — | Full name. |
| `telephone_e164` | `character varying` | Yes | — | Telephone e164. |
| `accepted_by_user_id` | `bigint` | Yes | — | Accepted by user id. |
| `expiry_recorded_at` | `timestamp with time zone` | Yes | — | Expiry recorded at. |
| `lock_version` | `integer` | No | `1` | Lock version. |

**Constraints**

- `vendor_invitations_accepted_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (accepted_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `vendor_invitations_invited_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (invited_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `vendor_invitations_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_invitations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `vendor_invitations_token_hash_unique` — UNIQUE: `UNIQUE (token_hash)`

**Indexes**

- `vendor_invitations_pkey` — `CREATE UNIQUE INDEX vendor_invitations_pkey ON public.vendor_invitations USING btree (id)`
- `vendor_invitations_token_hash_unique` — `CREATE UNIQUE INDEX vendor_invitations_token_hash_unique ON public.vendor_invitations USING btree (token_hash)`

## `vendor_listings`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `product_id` | `bigint` | No | — | Product id. |
| `vendor_sku` | `character varying` | No | — | Vendor sku. |
| `display_name` | `character varying` | No | — | Display name. |
| `status` | `character varying` | No | `'DRAFT'::character varying` | Status. |
| `ordinary_public_price_available` | `boolean` | No | `false` | Ordinary public price available. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `vendor_listings_product_id_foreign` — FOREIGN KEY: `FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE RESTRICT`
- `vendor_listings_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_listings_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `vendor_listings_vendor_organization_id_vendor_sku_unique` — UNIQUE: `UNIQUE (vendor_organization_id, vendor_sku)`

**Indexes**

- `vendor_listings_pkey` — `CREATE UNIQUE INDEX vendor_listings_pkey ON public.vendor_listings USING btree (id)`
- `vendor_listings_status_index` — `CREATE INDEX vendor_listings_status_index ON public.vendor_listings USING btree (status)`
- `vendor_listings_vendor_organization_id_vendor_sku_unique` — `CREATE UNIQUE INDEX vendor_listings_vendor_organization_id_vendor_sku_unique ON public.vendor_listings USING btree (vendor_organization_id, vendor_sku)`

## `vendor_memberships`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `user_id` | `bigint` | No | — | User id. |
| `role` | `character varying` | No | — | Role. |
| `can_manage_staff` | `boolean` | No | `false` | Can manage staff. |
| `status` | `character varying` | No | `'ACTIVE'::character varying` | Status. |
| `activated_at` | `timestamp with time zone` | Yes | — | Activated at. |
| `deactivated_at` | `timestamp with time zone` | Yes | — | Deactivated at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `lock_version` | `integer` | No | `1` | Lock version. |

**Constraints**

- `vendor_manager_delegation_check` — CHECK: `CHECK (can_manage_staff = false OR role::text = 'STORE_MANAGER'::text)`
- `vendor_memberships_role_check` — CHECK: `CHECK (role::text = ANY (ARRAY['OWNER'::character varying, 'STORE_MANAGER'::character varying, 'STORE_STAFF'::character varying, 'CUSTOMER_SERVICE'::character varying, 'INVENTORY'::character varying, 'FULFILLMENT'::character varying]::text[]))`
- `vendor_memberships_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `vendor_memberships_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_memberships_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `vendor_memberships_user_id_unique` — UNIQUE: `UNIQUE (user_id)`

**Indexes**

- `vendor_memberships_pkey` — `CREATE UNIQUE INDEX vendor_memberships_pkey ON public.vendor_memberships USING btree (id)`
- `vendor_memberships_user_id_unique` — `CREATE UNIQUE INDEX vendor_memberships_user_id_unique ON public.vendor_memberships USING btree (user_id)`
- `vendor_memberships_vendor_organization_id_role_status_index` — `CREATE INDEX vendor_memberships_vendor_organization_id_role_status_index ON public.vendor_memberships USING btree (vendor_organization_id, role, status)`
- `vendor_one_active_owner_unique` — `CREATE UNIQUE INDEX vendor_one_active_owner_unique ON public.vendor_memberships USING btree (vendor_organization_id) WHERE (((role)::text = 'OWNER'::text) AND ((status)::text = 'ACTIVE'::text))`

## `vendor_onboarding_steps`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `step_code` | `character varying` | No | — | Step code. |
| `status` | `character varying` | No | `'REQUIRED'::character varying` | Status. |
| `completed_at` | `timestamp with time zone` | Yes | — | Completed at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `vendor_onboarding_steps_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_onboarding_steps_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `vendor_onboarding_steps_vendor_organization_id_step_code_unique` — UNIQUE: `UNIQUE (vendor_organization_id, step_code)`

**Indexes**

- `vendor_onboarding_steps_pkey` — `CREATE UNIQUE INDEX vendor_onboarding_steps_pkey ON public.vendor_onboarding_steps USING btree (id)`
- `vendor_onboarding_steps_vendor_organization_id_step_code_unique` — `CREATE UNIQUE INDEX vendor_onboarding_steps_vendor_organization_id_step_code_unique ON public.vendor_onboarding_steps USING btree (vendor_organization_id, step_code)`

## `vendor_organizations`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `legal_name` | `character varying` | No | — | Legal name. |
| `store_name` | `character varying` | No | — | Store name. |
| `account_status` | `character varying` | No | `'ACTIVE'::character varying` | Account status. |
| `onboarding_status` | `character varying` | No | `'NOT_STARTED'::character varying` | Onboarding status. |
| `marketplace_status` | `character varying` | No | `'NOT_ACTIVE'::character varying` | Marketplace status. |
| `bulk_order_capable` | `boolean` | No | `false` | Bulk order capable. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `business_type` | `character varying` | Yes | — | Business type. |
| `established_on` | `date` | Yes | — | Established on. |
| `business_draft` | `jsonb` | Yes | — | Business draft. |
| `bulk_capability_recorded` | `boolean` | No | `false` | Bulk capability recorded. |
| `activation_hold` | `boolean` | No | `false` | Activation hold. |
| `withholding_demo_scenario` | `character varying` | Yes | — | Withholding demo scenario. |
| `submitted_at` | `timestamp with time zone` | Yes | — | Submitted at. |
| `regulatory_evidence_required` | `boolean` | Yes | — | Regulatory evidence required. |
| `regulatory_review_basis` | `text` | Yes | — | Regulatory review basis. |

**Constraints**

- `vendor_business_type_check` — CHECK: `CHECK (business_type IS NULL OR (business_type::text = ANY (ARRAY['SOLE_PROPRIETORSHIP'::character varying, 'PARTNERSHIP'::character varying, 'CORPORATION'::character varying]::text[])))`
- `vendor_onboarding_state_check` — CHECK: `CHECK (onboarding_status::text = ANY (ARRAY['NOT_STARTED'::character varying, 'IN_PROGRESS'::character varying, 'PENDING_VERIFICATION'::character varying, 'CHANGES_REQUIRED'::character varying, 'APPROVED'::character varying, 'REJECTED'::character varying, 'COMPLETED'::character varying]::text[]))`
- `vendor_organizations_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `vendor_organizations_marketplace_status_index` — `CREATE INDEX vendor_organizations_marketplace_status_index ON public.vendor_organizations USING btree (marketplace_status)`
- `vendor_organizations_pkey` — `CREATE UNIQUE INDEX vendor_organizations_pkey ON public.vendor_organizations USING btree (id)`

## `vendor_payment_accounts`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `provider` | `character varying` | No | `'XENDIT'::character varying` | Provider. |
| `provider_account_reference` | `character varying` | Yes | — | Provider account reference. |
| `status` | `character varying` | No | `'NOT_CONNECTED'::character varying` | Status. |
| `capabilities` | `jsonb` | Yes | — | Capabilities. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `evidence_origin` | `character varying` | Yes | — | Evidence origin. |
| `checked_at` | `timestamp with time zone` | Yes | — | Checked at. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `provider_status` | `character varying` | Yes | — | Provider status. |
| `connection_attempt_id` | `uuid` | Yes | — | Connection attempt id. |
| `connection_error` | `character varying` | Yes | — | Connection error. |

**Constraints**
- `vendor_payment_accounts_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_payment_accounts_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `vendor_payment_accounts_connection_attempt_id_unique` — UNIQUE: `UNIQUE (connection_attempt_id)`
- `vendor_payment_accounts_vendor_organization_id_unique` — UNIQUE: `UNIQUE (vendor_organization_id)`
- `vendor_provider_account_unique` — UNIQUE: `UNIQUE (provider, environment, provider_account_reference)`

**Indexes**

- `vendor_payment_accounts_connection_attempt_id_unique` — `CREATE UNIQUE INDEX vendor_payment_accounts_connection_attempt_id_unique ON public.vendor_payment_accounts USING btree (connection_attempt_id)`
- `vendor_payment_accounts_pkey` — `CREATE UNIQUE INDEX vendor_payment_accounts_pkey ON public.vendor_payment_accounts USING btree (id)`
- `vendor_payment_accounts_vendor_organization_id_unique` — `CREATE UNIQUE INDEX vendor_payment_accounts_vendor_organization_id_unique ON public.vendor_payment_accounts USING btree (vendor_organization_id)`
- `vendor_provider_account_unique` — `CREATE UNIQUE INDEX vendor_provider_account_unique ON public.vendor_payment_accounts USING btree (provider, environment, provider_account_reference)`

## `vendor_public_media_recoveries`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `store_media_id` | `uuid` | Yes | — | Store media id. |
| `object_key` | `character varying` | No | — | Object key. |
| `state` | `character varying` | No | — | State. |
| `provider_asset_id` | `character varying` | Yes | — | Provider asset id. |
| `provider_format` | `character varying` | Yes | — | Provider format. |
| `last_error` | `character varying` | Yes | — | Last error. |
| `cleanup_attempts` | `smallint` | No | `'0'::smallint` | Cleanup attempts. |
| `recovered_at` | `timestamp with time zone` | Yes | — | Recovered at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `vendor_public_media_recovery_state` — CHECK: `CHECK (state::text = ANY (ARRAY['PLANNED'::character varying, 'UPLOAD_UNCONFIRMED'::character varying, 'UPLOAD_CONFIRMED'::character varying, 'LINKED'::character varying, 'CLEANUP_REQUIRED'::character varying, 'CLEANED'::character varying]::text[]))`
- `vendor_public_media_recoveries_store_media_id_foreign` — FOREIGN KEY: `FOREIGN KEY (store_media_id) REFERENCES store_media(id) ON DELETE RESTRICT`
- `vendor_public_media_recoveries_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_public_media_recoveries_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `vendor_public_media_recoveries_object_key_unique` — UNIQUE: `UNIQUE (object_key)`

**Indexes**

- `vendor_public_media_recoveries_object_key_unique` — `CREATE UNIQUE INDEX vendor_public_media_recoveries_object_key_unique ON public.vendor_public_media_recoveries USING btree (object_key)`
- `vendor_public_media_recoveries_pkey` — `CREATE UNIQUE INDEX vendor_public_media_recoveries_pkey ON public.vendor_public_media_recoveries USING btree (id)`
- `vendor_public_media_recoveries_state_updated_at_index` — `CREATE INDEX vendor_public_media_recoveries_state_updated_at_index ON public.vendor_public_media_recoveries USING btree (state, updated_at)`
- `vendor_public_media_recoveries_vendor_organization_id_state_ind` — `CREATE INDEX vendor_public_media_recoveries_vendor_organization_id_state_ind ON public.vendor_public_media_recoveries USING btree (vendor_organization_id, state)`

## `vendor_tax_profile_reviews`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_tax_profile_version_id` | `uuid` | No | — | Vendor tax profile version id. |
| `reviewer_user_id` | `bigint` | No | — | Reviewer user id. |
| `decision` | `character varying` | No | — | Decision. |
| `reason` | `text` | Yes | — | Reason. |
| `approval_scope` | `character varying` | No | — | Approval scope. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `tax_review_decision_check` — CHECK: `CHECK ((decision::text = ANY (ARRAY['APPROVE'::character varying, 'RETURN_FOR_CORRECTION'::character varying, 'REJECT'::character varying]::text[])) AND (decision::text = 'APPROVE'::text OR reason IS NOT NULL AND length(TRIM(BOTH FROM reason)) >= 3))`
- `vendor_tax_profile_reviews_reviewer_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (reviewer_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `vendor_tax_profile_reviews_vendor_tax_profile_version_id_foreig` — FOREIGN KEY: `FOREIGN KEY (vendor_tax_profile_version_id) REFERENCES vendor_tax_profile_versions(id) ON DELETE RESTRICT`
- `vendor_tax_profile_reviews_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `vendor_tax_profile_reviews_pkey` — `CREATE UNIQUE INDEX vendor_tax_profile_reviews_pkey ON public.vendor_tax_profile_reviews USING btree (id)`

## `vendor_tax_profile_versions`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_tax_profile_id` | `uuid` | No | — | Vendor tax profile id. |
| `version` | `integer` | No | — | Version. |
| `taxpayer_key_hash` | `character varying` | No | — | Taxpayer key hash. |
| `entity_class` | `character varying` | No | — | Entity class. |
| `registration_category` | `character varying` | No | — | Registration category. |
| `vat_category` | `character varying` | No | — | Vat category. |
| `fiscal_year_start_month` | `smallint` | No | `'1'::smallint` | Fiscal year start month. |
| `effective_from` | `timestamp with time zone` | No | — | Effective from. |
| `effective_until` | `timestamp with time zone` | Yes | — | Effective until. |
| `submitted_by_user_id` | `bigint` | No | — | Submitted by user id. |
| `approved_by_user_id` | `bigint` | Yes | — | Approved by user id. |
| `approved_at` | `timestamp with time zone` | Yes | — | Approved at. |
| `content_hash` | `character varying` | No | — | Content hash. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |
| `profile_data` | `jsonb` | Yes | — | Profile data. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `evidence_origin` | `character varying` | No | `'SAMPLE_DEMO'::character varying` | Evidence origin. |
| `owner_attested_at` | `timestamp with time zone` | Yes | — | Owner attested at. |

**Constraints**

- `tax_profile_approval_actor_check` — CHECK: `CHECK (approved_by_user_id IS NULL OR approved_by_user_id <> submitted_by_user_id)`
- `tax_profile_fiscal_month_check` — CHECK: `CHECK (fiscal_year_start_month >= 1 AND fiscal_year_start_month <= 12)`
- `vendor_tax_profile_versions_approved_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (approved_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `vendor_tax_profile_versions_submitted_by_user_id_foreign` — FOREIGN KEY: `FOREIGN KEY (submitted_by_user_id) REFERENCES users(id) ON DELETE RESTRICT`
- `vendor_tax_profile_versions_vendor_tax_profile_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_tax_profile_id) REFERENCES vendor_tax_profiles(id) ON DELETE RESTRICT`
- `vendor_tax_profile_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `vendor_tax_profile_versions_vendor_tax_profile_id_version_uniqu` — UNIQUE: `UNIQUE (vendor_tax_profile_id, version)`

**Indexes**

- `vendor_tax_profile_versions_pkey` — `CREATE UNIQUE INDEX vendor_tax_profile_versions_pkey ON public.vendor_tax_profile_versions USING btree (id)`
- `vendor_tax_profile_versions_vendor_tax_profile_id_version_uniqu` — `CREATE UNIQUE INDEX vendor_tax_profile_versions_vendor_tax_profile_id_version_uniqu ON public.vendor_tax_profile_versions USING btree (vendor_tax_profile_id, version)`

## `vendor_tax_profiles`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `environment` | `character varying` | No | `'TEST'::character varying` | Environment. |
| `status` | `character varying` | No | `'INCOMPLETE'::character varying` | Status. |
| `current_version_id` | `uuid` | Yes | — | Current version id. |
| `lock_version` | `integer` | No | `1` | Lock version. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `vendor_tax_profiles_environment_check` — CHECK: `CHECK (environment::text = ANY (ARRAY['TEST'::character varying, 'DEMO'::character varying, 'LIVE'::character varying]::text[]))`
- `vendor_tax_profiles_current_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (current_version_id) REFERENCES vendor_tax_profile_versions(id) ON DELETE RESTRICT`
- `vendor_tax_profiles_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_tax_profiles_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `vendor_tax_profiles_vendor_organization_id_unique` — UNIQUE: `UNIQUE (vendor_organization_id)`

**Indexes**

- `vendor_tax_profiles_pkey` — `CREATE UNIQUE INDEX vendor_tax_profiles_pkey ON public.vendor_tax_profiles USING btree (id)`
- `vendor_tax_profiles_vendor_organization_id_unique` — `CREATE UNIQUE INDEX vendor_tax_profiles_vendor_organization_id_unique ON public.vendor_tax_profiles USING btree (vendor_organization_id)`

## `vendor_vehicles`

Vendor organization, access, onboarding, or storefront record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_organization_id` | `uuid` | No | — | Vendor organization id. |
| `vehicle_type` | `character varying` | No | — | Vehicle type. |
| `name` | `character varying` | No | — | Name. |
| `capacity_kg` | `numeric` | No | — | Capacity kg. |
| `number_available` | `smallint` | No | — | Number available. |
| `active` | `boolean` | No | `true` | Active. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `vendor_vehicles_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `vendor_vehicles_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `vendor_vehicles_pkey` — `CREATE UNIQUE INDEX vendor_vehicles_pkey ON public.vendor_vehicles USING btree (id)`

## `webhook_events`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `provider` | `character varying` | No | — | Provider. |
| `provider_event_id` | `character varying` | No | — | Provider event id. |
| `payload_hash` | `character varying` | No | — | Payload hash. |
| `state` | `character varying` | No | `'RECEIVED'::character varying` | State. |
| `processed_at` | `timestamp with time zone` | Yes | — | Processed at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `webhook_events_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `webhook_events_provider_provider_event_id_unique` — UNIQUE: `UNIQUE (provider, provider_event_id)`

**Indexes**

- `webhook_events_pkey` — `CREATE UNIQUE INDEX webhook_events_pkey ON public.webhook_events USING btree (id)`
- `webhook_events_provider_provider_event_id_unique` — `CREATE UNIQUE INDEX webhook_events_provider_provider_event_id_unique ON public.webhook_events USING btree (provider, provider_event_id)`

## `withholding_assignments`

Phase 1 platform foundation record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `vendor_tax_profile_version_id` | `uuid` | No | — | Vendor tax profile version id. |
| `tax_rule_version_id` | `uuid` | No | — | Tax rule version id. |
| `atc_code` | `character varying` | Yes | — | Atc code. |
| `rate_basis_points` | `integer` | No | — | Rate basis points. |
| `effective_from` | `timestamp with time zone` | No | — | Effective from. |
| `effective_until` | `timestamp with time zone` | Yes | — | Effective until. |
| `reason` | `character varying` | No | — | Reason. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `withholding_rate_check` — CHECK: `CHECK (rate_basis_points >= 0 AND rate_basis_points <= 10000)`
- `withholding_assignments_tax_rule_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (tax_rule_version_id) REFERENCES tax_rule_versions(id) ON DELETE RESTRICT`
- `withholding_assignments_vendor_tax_profile_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (vendor_tax_profile_version_id) REFERENCES vendor_tax_profile_versions(id) ON DELETE RESTRICT`
- `withholding_assignments_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `withholding_assignments_pkey` — `CREATE UNIQUE INDEX withholding_assignments_pkey ON public.withholding_assignments USING btree (id)`

## `work_package_lines`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `work_package_version_id` | `uuid` | No | — | Work package version id. |
| `material_id` | `uuid` | No | — | Material id. |
| `unit_id` | `uuid` | No | — | Unit id. |
| `quantity` | `numeric` | No | — | Quantity. |
| `specifications` | `jsonb` | Yes | — | Specifications. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `work_package_line_quantity_check` — CHECK: `CHECK (quantity > 0::numeric)`
- `work_package_lines_material_id_foreign` — FOREIGN KEY: `FOREIGN KEY (material_id) REFERENCES materials(id) ON DELETE RESTRICT`
- `work_package_lines_unit_id_foreign` — FOREIGN KEY: `FOREIGN KEY (unit_id) REFERENCES units(id) ON DELETE RESTRICT`
- `work_package_lines_work_package_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (work_package_version_id) REFERENCES work_package_versions(id) ON DELETE RESTRICT`
- `work_package_lines_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `work_package_lines_pkey` — `CREATE UNIQUE INDEX work_package_lines_pkey ON public.work_package_lines USING btree (id)`

## `work_package_versions`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `work_package_id` | `uuid` | No | — | Work package id. |
| `version` | `integer` | No | — | Version. |
| `content_hash` | `character varying` | No | — | Content hash. |
| `locked_at` | `timestamp with time zone` | Yes | — | Locked at. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `work_package_versions_work_package_id_foreign` — FOREIGN KEY: `FOREIGN KEY (work_package_id) REFERENCES work_packages(id) ON DELETE RESTRICT`
- `work_package_versions_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`
- `work_package_versions_work_package_id_version_unique` — UNIQUE: `UNIQUE (work_package_id, version)`

**Indexes**

- `work_package_versions_pkey` — `CREATE UNIQUE INDEX work_package_versions_pkey ON public.work_package_versions USING btree (id)`
- `work_package_versions_work_package_id_version_unique` — `CREATE UNIQUE INDEX work_package_versions_work_package_id_version_unique ON public.work_package_versions USING btree (work_package_id, version)`

## `work_packages`

Procurement or immutable commerce record.

| Column | Database type | Null | Default | Key / meaning |
| --- | --- | --- | --- | --- |
| `id` | `uuid` | No | — | Primary key. Id. |
| `project_id` | `uuid` | No | — | Project id. |
| `name` | `character varying` | No | — | Name. |
| `status` | `character varying` | No | `'DRAFT'::character varying` | Status. |
| `budget_centavos` | `bigint` | No | — | Integer Philippine centavos. Budget centavos. |
| `selected_vendor_organization_id` | `uuid` | Yes | — | Selected vendor organization id. |
| `current_version_id` | `uuid` | Yes | — | Current version id. |
| `created_at` | `timestamp with time zone` | Yes | — | Created at. |
| `updated_at` | `timestamp with time zone` | Yes | — | Updated at. |

**Constraints**

- `work_packages_budget_check` — CHECK: `CHECK (budget_centavos >= 0)`
- `work_packages_current_version_id_foreign` — FOREIGN KEY: `FOREIGN KEY (current_version_id) REFERENCES work_package_versions(id) ON DELETE RESTRICT`
- `work_packages_project_id_foreign` — FOREIGN KEY: `FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE RESTRICT`
- `work_packages_selected_vendor_organization_id_foreign` — FOREIGN KEY: `FOREIGN KEY (selected_vendor_organization_id) REFERENCES vendor_organizations(id) ON DELETE RESTRICT`
- `work_packages_pkey` — PRIMARY KEY: `PRIMARY KEY (id)`

**Indexes**

- `work_packages_pkey` — `CREATE UNIQUE INDEX work_packages_pkey ON public.work_packages USING btree (id)`
