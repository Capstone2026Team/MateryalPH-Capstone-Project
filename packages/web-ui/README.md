# Web UI

Shared accessible React 19 primitives for the Vendor and Admin portals. Components use semantic utilities emitted by `@materyalph/design-tokens`; role-specific pages and authorization rules remain in their owning applications and the backend.
